Hello, everyone. Today, let's discuss the difference between a Mealy machine and a Moore machine. This is an important portion for your college or university level exams, as well as for competitive exams. Before we start, if you haven't liked the video or subscribed to the channel yet, please do so and press the bell button so you get all the latest notifications.

Let's start discussing the differences between Mealy and Moore machines. First, I will point out their formal definitions. The formal definition of a Mealy machine and a Moore machine, and how their transition tables are made—I have already explained this in a previous video, and its link is given in the description box below. You can check that topic if you want to look at the formal definitions and how they are defined using tuples and symbols. 

Let's start. First of all, the output of the Mealy machine depends on two things: the present state and the present input. Whereas, the Moore machine output is only dependent on the present state; that is, it is independent of the input. How will you know this? Let's check the diagrams. For example, we have a Mealy machine and a Moore machine here to check what the remainder is if we divide a binary string by three. That is, if you divide a binary string by three, the remainder can be zero, the remainder can be one, or the remainder can be two. For this, I have made a Mealy machine here and the same I have made a Moore machine here. 

Let's talk about the Mealy machine first. In the Mealy machine, look at the states—for example, at state $Q_0$, if you give a $0$ input, the output is $1$, or if you give a $01$ input, the output is $1$. What does this mean? Here, the output depends on the current state and the current input. 

If we come to the Moore machine, in the Moore machine, look at state $Q_0$—at state $Q_0$, whether you give a $0$ or a $1$ doesn't matter. At state $Q_0$, if any string comes, the output is $0$. That is, the output is defined inside each state itself. Look at $Q_0$, inside it, the output is defined as $0$; for $Q_1$, the output is $1$; for $Q_2$, the output is $2$. Here, in the Moore machine, it is defined inside the state. Here, depending on the state, as well as the input symbol (whether you give $0$ or $1$), on the left-hand side, whatever input you give and whatever corresponding output comes—both of these things are present here, depending on the current state and the current input.

Next, we have the length of the input string, and this is an important portion. If your input string is of length $n$, then the output string will be of length $n$. What is the reason for this? You will understand this through the diagrams. Suppose we have a string of length $n$, like $11011101$. If we process it, here, initially, let's see how the machine processes it. Here, you give $0$, or you give $1$. Regardless of whether you give an input, it consumes the input and generates some output. If you give one input, the output is generated. That means if you give, let's say, $0$, the output is $1$; if you give $1$, the output is $1$. That means for every symbol, a corresponding output is generated. 

In the case of a Moore machine, at the very beginning, you get an extra symbol—either $0$ or $1$—by default, $0$ comes in the output. This is because of the starting state $Q_0$ and the output associated with the starting state, which is $0$. And this output is independent of whether you give $0$ or $1$; by default, it comes. That is, an extra $0$ comes at the beginning, and after that, for every symbol you give, a corresponding output comes. So, this length being $n+1$ in the Moore machine is because of the starting state, because from the starting state itself, you get a $0$ output in the beginning. This is a major difference between the two.

Now, if we talk about the output function, the output function of the Mealy machine is defined as $\lambda: Q \times \Sigma \to \Delta$. If we talk about $Q$, what are the states? Suppose there are three states. So, on these three states ($Q_0$, $Q_1$, $Q_2$), it depends, and along with that, your input is also given—that is, whether you give $0$, or $1$, or $0$, or $1$, whatever arrow input you give, it depends on the corresponding input, as I pointed out in the previous point. 

However, if we talk about the Moore machine, the $\lambda$ function, which is the output function, is defined only on the state. That is, the state you are in—$Q$ is a state—its output is already defined within the symbols inside that definition. If you checked that video, you can check that previous video to get clarity about these symbols. Here, it depends only on $Q$; that is, it depends only on the $Q_0$, $Q_1$, or $Q_2$ states. That is, here, it does not depend on the input symbols. In the Mealy machine, depending on the state, as well as the input symbol you give, whether you give $0$ or $1$, different outputs come. Here, it depends on the input.

Next, if we look at the diagram clearly, a Mealy machine is asynchronous, and its output is asynchronous with the clock. If we talk about a Moore machine, the output is synchronous with the clock. If we apply a clock pulse—like when a positive edge trigger is applied—then the output is given. Whereas in the case of Mealy, it is asynchronous. That is, here, if our clock pulse is not changing even then, your output comes. That is why we can say that this machine is faster than the Moore machine. That is, this machine is a bit faster because it processes the clock—although processing the clock takes time, and even though that time is minute, if we compare the two, you can say that this machine is faster, meaning the output comes faster because it is asynchronous and not dependent on the clock. 

Finally, let's talk about their conversion. If we convert a Mealy machine to a Moore machine, then the number of states can increase. But if you convert a Moore machine to a Mealy machine, the number of states does not increase; it remains the same. That is, if there are three states in a Moore machine, when you convert it to a Mealy machine, the states can become more than three. How can they become more than three? We will learn about this conversion in the next video, where you will understand how a Moore-to-Mealy and Mealy-to-Moore conversion is done. 

So, these are all the major differences between Mealy and Moore machines. Thank you.