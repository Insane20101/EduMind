Hello friends, welcome to my channel. This is Varun Singla. In today's video, we are going to discuss about the ALTER command. As we had discussed in the last video, in DDL, the first command is about CREATE, in which we had done table creation with implementation. In this video, we are going to discuss the second command, or second DDL command, ALTER. After ALTER, we will also do its implementation in Oracle.

First of all, what can ALTER do? Meaning, what is the use of the ALTER command? The major use of the ALTER command is this: what is to be done in the ALTER command? If we want to change anything in the schema—I am focusing again and again on this schema word, I am concentrating because schema means structure. Structure means where I want to put data. We call it as structure. And what is structure? What is structure in a relational database? A table, which we also call as a relation. So, in structure data, we always create a table only to store the data.

Let's say we have a student table. This is its structure. Structure means it has two columns: ID and Name. ID, let's say, is an integer, and Name is varchar. This is already given; it is created. Because if you want to make a new thing, then to make new things, we already have the CREATE command. So, when you are making a new thing with the CREATE command, then according to your requirement, put the values. But the meaning of ALTER is the thing that is already made. If I want to make some change in that, then over there, I use what? The ALTER command.

So, suppose that over here, the student table is already made. We have already taken ID of int type. Name is already made of varchar data type. So, now what can we do in this? Let's say our requirement is to add a new column. Let's say over here, I want to add a new column, City or Address. Take anything, let's say I want to add Address. So, to add the address column, what will we have to do? We have to make a change in this structure. So, whenever you want to make a change in the structure, what do we use? The ALTER command. 

So generally, what do we do? `ALTER TABLE tablename`, let's say the table name is `student`, `ADD`, over here which keyword do we use? `ADD`. Add column name, let's say what is the column name? We have `Address`. And the address—if you will run this, then this will give an error. Reason? Because how does the compiler know the address? Our computers are not so much artificially intelligent, still, there is artificial intelligence in them that they cannot say by themselves that address will be varchar. Over here, we have to mention varchar size also, let's say size 30. You can give it. Although we will see these things in implementation, you will get an overview that what all we can do with the ALTER command. 

So, add a column or columns, which means even if you want to add more than two columns, then through one command only, through the ALTER command, you can do it simply. By adding brackets over here, you can write multiple columns. 

Then, remove a column. Let's say that in the future, we have a requirement that I don't want this particular column. Then what will you do? Will you delete the full table? No. Meaning, let's say you have a big database in which there are 30-40 columns and there are lakhs of rows, there are lakhs of tuples. So, even if by mistake you deleted the table, then this is a blunder mistake. Because in any organization, for any company, what is the most important asset? Data. Even more than employees, the most important asset for all the companies in today's time, what is it? Data. So, the data—it is not so that anyone will delete, anyone will add, no. For that, proper privileges are required. So, if we are working as an administrator, then if it comes to us that a particular column we have to remove, let's say over here the column is address, the one which we just added, or take any other column, let's say age. Tomorrow if we get [a request to] remove age, so to remove age, what will we do? And to remove a column also, we use the ALTER command. 

So, to add, remove, modify the datatype—let's say today I feel that my ID is sufficient in int data type, but after that, my number of employees increased a lot. So, instead of a number, what do I want? That it should be a combination of characters and numbers, in this way. So, to do this, you will have to make it varchar. So, varchar means at present you have made this as int, so varchar means you will have to modify what? The datatype. And when you have to modify the datatype, then we again use the ALTER command. 

Even the length of the datatype—let's say at present you have given name as size 20, `varchar(20)`. But tomorrow if you feel that the names of employees in the company are very big, then again you can increase that length by using the ALTER command. 

Then, add and remove the constraint, meaning primary key, foreign key, not null, check, default. If you want to add all these constraints after the table is created, then we do it with what? With the ALTER command. Remove also. 

And rename column. Column name, let's say if you think ID is fine, tomorrow you think that ID is not fine, it should be roll number over here, or it should be registration number. So, if you want to change such types of names, or even the full student table, if you want to change the name of the table—let's say right now the name is student, tomorrow you want to make it student detail—to do this type of changes, to do renaming again, the ALTER command is used. 

So, these are the multiple ways through which you can, through the ALTER command, perform all these tasks. So, come, we will see its implementation. 

Come, we will start with implementation. First of all, I want to tell you, over here I have written the `CREATE TABLE` command, and we are creating a table over here, `employee`, and I have added two columns, ID and Name, in it. So, first of all, we will run this, and over here we have created a table `employee`. Because when we had seen that ALTER command, we use the ALTER command after creating the table, meaning that table is already there, in that if we want to do any changes, then what is the first change that we saw? `ALTER TABLE tablename`. And first of all, suppose we want to add a new column, let's say `ALTER TABLE tablename employee ADD`, we will add a new column with the name of address. So, `Address`, along with that, it is necessary for you to give the datatype. So, over here we have mentioned the datatype as `varchar`, and we will select this and run. 

So see, the table is altered, meaning already in this table a column `address` has come. So, first of all, we will check `DESCRIBE`. If we do `employee`, then over here with the `desc` command you get to see the whole schema of the table. So, over here you see the `address`, it is already created in that. Now, ID and Name were already there, now we have also inserted `address` in this. So, first of all, what have we seen? That if we want to insert a new column in the table, then we can insert it in this way. 

Now, the second thing, what can we do? We can drop in this. Suppose we want to drop a column, we will drop `address` only, which we have just inserted. See what we have to do for that: `ALTER TABLE tablename DROP COLUMN`. With `DROP`, you have to remember you definitely have to write the `COLUMN` word, and along with that, the column name, like if we want to drop the address. Now you need not mention the datatype, because the compiler already knows it that its datatype will be varchar, or whatever we have given in this, because if we are dropping, at that time you need not mention the datatype. 

So see what is written: "Table dropped." This is actually over here a flaw of the software, the table is not dropped. If you do `DESCRIBE tablename` and you run, then see, with ID and Name, the `employee` table exists, but the `address` that was here earlier, that `address` has now been dropped. We have dropped one column over here. So, you can insert multiple columns, you can also drop multiple columns. 

What is the third thing that we can do? `ALTER TABLE tablename`, let's say `employee`, and we want to modify the datatype. Let's say ID—what is the datatype of ID? Number. So, I want to change it to, let's say, varchar. So, when we change it to varchar, then varchar and it is necessary to specify the length. We gave the length and we are running this. 

So, see what is written: "Table altered." So, we will check it. We again selected `DESCRIBE`, we are running, then see ID and Name are coming, and now what is the ID? `varchar2`. Oracle by default works with the name of `varchar2`. The default datatype in this is `varchar2`. So, according to Oracle, even if you are giving simple varchar, then also it will consider this as `varchar2` only. So, first, it was number integer, now we have changed it to varchar, even its length—like what length we have given? 10. So, if tomorrow you want to change this, you want to make any change in schema, let's say you want to make its length 30, then you just have to write it again in this command: `MODIFY column name varchar`, whatever is its datatype, and `30`. 

So, see, now we will again select this and run. So, see what has come? Over here the length mentioned is 30. So, in this way, we can add, drop, modify column datatype, and along with that, the length. 

Next, what can we do in this? Rename. `ALTER TABLE tablename employee`, and what have we done over here? Rename what? Column. Let's say over here we are renaming—the `COLUMN` word is necessary to write over here also—so `RENAME COLUMN`, let's say we are changing ID, ID to let's say we are converting it into `roll number`. Whatever name you want to change, you can change. So, ID is the old name, and `roll number` is the new name. So, select this, and we will run this also. 

So, see what is written: "Table altered." So, along with that, we will check it: `DESCRIBE tablename`. And see, we are running this, then see, first ID was coming, now what is coming over here? `roll number` is coming. So, in this way, we can add, drop, modify, and along with that, you can rename. 

So, `ALTER TABLE tablename employee`. We will also rename it a second time. `RENAME TO`, let's say, `emp`. Any small name we will give over here. So, as we run this, it is showing that the name is already used by an existing [object]. What does that mean? That a table with this name already exists. 

So, give it some new name, let's say `employee11`. Anything over here we can give. So, see, the table is altered. What have we done? `ALTER TABLE tablename RENAME TO emp`. It means, first, what had we done in this command? We had changed the column name. Now we have changed the table name only, `employee` rename to a new name, which means the new name of the table. 

So now see what we have done: `DESC DESC employee`. As we run it, what is it showing? "Object to be described could not be found," which means nothing exists with the name employee. And obviously, when we have changed the name, then see over here, the new name that we had kept, what was it? `emp11`. So, I will give this `emp11` over here, and now I will run. See what comes over here? `roll number`. That `varchar2(30)` which we made changes in this, so all those changes as it is exist, but the table name, what have we changed? `emp11`. 

So, this major, what we can do with alter is: add, drop, modify, rename column, or rename table. Other than that, one more thing that we can do, that is constraints. `ALTER TABLE tablename`, let's say if I want to give primary key, then `ADD PRIMARY KEY` to ID—but ID does not exist over here, so let's say `roll number` I want to make primary key. So, `roll number`—you have to carefully add the column name. So, as we have added the column name over here, `ADD PRIMARY KEY`, and as we are running it, then see, this is showing "Table or view does not exist." Okay, over here, `employee` name we had converted to `emp11`. So, this actually exists with this name over here. 

So, see, the table is altered. Now, over here, the primary key is added. Other than the primary key, we can add foreign key, not null, all these constraints. So, see, this nullable over here, this is ticked, which means null value can go into name, but null values cannot come in `roll number`, because we have made this as a primary key. But this primary key, foreign key, not null, default, check—all these constraints, when we will discuss in constraints, over there we will see them in detail. 

Now, in `ALTER TABLE`, this is the main thing that you have to remember: that in the table we can add, drop, modify, rename, all these major works that we can do through the ALTER command. So, this is the full concept of ALTER. Thank you.