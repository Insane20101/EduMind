B.C.N.F, Boyce-Codd Normal Form, is also called the special case of the third normal form. Because in the third normal form we have checked two conditions: there should not be transitive dependency. Transitive dependency, what does that mean? On the left side, whatever our functional dependencies are, on their left-hand side, should be the candidate key and the super key, or on the right-hand side may have a prime attribute. 

But BCNF has even more restrictions. Because as we move the table towards the BCNF, we have more restrictions. BCNF's condition is that on the left side, the left-hand side of each functional dependency, there should be the candidate key. That means the OR condition that existed in the third normal form was removed here, meaning whatever attributes are in my functional dependencies, all should be dependent on a candidate key. 

Let's examine this with a simple example. Suppose there is a student's table. We have already seen that table is in the third normal form. That means first my table should be in the third normal form, and after that, we will start examining the BCNF. Now, in the student table, there is roll number, name, voter ID, and age, and some data has been inserted into it. In this table, the candidate keys are the roll number and voter ID. The functional dependencies are: roll number goes to name, roll number goes to voter ID, voter ID goes to age, and voter ID goes to roll number. 

Now, what are the candidate keys in this table? I have already given that roll number and voter ID are the two candidate keys in the table: one is a roll number, and one is a voter ID. Now, let's look at the left side of the first functional dependency. This is the first functional dependency, this is the second one, the third one, and the fourth one. So we need to check each one by one, like we saw in the last cases in the third, second, and first normal forms. In the same way, we check all the cases in all the functional dependencies. 

So let's examine the first left-hand side: roll number. Roll number should be a candidate key, so we will be checking. Yes, in the candidate key set I have roll number, that means roll number is on the left-hand side, which is a candidate key. So, yes, this functional dependency is valid. Better yet, you just write down: the left-hand side of each functional dependency should be the candidate key or super key. It can be a super key also. 

So let's examine the second one. Again, the roll number; the left-hand side is a candidate key. Yes, the left-hand side is a candidate key, so yes, this is also valid. 

The third one: voter ID is used to determine the age of the student. What does the functional dependency mean here? We are using voter ID to determine the age, and we'll check if the left-hand side is the candidate key. As we saw in the candidate key set, yes, voter ID is also a candidate key. So yes, this is also valid according to BCNF. 

The fourth one is also valid because again it uses voter ID. So this is also valid according to the condition of BCNF. All the functional dependencies were given in this table, and you don't need to worry about designing functional dependencies, because to design functional dependencies, you need a lot of experience. When we design and model databases, we need a lot of experience for that. In practice, we never need to create functional dependencies; functional dependencies are always given to us. What are the functional dependencies a database can have? So in this case, all four are valid according to BCNF. What is its condition? The left-hand side of each functional dependency should be a candidate key or super key. So according to this, my table is in BCNF. Fine, this table is definitely in BCNF.

If we compare the normal forms we have studied till now—First Normal Form, Second, Third, and BCNF—we can see their hierarchical relationship. BCNF says that the table should be in the third normal form plus the condition that the left-hand side should be a candidate key, meaning BCNF is the innermost domain. Then we have the third normal form, which says that the table should be in the second normal form plus its condition. Then we have the second normal form, which says that the table should be in the first normal form plus some condition related to partial dependency. And the last one is the first normal form, which is the outermost. 

So if I am at the point of the second normal form, and a person is standing in the second normal form, does that mean they are in the third normal form? No, they are outside the domain of the third normal form. Many times, questions come up in this way: if a table is in the second normal form, then it must be in the third normal form. No, it is invalid, because if the table is in the second normal form, it doesn't mean that it is in the third normal form. It can be or it cannot be. To check that, you have to check the condition for transitive dependency. 

Similarly, if it is given that a table is in the third normal form, does that mean it will also be in BCNF? It is not necessary; the domain of BCNF is strictly within it. Whenever you get a question of this method, you can remember this diagram, and this will make it easy for you. In this way, you can compare these four normal forms. 

Fine, thank you.