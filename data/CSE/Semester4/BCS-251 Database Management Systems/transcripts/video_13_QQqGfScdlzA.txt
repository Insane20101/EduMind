Hello friends, welcome to Gate Smashers. The topic is the concept of super keys. Many students have requested to make a video on this topic, so here I am going to cover the whole concept with numerical examples possible, bringing that video here. 

First of all, let's start with the concept of a super key. A super key is the combination of all possible attributes which can uniquely identify two tuples in a table. Let me tell you that the candidate key is mostly used. If we talk about the whole DBMS, the concept of primary key and candidate key is mostly used. So what is the candidate key? Whatever uniquely identifies we have, meaning the set of all keys which can uniquely identify any two tuples, we call that the candidate key. But with that candidate key, we add any attribute and that becomes a super key. 

Let's take a simple example. If we have a student table in which we have roll number, name, and age—there are three attributes. Now, let's say that roll number is a candidate key. There is only one candidate key here, though candidate keys can be multiple. So what do we have as the candidate key? Roll number. If the roll number is the candidate key, then it can uniquely identify any two tuples. Roll number itself is a candidate key, and we can also call it a super key. But with the roll number, if I add any attributes—let's say with the roll number I add the name, or I add age with the roll number, or I add all three: roll number, name, and age—what will this whole thing become? A super key. Very simple concept. 

Candidate key is minimal, meaning the least. So what is the lowest? Roll number. Then this is the candidate key. You can also call it a super key. But with that, if you are adding any element, what will it become? A super key. But if I ask here, can name and age combinedly be a super key? No. A super key does not mean that if you add any two or three elements then it is a super key. No. What should be in the super key at least? A candidate key. There is no candidate key in name and age. The candidate key is the roll number. The roll number should be there; you can add anything with it, and that is a super key. In fact, what can we say? A superset of any candidate key is known as a super key.

Many times numerical questions come, and I am discussing a numerical question with you. Suppose we are given a table, $R$, in which we have $A_1, A_2, A_3$ up to $A_N$ number of attributes. How many super keys are possible if $A_1$ is a candidate key? Meaning we have this table in which there are $N$ numbers of elements, and it is given that $A_1$ is a candidate key. What do we have to do now? Let's say if nothing is given and we take its power set. A power set means how many possible subsets are possible here. 

If we have $A_1, A_2, A_3$—let's say we have three elements—what do we do in a power set? Either we consider $A_1$ or we don't. We consider it or not, meaning we have two choices for each attribute. What are the two choices? Either you take $A_1$ or don't take it. If you take $A_1$, then let's say $A_1, A_2$, or $A_1, A_3$, or $A_1, A_2, A_3$. If you don't take $A_1$, then let's say $A_2, A_3$, and in this way there are two choices each here. So how much are the total number of possibilities? 

But if we talk about this question, in this point, $A_1$ is a candidate key. Meaning it is given that in this whole table, $A_1$ will come in every set, now whatever subset you are making. $A_1$ is compulsory in it. You can put $A_2$ with $A_1$, let's say $A_1$ is alone, put $A_2$ with $A_1$, put $A_3$ with $A_1$, put $A_4$ with $A_1$. We have $N$ numbers of elements. Or make a set of three: $A_1, A_2, A_3$. Make a set of four, but $A_1$ will definitely come. So if $A_1$ will come, how many elements are left? $N - 1$. So how many options do we have for these $N-1$ elements? How many choices do we have? There are still two choices, even if you take $A_2$ or not, take $A_3$ or not, take $A_4$ or not. But you will have to take $A_1$. So how much will it come to? $2^{N-1}$. So if it is given that $A_1$ is a candidate key, then how many super keys are possible? $2^{N-1}$.

Next, if we talk about the second point, this will make you more clear here. If we talk about the second point, $A_1$ and $A_2$ both are candidate keys. It is given that this table, which has $N$ number of attributes, has two candidate keys: $A_1$ and $A_2$. How will you make it? $A_1$, $A_1, A_2$, $A_1, A_2, A_3$, $A_1, A_2, A_3, A_4$, make as many as you want. How many possibilities are there? Just now I told you that if we are taking $A_1$ alone, then you'll have to consider $A_1$. But for the rest of the attributes, you will have two choices each, whether you consider them or not. How many possibilities are there? $2^{N-1}$. What do we have for another candidate key? $A_2$. So how many possibilities are there for $A_2$ as well? $2^{N-1}$. So what will be the total? $2^{N-1} + 2^{N-1}$. 

But there is another important point here, which is: if we take $A_1$ alone, then $A_1, A_2$ will be made in this way. What will happen if we carry $A_2$ alone? Let's say $A_2$, $A_2, A_1$, $A_2, A_1, A_3$, $A_2, A_3, A_1$. Make whatever you want, make as you wish. What's my main point here? There will be some sets common here. Look at this set and this one—they are the same because there is a commutative property here. Either say $A_1, A_2$ is a candidate key or say $A_2, A_1$ is a candidate key, it's the same thing. Here you will find common sets in a lot of places. So you have to take out those common ones. 

What will be common? Common will be sets in which $A_1$ and $A_2$ will definitely come. Meaning, sets that contain $A_1$ and $A_2$. So how many will they be? If we say, in every set, $A_1$ and $A_2$ must come together, then $2^{N-2}$ choices are there, because we fixed two. Minus as much as you're fixing. Let's say you have fixed two, $A_1$ and $A_2$. How many are left? $N-2$. If you have fixed three—$A_1, A_2, A_3$ will definitely come—then how many are left? $2^{N-3}$. So just this is the way you have to do it. 

How many are there with $A_1$ alone? $2^{N-1}$. How many are there with $A_2$ alone? $2^{N-1}$. Add both of them, but what to minus? $2^{N-2}$. This is the actual answer. In this way, many times you get questions in the UGC NET and GATE. The concept of super keys is very simple, but if you get a question in this way, many times students become confused.

Let me tell you a little more about the last point. If we see another question, let's say $A_1, A_2$ and $A_3, A_4$. In this way, if it is given to us, there are two candidate keys, and the candidate keys are composite, meaning a candidate key consists of two attributes: $A_1$ and $A_2$. So if we talk about this table, which has these numbers of elements, how many possibilities do you have by combining $A_1, A_2$? How many sets can you make? How many power sets? $2^{N-2}$. I just told you the reason: $A_1$ and $A_2$ will definitely come, but from $A_3$ to $A_N$ you have two choices, so how many elements are left? $N-2$. 

Similarly, if we say $A_3$ and $A_4$ are candidate keys, then how many super keys? $2^{N-2}$. So what will be the total? $2^{N-2} + 2^{N-2}$. But here too there is the same concept. Many candidate keys will be there, and you will have many super keys which will be common—it will be in both. Let's say $A_1, A_2, A_3$ will also be in it, $A_1, A_2, A_3, A_4$ will also be in it. So that you have to minus. How much will it come to? What is the common of the four? Meaning such subsets in which these four are there, how many will they be? How many will be fixed? Do minus four: $2^{N-4}$. That's it. 

So this is all about the concept of super keys. Guys, if you like this video, then please like and share it as much as possible, and please subscribe to my channel. Thank you.