Hello friends, welcome to Gate Smashers. In this video, I'll explain the B-tree introduction part. 

If we talk about a B-tree, we call this a dynamic multilevel index. On multilevel indexing, I've already explained a question, the link of which you'll get in the description box. Generally, in a multilevel index, what we do is: our secondary memory has all our data—let's say we have a number of records. We've entered all that data in secondary records, and corresponding to that, we'll make an index. The types of indexes—all I've explained: primary index, clustered, and secondary index, what is sparse and dense—all that you'll get in the link in the description box. The introduction part of indexes you'll get there. 

So, when we make a dense index, in that, for every entry, there will be an entry in the index also. And in the index, we only keep two things: a key value and a pointer. A key value means on which basis I have to search. If we take a simple example: in your college or university, your professor wants to take online details of you—I'm not talking about offline or from a register. If any online entry has to be found in your college or university, what will he use? Which key value will he use? Either your roll number or registration number, because I know it is unique, and searching on the basis of it will be easy. One is the key value and the second is the pointer, so that the book size won't be much bigger. As at the starting of a book, at the starting of a book, you'll get a key value (meaning the topic name) and its corresponding pointer (the pointer means the page number). Then you can move to that page and see the whole description of that page, which is kept in secondary memory. 

So, in a way, you've to visualize that we have this type of architecture. Sometimes we have a larger size of an index. We create a multilevel of that also; we further make an index of an index and further make an index of that. As levels keep on increasing, inserting into this and deleting becomes difficult. Let's say I've inserted any element in secondary memory, so I've to change everywhere corresponding to that. Similarly, if we delete, corresponding to that, we've to delete the value. So this is a bit difficult. To remove that thing, or to make it better, we use a tree data structure, and in that, we specially use a balanced tree, meaning a B-tree. 

So, a B-tree is generally used for multilevel indexing, meaning our data is kept as it is, but we'll make an index of it using a tree data structure. Now, about trees, you already know that in a tree there are nodes and edges. Like we make a tree in such a way: this is called a root node, and these are all intermediate nodes, and these are your leaf nodes. Root, leaf, and whatever comes in between that is intermediate—you can call it a leaf also—and that is your root element. We enter values in nodes and connect them with edges. Cycles are not in this, because in graphs there are cycles, but a tree is acyclic. 

Now see, here we're using a slightly different structure from a standard tree. One is that it is balanced. Balanced means all the elements will be on the same level of the leaf node. Here, I can make it in such a way: is this a tree? Yes, it is also a tree, but here it is not balanced, because in this, one leaf node is here, but there is no leaf node further to this. So this is unbalanced, because this leaf is on level one and the second one is on level two, so it is not balanced. But here, there will always be balance. 

Now, whatever we talk about in a B-tree: in this, firstly we'll talk about nomenclature, meaning what values we use in this. 

First, we use a block pointer, or you can say a tree pointer. What is its work? It is used in the tree. Like if we talk, here we have a node—meaning there is a node of a tree, which we show in a round shape, but you can make it like this also, because in a normal tree we add only one value, like in this node we entered 10. Here you can add more values also in a node. Multiple values in a single node—that node further denotes its child. So, for this, which pointer is used? A block pointer or tree pointer. Like we've used here: further of this root, the left child is denoted by a block pointer, the right child is denoted by a block pointer or tree pointer. 

Second, here we talk about keys. Keys mean, as I've already told you, on which basis you have to search. So that searching criteria is the key. Where will we enter keys? We'll enter keys in these nodes. We can add multiple keys. A single key we add for sure, but we can add more than one key. 

Corresponding to that key, a data pointer will be there, or you can say record pointers. What is the use of that? Like if I make a node like this, and in this node let's say I've added the element 10, so now corresponding to 10 there will be one record pointer. What is that record pointer doing? Where is that record pointer in secondary memory kept? Here I'm only getting 10 as a key, meaning roll number 10, but corresponding to that roll number, name, father's name, mother's name, address, gender, marks, mobile number, and all—where is this whole information kept? That is in records of secondary memory. So this record pointer actually points there. 

So here, the first important point is: the record pointer will be equal to the total keys, obviously. According to every key, there will be a record pointer which will actually point to data in secondary memory. Similarly, let's say I added 20. So 20 has a corresponding record pointer which will point in such a way. 

But if we talk about the block pointer, so the block pointer is depending on what? It is depending upon how many children there are in the node. Total children will be equal to the block pointer. It has a simple architecture. 

So see, the story starts with the order. This is given to you in such a way that the order of the B-tree is $P$. So what is the order? The order is equal to the maximum number of children a node can have. The maximum number of children a node can have—that is called your order. 

So see, let's say if order 4 is given to you—let's say if order 4 is given to you—so what can you find according to this? If the order is 4, here you keep remembering this table: 
* Root: In the root, how many maximum—this is showing children—how many maximum children it can have? In the root, maximum children can be $P$, and minimum children will be 2. 
* Intermediate node: Means whether it is an in-between node or a leaf node, its maximum children can be $P$, and minimum can be $\lceil P/2 \rceil$ (ceiling value). 

So remember that how many maximum and minimum children an intermediate node can have, and the root node. For the root node it is $P$ and 2. And if the value given here is 4—the value is given that the order of the B-tree is 4. So if the order of the B-tree is 4, what is the maximum number of children? So how many maximum children can it have? So the maximum number of children will be $P$ (that is for your root node, so maximum children can be 4, and minimum can be 2). If maximum can be 4, so you can make it in such a way—see this: this is child 1, this is child 2, this is 3, and this is child 4. Maximum 4 children we can make. So obviously, who is denoting every child? The block pointer is representing it. So the block pointer will be equal to the total order. So if order $P$ is given to you, the block pointer will also be equal to $P$, if you're taking the maximum value. So obviously we'll take the maximum, because we wish to make the maximum to maximum and put the data. 

Next, if we talk about the key: if the value is given to you and $P$ is the order, so how many key values can it have? Total keys can be $P - 1$. How many total keys can come? It is $P - 1$. And how many record pointers will there be? So obviously, as I've already told, the record pointer is equal to the keys. So the record pointer will be $P - 1$, and keys will be $P - 1$. 

So what I've written in this question, that how much is the order? It is 4. So maximum 4 children there can be: $C_1, C_2, C_3, C_4$, and keys will be 1, 2, 3, and block pointers corresponding to that. Record pointers 1, 2, 3. 

Always remember that we insert keys in this in sorted order. Let's say data is given: 10, 20, 30. So you've to do like 10, 20, 30. It is in sorted way only we insert the data, because it follows the binary search property. 

And if we talk about here, this is your record pointer that is pointing towards secondary memory, and this is also a record pointer, and your block pointers we use in a B-tree. So block pointers in a tree point to the total number of children. Record pointers point to secondary memory where actually data is presented. And key values show you: the key value is showing you that on which basis you have to search, meaning what is the searching criteria for your search—that is called a key. 

So like this way, we insert data. On this, questions can come, like what is the theoretical value of the tree—all these basic-level questions can be asked from you. Thank you.