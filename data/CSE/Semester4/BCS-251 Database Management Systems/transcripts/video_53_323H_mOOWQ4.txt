Hello friends, welcome to Gate Smashers. In today's video, we are going to discuss the Introduction to SQL. Along with the introduction, we will also discuss a little about the history of SQL and its syllabus. Because many times students query what are the topics we have to cover inside SQL, whether it is for higher competitive exams or your placement, which are the topics that have the highest weightage and high priority?

First of all, what is SQL? Structured Query Language. Here, I am first picking up the word "language." Why do we generally use language? For communication. What language am I using here? I am using the Hindi language so that you guys can understand better what I want to explain to you, and you can understand that thing well. 

What is it actually? Here, you can say that there are two interfaces or two users on one side, and other users on the other side. For communication in between, what do they need? A language. In the same way, if we talk about our system, what happens in the system? On one side, there are users, and on the other side, there are databases. If we talk about structured data, the database is on the other side. How will the user interact with that database? How will they communicate? By using a language—but which language? Structured Query Language. 

And why "structured" only? Because we are talking about structure here, which means it has a fixed data model. A data model means whenever we store structured data, what do we use? Tables, or what we call relations. We also call relations tables. Therefore, we often call this subject a Relational Database Management System (RDBMS), because we always store data in the form of relations or tables. 

This means the structure that I have, the place where I have to enter the data, is already fixed—what is that? A table. It is therefore called Structured Query Language. Whenever we have to do any query on structured data, fetch the data, or store the data, what do we use? SQL, that is Structured Query Language.

And where did the concept of Structured Query Language actually start? In 1970, scientist E.F. Codd, whom we call the Father of DBMS, published a paper on the relational model. A relational model means data is stored in the form of tables. He published a paper on the relational model and tried to implement that paper in actual theory, showing that we can store data and later fetch it. 

Whose help did he take to prove this thing? They used relational algebra as well as tuple relational calculus. In actuality, he used mathematical functions, which is relational algebra, and what do we use in tuple relations as well? Mathematics. So he used these two mathematical functions, using formulas inside them, and then he showed that we can actually store data at some place and then fetch it later on the basis of that paper. 

Later on, IBM started implementation in actual. Because what had E.F. Codd given? He gave a theoretical model of how we store data—projection, and all mathematical base functions were there. But what IBM did later was convert this into a language that is Structured Query Language. Initially, they called it Simple English Query Language (SEQUEL). The name of the first Sequel used to be pronounced in this manner: "Sequel," and its meaning was Simple English Query Language. But later, what did they shorten the name to? Structured Query Language. 

If you look carefully, even inside SQL, we use keywords that are of English letters only—whether it is `select`, `from`, `where`, `group`, `having`, `order`, or `join`. Whatever word you use here, what is it all about? It is based on the English language. 

So this was a small history of SQL, because many times in exams or placements they definitely ask a little bit of history: Do you know a little history about when this thing's evolution started? When this thing started in actual? After that, it got full competition in the Oracle market, and Oracle did complete work on it. Then Microsoft made its SQL Server, and IBM launched its own DB2 different database. 

And if we talk about today's scenario, then it is totally different. Now we do not talk about SQL alone; now we talk about NoSQL. The structured data we have at today's time is very less—only ten percent of the data on this earth. What is that? Structured. Ninety percent of data is unstructured, for which we have NoSQL, Big Data, Spark, MongoDB, and all these concepts that we use in actual. But you should know the advanced, while what is basic should be known first.

If we talk about structure here, in Structured Query Language, the first feature that comes to us is that SQL is a Domain-Specific Language. What is the meaning of a Domain-Specific Language? There are two types of languages: domain-specific and general-purpose. General-purpose means that the applicability is in multiple places, like C or C++ languages; it cannot be used at just one place, you can use it in multiple areas. But if we talk about SQL, it is domain-specific. Why is it domain-specific? Because we can use it only for structured data—meaning only inside relational databases—and what can we use inside a relational database? SQL, where we have our data in the form of tables and we have to fetch it. What language do you use? SQL. There is no general use of SQL, so it is a particular domain where we use it. What is that domain? Relational databases. That's why we call it a Domain-Specific Language.

The second point here is that SQL is a Declarative Language. Again, here also there are two categories: it is declarative or procedural. What is the meaning of declarative? "What to do." "What to do" means we have to mention what to do, but "how to do" is not mentioned. But if we talk about a procedural language—like if we talk about C or C++—then what would we have there? "What to do" happens, and "how to do" happens too. That means I have to mention both things inside the program: what to do and how to do. So there we write procedures, write functions, etc. That function is actually... let me define that. Okay, suppose I want to find the factorial of any number. But how to do it? If we use a `while` loop or a `for` loop, then where is that logic also mentioned? "How to do" is inside the program. 

But here we just mention "what to do." For example, suppose I have to find out the name of students who belong to Delhi. The students who do belong to Delhi, their details have to be retrieved. Then we write a simple query: `select * from students where address = 'Delhi'`. So we only mentioned what to do, but how does it work internally? That is the database; it works on its own end. That means "what to do" is mentioned here; "how to do" we do not mention. So that's why we call it a declarative language. 

However, after that, I have an advance of SQL which is an add-on feature: PL/SQL (Procedural Language/SQL). It is actually procedural-based. But what is normal SQL? Purely a Declarative Language. 

In this, we use commands that are generally simple, like DDL, DML, DCL, and TCL. These are different different commands to create data, to insert data, to fetch, to control, and to give privileges. Which commands are there in all of them? I have already made a video on them; the link I have given in the description box. Please check it, and you will know what DDL, DML, and DCL commands actually exist inside. 

This is the first point that you will have to study within the syllabus, which will give you an initial idea of how we work within SQL. 

Here we are using keys and constraints: primary key, foreign key, candidate key—different different types of keys which are used here—and constraints like primary key, foreign key, not null, default. Different types of constraints for databases are used to help create and apply rules. 

Then operators, like `BETWEEN`, `IN`, `NOT IN`, conditional. I have mentioned a few points here so that you will get an overview of what is actually in the syllabus. In operators, we like `IN`, `BETWEEN`, `NOT IN`; they are used a lot. Apart from this, we also use `EXISTS` and `NOT EXISTS`. So all these are operators which we use internally to do queries, meaning we store the data—done. 

Now, after that, we have to fetch that data. For example, let me tell you a simple example which all of you people use: IRCTC. What has IRCTC done? They have made a strong database of their own. In the database, they have created a complete database of trains, and what do we do? We do a query. How do we do a query? We don't write a query in SQL directly; you have APIs available. So we use a direct application program interface, and we do a query like: for that particular day, I have to search any train, I have to search any seat inside that train. What is it internally in actual? It is actually a query only, which we perform on structured data. And IRCTC—what is it? It is based on structured data actually. When we open it, then if you have seen, it is shown in the form of tables. If you check from this date to this date which trains are available, which values are available, then it always shows information of trains. It is shown in the form of tables, so they can take an example of structured data here. 

Then we have clauses. In clauses, we have `DISTINCT`, `ORDER BY`, `GROUP BY`, `FROM`, `HAVING`. This also we majorly use inside a query. 

Then aggregate functions: `MIN`, `MAX`, average (`AVG`), `COUNT`. These are different aggregate functions we use. 

Then joins and nested queries. I will give the most stars to this point here, because the students who get stuck the most—where do they get stuck? In joins and nested queries, they get stuck in subqueries or nested queries. Because they clear the basic idea, but where they have to extract combined data using joins—meaning two or three tables—they get stuck because here it needs practice. How do you have to create as many tables as possible and fetch the data inside them? This point is most important from the placement point of view, I would say. It is the most important—that is, joins and nested queries. But if we talk from the point of view of competitive exams, then it is also the most important, obviously, but the rest also keep their weightage. From the point of view of placement, this is the most important thing: joins, nested queries, or subqueries (it is also called correlated queries). 

Then PL/SQL. What is PL/SQL? Procedural Language. It means that SQL is declarative, to which we have also added the procedural part in which we create triggers, create functions, create cursors. We can also create procedures, which we do in the C language—we create functions. What functions can we create? Inside SQL, but named it PL/SQL. So it is actually procedural; it tells "what to do" and "how to do it." But pure SQL tells "what to do"; you don't need to mention "how to do it" because in internal libraries, if you are using Oracle or SQL Server, their libraries are already defined as to what the function of `SELECT` is, what the role of commands is in actual. 

So this is all about the basics of SQL.