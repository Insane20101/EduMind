Hello friends, welcome to Gate Smashers. In today's video, we're going to discuss the timestamp ordering protocol in concurrency control protocols. If we talk about its further verification, the basic timestamp ordering protocol is one of the most important protocols, and this is where maximum students face confusion. 

So guys, in this video, I'll explain in simple language with examples what the timestamp ordering protocol is, how it works, and what its rules are. Please like this video and subscribe to the channel if you haven't yet, and please press the bell button so that you can get all the notifications. So let's start.

In the timestamp ordering protocol, what is a timestamp? Because the main thing here is related to the timestamp. What is a timestamp in actuality? It is a unique value assigned to every transaction. We're assigning a unique value to a transaction when it enters the system, meaning when it comes into the system. Just after entering the system, we assign a unique value or random value. What will we get from this? What is its significance and benefit? It tells the order of when they entered into the system. From this, we are getting the order of when any transaction entered in a system. 

Let's suppose here I'm writing transaction T1, and let's say T1 entered in a system at 10 o'clock. Now, exactly at 10, as soon as it entered the system, I gave it a unique value, one timestamp. Let's say I've given a timestamp of 100. You can give any value: 1, 10, 20, anything that you want; it should be unique. 

Now, at 10:10 AM/PM, meaning after 10 minutes, the T2 transaction was done. So as soon as the T2 transaction was done, I had to give a timestamp that is larger, meaning a larger timestamp. In that, you can give 101 also, 102 also, 200, or 1000, or whatever value you want to give. 

From this, what do I understand? From this, I understand that T1 came earlier. I've seen the timestamp; we are not using time here. We're only checking the timestamp. So what is the timestamp telling me here? That the T2 timestamp is larger. It means that T1 is older, and T2 is younger. From this first point, you'll understand, then you'll know things in detail. First, you should know the basic points. So T1 is older, T2 is younger, and that transaction which was done later, we provide a larger timestamp to it. Mostly it is done like this only. This question can be asked in the opposite way also, so based on the question, you can solve it, but we use this method only maximum times: that the transaction which came earlier, we gave a value to it, and the one which came later, we gave a larger value to it. And from the larger value, I understood which one is older and which one is younger. 

Let's say T3 came at 10:15. So what I'll do is, we'll give a new value to T3, let's say 300. Now you can say that the youngest transaction is T3. This we understand from the timestamp.

Secondly, here we talk about three times. 
First is the timestamp of the transaction. We denote the timestamp of the transaction like this: $TS(Ti)$, where $Ti$ is $T1$, $T2$, $T3$, or anything that you're taking as the transaction number, and $TS$ is the timestamp of the transaction. Like here, we've taken 100 and 200, meaning the timestamp of $T1$ is 100, and the timestamp of $T2$ is 200. This is the way we denote our first time. 

This is our first time, and the second time is the Read Timestamp, that is $RTS$. Understand this properly: the last transaction number which performed the read successfully, or you can say the latest transaction which has performed the read successfully, that is called the read timestamp. 

Let's suppose $T1$, $T2$, and $T3$ — we are taking 3 transactions. Let's say $T1$ transaction is 10, $T2$ is 20, and $T3$ is 30. So which is youngest? This one. And oldest is this one, that you can see. 

Let's say here I'm writing $Read(A)$, $Read(A)$, $Read(A)$ in any way you write it. See here, firstly $T1$ read. You can see from here that firstly $T1$, then $T2$, and lastly $T3$. Here, the read timestamp of $A$ — if we want to find the read timestamp of $A$, so what will be the read timestamp of $A$? So which has read firstly? $T1$, then $T2$, and lastly by $T3$. Whosoever read at last, that you can say, or the latest one that has read, that we call the read timestamp. So which one has latestly read? $T3$ has. So what is the timestamp of $T3$? It is 30. So the read timestamp of $A$ is 30. 

So this is a simple concept. Remember, whatever it is ($A$, $B$, $C$), we always find $RTS$ and $WTS$ of all variables. 

So what is $WTS$? It is only the same concept, but the last transaction or latest transaction which has performed the write operation successfully, that is our write timestamp. 

Let's suppose $T1$, $T2$, and $T3$. Firstly $T1$ has written, then $T3$, and then let's say $T2$ has performed write. In this way, a transaction schedule is given to you. Let's say $T1$ timestamp is 10, $T2$ timestamp is 20, and $T3$ is 30. Now, if you want to write $WTS(A)$ (write timestamp of $A$), so how will you write it? In this, which one has performed at last? $T1$ had done firstly (10)? No, $T3$ has done after this (30). Wait, at any time it could be 20 also, 30 can also be, but latestly which one has done? Latest is done by $T2$. So $T2$ timestamp is 20. So $WTS(A)$ will be 20. 

So these points you've to remember, because when we'll solve questions, at that time you've to use these points. So that's why these three items—read timestamp, write timestamp, and timestamp of the transaction—all these three you should know. So you can note this because after this we come to rules, which are very important. 

In the timestamp ordering protocol, in basic timestamp, we have two rules: when a transaction reads and writes, then what we have to do. So we'll start. 

Firstly, I'll tell you that you can note this. First thing I'll tell you here: let's say if we talk about the timestamp protocol, then the timestamp ordering protocol's most important point to remember is that the transaction which comes first, that transaction will complete first. We always consider this: that which transaction came first will also get completed first. Meaning we've to give priority to the older one, that the older came first, so it will be firstly completed. 

Let's say if I take two transactions, $T1$ and $T2$, and $T1$'s timestamp is 100, and $T2$'s timestamp is 200. It would create confusion, so I'll remove $T3$ from here, and the time also I'll remove from here. I'm taking a simple point here: that $T1$'s timestamp is 100 and $T2$'s timestamp is 200. Which one is older? This is the old one; this is young. So we understood. 

Now, let's say $T1$ has performed $Read(A)$, and after that $T2$ wants to do $Write(A)$. $T1$ has performed $Read(A)$, and $T2$ wants to do $Write(A)$. So we'll allow this. Why? What we're saying is that the transaction that took place earlier, we'll give priority to it. So whenever this type of problem comes—although this is a conflict, read and write, but there is no problem in this—we are saying that this is performing write, but this is younger, so no problem, it came later. So like this way, we perform $Write(A)$. 

If I'll reverse this: $T1$ has performed write, after that $T2$ has done read, then there is no problem, it is performed, because we're giving priority to the old one. 

And if I frame a third case, what will it be? $Write(A)$: this has performed write of $A$ earlier, after that younger wants to do $Write(A)$, let it do, there is no problem here. 

Read and read I've not considered, because conflict pairs are read-write, write-read, and write-write. So there is no conflict in read and read, so we don't consider it. 

But the important points are those which I've written in rules. 
If $T1$ wants $Read(A)$ earlier... sorry, $T2$ wants $Read(A)$ earlier, but $T1$ came and wants to write its value. See here, I will not allow it here. This point is saying: $T2$ has done $Read(A)$, and $T1$ says to write. So come to this rule: transaction $T1$ wants to do the $Write(A)$. As we're talking about $T1$, $T1$ came now. $T1$ let's say... this transaction came later, sorry, this transaction came earlier, but its operation is later. $T1$ is performing $Write(A)$ later. So $T1$ wants to do the $Write(A)$. So what do we have to check? Read timestamp and write timestamp both we have to check. 

So what is it telling basically? This is telling this point only: that if in any transaction, the latest... which transaction is this? Latest. If this has read, and if the former transaction wants to write, we won't allow it. It means that this transaction came earlier, so it will be completed first, and if this gets completed earlier, after write it goes to database. And if we update here, so this value will be the wrong value. Let's say this has read 10, and this value after write... let's say after writing 20 and got commit... always remember that the transaction which came earlier, that we will complete first. So if this completes first, so in database it will be 20. So which value is read by 10? This value is wrong. What we'll do with this value? We'll not let it happen. Meaning if it performs write, what we'll do with this? We'll rollback this. We'll rollback. 

Second case: $T1$ and $T2$, it is also the same case, 100 and 200. So which one is older and younger? This is older and this is younger. The second case is $Write(A)$ $Read(A)$, meaning $T2$ has already written, $T1$ came and wants to read. So which rule will be there? This one: $T1$ wants to $Read(A)$. So what will we check? Because read conflict is with write, so we'll check write, and write timestamp obviously will be more, because $WTS$ is write by $T2$, and $T2$'s timestamp is more. It means if the younger recently wrote and the older reads, what will happen because of this? Let's say this has written 20, and this has read 20. 

What I'm saying again and again: the transaction that came earlier, that will finish first, means this will get completed. This reads 20 and gets completed and commits, and this let's suppose fails in future, and if it gets fail, then it will be rolled back. So what is the validity of 20? Where is 20? This 20 is nowhere, so it worked on wrong data. So this is what we are calling... so this is a dirty read kind of problem. But in this, we're imagining that you've to give priority to the older one, the one which came earlier to that only. So in this, we'll say to read, "No, we'll not allow you to read, so you get rolled back." So in this also, we'll rollback $T1$. Both cases you should remember. 

Third case: $T1$ and $T2$, same case, 100 and 200. $T2$ let's say writes earlier, and $T1$ wants to write later on the same variable. Always keep in mind that I'm always taking $A$ only, and if you're taking $B$, then take $B$ only; don't take $A$ and $B$, because there will be no confusion between $A$ and $B$, there is no conflict. Let's say $T2$ has... younger has written, and older wants to overwrite it. So if older overwrites, obviously there will be a lost update problem; this will commit, and let's suppose this gets failed in future, so my updation will be lost. So we won't want this also to happen. So we'll rollback this one also, that $T1$, you get rolled back, we're not allowing you. 

You should know all the three cases, and you should remember this. And yes, if read is done by older and younger later writes or reads, there is no problem in that, let it happen, because we're imagining the transaction which came earlier, that will commit earlier by default. This is the assumption: the transaction which came earlier will commit first. If you don't follow this, you'll face difficulty in rest of the cases also, but you have to follow all three cases. The reason for that I'll tell is that if this is an older transaction and this is a younger transaction, then this protocol always says... how this will be serializable? $T1$ goes, $T2$. This will follow one serializability only: that older will be first and $T1$ later commits. So $T1$ goes to $T2$, what will we call it this way? Conflict serializability it follows. 

That's why we always say that if younger does anything... younger either reads or writes... here younger had read, and here has written... and older afterwards wants to perform write or read, we won't allow it. If you see from the book or from any material, you won't be able to clear this concept. Students always try to cram this. These rules are only looking big, but when I'll apply this in questions, you will find this easy. 

Always remember these three cases: that if younger has read or write, and older later wants to read and write this, so we'll not allow this. So this was the concept of the basic timestamp ordering protocol. After this, we are going to discuss a numerical; from that, you'll have an idea of how we have to apply the rules. Thank you.