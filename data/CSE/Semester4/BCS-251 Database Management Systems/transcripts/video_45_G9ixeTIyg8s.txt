Hello friends, welcome to Gate Smashers. The topic is projection, that is the pi operator. It is the first operator in relational algebra. First of all, we see its use: what is its use in the projection operator? Here, first, I have taken a table, which we can also call a relation. Now, it may be coming to your mind why we are taking a table, because we are talking about RDBMS. RDBMS means Relational Database Management System, where there are many types of databases. Out of those database types, what are we using? Relational database. Although I have object-oriented databases, network databases, multiple types of even entity-relationship data, but what am I using here? Relational database, where we represent data in tabular form, so therefore I have taken a table here. 

Now, first, we will talk about what is the function of the Pi operator, what does it actually do? So, what is the most basic functionality of the Pi operator? It is retrieving the data. Retrieving means, for example, I have written a query here: retrieve the roll number from table students. Wherever I have a column, I have to fetch data of any particular column; it can be one or more than one column. So, I have to fetch that data there, and which operator will I use first? That is projection. Here, I have to fetch roll number. To fetch roll number, how do we write in relational algebra? First of all, I have to write the table name, represented normally inside the bracket. From this student table, what I have to fetch is the roll number. So, to fetch the roll number, what am I using here? Pi. Pi means project, and project means from the whole table, separate this. Which one? What is said in the query: roll number. So, what do I write in its subscript here? Roll number. It is very simple; there is nothing complex in this. But what is this actually? This is the base, meaning you will know very well about these things so in the coming time you can handle complex queries also. 

Similarly, if I write the next query: $\pi_{\text{roll number, name}}(\text{student})$, which is the name of the table. If you are asked what is the meaning of this query, the name of the table is written, and $\pi_{\text{name}}$—what is its meaning simply? That from the database, we are searching two columns: roll number and name. And when we will execute this query, although there is no meaning to execute this because this is only a mathematical expression, but on this mathematical expression, what is there? SQL, that is Structured Query Language. So there we will see actually how queries are written and their different platforms, ways of executing those queries, so the proper result will be shown. If you have to see what is the result of this, what will be the result? A simple table will come with roll number and name, because we have written two columns, so two columns from this will come out, and their data is as it is (1, 2, 3 and A, B, A). So this is the output of this query. 

And what is the output of this query ($\pi_{\text{roll number}}(\text{student})$)? The column with roll number will come out, and the rest of the data will remain in the backend only. The data is represented, and the query which the user is writing, the output is coming. This does not mean we have divided the table; the table is in the backend, and this is showing it as output. When you remove the query, this data, this output will also move from there. 

Here, there is an important point of projection: projection always works with distinct values. What does it mean by distinct? If I have written here student roll number, what is the data in roll number? 1, 2, 3. So what will be the output? Roll number 1, 2, 3; it will give in tabular form. If we talk about RDBMS, I will have output in this tabular form. But if I do $\pi_{\text{name}}$ of name from the student table—I am just retrieving or projecting the name of students—then what output will I get? Then I will have the output A, B. This is a very important point here, because projection by default will give a unique answer; it will never show an answer from duplicate data. If from the whole I am just taking name from student because I am fetching the column with name, what output will come? A, B. Now, there are three rows (row 1, 2, 3). Here, what is the data in the row? A, B, A. But A and A are the same. Therefore, what output is it giving? A, B. When we are taking out only roll number, then what output is coming? 1, 2, 3. Obviously, 1, 2, 3 is unique, so the output will come with 3 rows. If I fetch here both roll number and name—if it came in your mind that if we fetch both roll number and name, then what will come?—then also there is no problem. This is (1, A) and this is (3, A), meaning both of these rows are different, unique; this is not duplicate. But if I just take the name alone, then yes, it is a duplicacy; this will not show duplicacy. Only A, B—meaning how many numbers of rows will it show in output? Only 2. So this is how it actually works; projection works in this way. 

I will tell you one more important point: generally, whenever you are given to write a query, we always write projection in the last, before that we use other operators, that we will see in the last. Because here we are only seeing a basic example on projection, afterwards when we will see complex queries then I will explain to you: projection we use at what level, when do we use? We use it at the last. 

So this is all about the projection. Thank you.