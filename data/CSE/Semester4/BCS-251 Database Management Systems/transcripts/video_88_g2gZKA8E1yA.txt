Hello friends! Welcome to Gate Smashers. The topic is irrecoverable schedule. On irrecoverable schedule or recoverability, actually what is the topic? Recoverability. In transactions, when we talk about schedules, then generally we classify schedules in two ways: either serializability or recoverability. And this serializability or recoverability—on both, many times questions come in all competitive exams. Meaning, if from DBMS any topic they put, and if in that they choose the topic transactions, then the most important topics in transaction are serializability and recoverability. 

So over here, we are talking about irrecoverable schedule. Irrecoverable—with the name only, we can come to know, such schedule in which any value cannot be recovered. Which are such schedules? How do we have to recognize them? Actually, what question comes is that you will be given a schedule, and you have to check whether it is recoverable or it is irrecoverable. Meaning, the ability to get recovered or it doesn't have the ability to get recovered. So these things, you just have to check by looking at the schedule. 

So what happens in an irrecoverable schedule? About that, I am telling you. In this, I have taken a schedule. We will name the schedule, let's say, $S$. And what happens in a schedule? Multiple transactions run. So over here, there are two transactions, $T1$ and $T2$, and it is a parallel schedule. In a parallel schedule, multiple transactions interfere with each other and in between execution goes on. 

So over here, when we talk about $T1$, then when $T1$ is doing `read(A)`: `read(A)`. Let's say the value of $A$ in the database is $10$. The value of $A$ in the database is $10$. When this is reading, from where will the initial read be? From the database, it will read, and it has read the value $10$. Then, an arithmetic operation is there: $A = A - 5$. Simple, we are subtracting $5$ from the value of $A$. So what is my value? $5$. `write(A)`. `write(A)` means this $5$ value that is kept in the shared memory. Now the value in the database is $10$ only. In shared memory, what is the value? $5$. 

Now `read(A)`: a second transaction came, $T2$. Now this transaction is going on. Meanwhile, $T2$ transaction came in the middle, and what did $T2$ do? `read(A)`. $T2$ did `read(A)`, so it will read from where? I have already told in the video that from where this read will be done. This read will be done from this `write`, because just now someone has done some changes. If this will also read the value $10$, then there is no benefit of this operation. So that's why the work that this has done—the new transaction that has just come—that will read this value. So what did this read? $5$. 

After reading $5$—because this has read $5$, means for this the value of $A$ is $5$—$A = A - 2$. This has also done an arithmetic operation, and `write(A)`. `write(A)` means the value of $A$ is now $3$. And as it did `commit`: what is the meaning of `commit`? `commit` means whatever changes are done, save them in the database permanently. Because we want to save it permanently—durability property. So according to the durability property, we will save these $3$ values permanently inside the database. So this has completed its work. $T2$ is committed. $T2$ completed his work and told that I have updated the value. Now what is the value of $A$ in the database? $3$. 

But still, $T1$ was running. Still, $T1$ is not completed. $T1$ is doing its work. While doing, $T1$ did `read(B)`. It read some value of $B$. Let's say the value of $B$ in the database is $20$. It read the value of $B$. Now it was running, but over here, at this point of time, this transaction failed due to some hardware failure, software failure, or any reason. There can be many outages—planned outages are there, unplanned outages are there, or natural disaster, whatever the reason. If this transaction failed at this point of time, as the transaction fails, the atomicity property—you should remember these properties, already my video is there named ACID properties, please check it once so that you come to know that the keywords durability or atomicity, why am I saying these words. 

So what does the atomicity property say? Either all or none. Meaning, either all the operations will be performed, or not even a single will be performed. So this transaction failed at this point. As it failed, what do we have to do? Rollback. And what is the meaning of rollback? Do undo. Absolutely! From here, bring it here at this point of time. If time is increasing like this, then bring it on $0$ time, means when $T1$ had started. So when $T1$ was started, at that time what was the value of $A$? $10$. So in the database, what is the value of $A$ again? $10$. 

Why? Because $T1$ failed. $T1$ failed, so we did rollback. Doing rollback means now $T1$ does not exist. Now $T1$ is nowhere. So if $T1$ is nowhere, then all the changes that it had done—all those changes should be undone. So what is the latest value of $A$? $10$. 

But if you see carefully, $T2$—$T2$ also had something. What had $T2$ done? Read the value of $A$ from $T1$ and then did some change in it. But where is that change? Now where is that change? That change is lost. It cannot be recovered. It cannot be recovered. So this type of schedule is called an irrecoverable schedule.