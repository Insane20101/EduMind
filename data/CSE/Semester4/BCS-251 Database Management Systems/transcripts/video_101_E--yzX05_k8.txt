Hello friends, welcome to Gate Smashers. The topic is indexing. Indexing from the GATE and UGC point of view, or from the point of view of other competitive exams, is very much important, but it is mostly important in interviews. Whenever a company comes to take an interview and they ask questions from a database, indexing and hashing topics are very favorite topics. But here, how indexing works, what it is, and what its use is are later topics. We will start this topic from the very basic architecture, why indexing is actually used, and before that, we'll talk about architecture and on which scenario we're talking. 

In this video, I'll firstly clear the scenario: what is the scenario in actual? Where does indexing exist? So, we'll start the video here. I have an architecture, general architecture in our laptop and computer and in our devices, that we have a CPU. CPU, which is a processor—we call it a processor—which has to process queries. Queries come to CPU, and we talk in terms of a database. So, in a database, we call a query, meaning a question is coming, that "select * from the table where this," "find the name of the student whose age is this," "find the detail of student whose roll number is this." Like this way, there are many different queries. So, those queries are handled and processed by CPU.

Now, if a query is coming, a simple query: `select * from student table where roll number is equal to 1`. I've written a basic query: `select * from the student where roll no is 1`. Meaning, from the student table, find out the detail of roll number 1, meaning its name, mother's name, father's name, age, gender, marks, anything whatever in the database, take out the whole record. Now, this query is executed by CPU. Is the data of the student table with the CPU? No, data is saved in memory, and there are many types of memory, but in general architecture, we consider using two memories: first one is RAM, and second is secondary memory. 

RAM is volatile, meaning RAM directly works with CPU because CPU speed is generally in MIPS (Million Instructions Per Second), and hard disk is very slow as compared to CPU. Because CPU speed and hard disk speed are not compatible with each other, the data is in the hard disk. Why? Because we want to store data permanently. We don't want to temporarily store data. If the whole student table is on RAM, if you shut down the system, the table is lost, data is lost. So where to keep the data? We always save the data permanently in the hard disk. So data is here, CPU is here. 

When CPU will execute this query, will CPU interact with the hard disk? No, it will not, because CPU speed is in MIPS (million instructions per second), and hard disk speed is very slow (generally counted in milliseconds, while CPU speed works in nanoseconds or picoseconds), so their speeds are not compatible with each other. 

So the data that we have here, in this way: roll number 1, father's name, age, gender male, in this way we have a lot of data and records. And how generally do we keep the records? First, you see that in hard disk, whatever memory we're using, we divide the hard disk into blocks, logical blocks, not physical blocks. In your laptop also, C drive, D drive, E drive, F drive are made; these all are logical drives so you can manage data. But at the back end, the storage manager and the whole operating system divide the whole hard drive into fixed-size blocks, and then insert the data. Records in this way will get inserted.

Here you have to understand that data is here and CPU is here. CPU doesn't interact directly. CPU will tell RAM that I need data. RAM will call the data from secondary memory. From secondary memory, here blocks and you can say pages are also made. In operating system terms, instead of blocks, we call it pages: that we have divided secondary memory in pages. We are considering general architecture, so you can call it block or page. Block means a fixed-size box or boxes, and a collection of boxes is called a hard disk, and in those blocks, there are records.

If we see a general example: if I have 10,000 numbers of records. 10,000 numbers of records means in a student table, there is a record of 10,000 entries, data of 10,000 students. And if the size of one block I've given—one block can store 100 records—if it is given 10,000 records are there and every block has a size of 100 records (meaning one block can store 100 records), then how many blocks are required? Number of blocks required—how will you calculate? 10,000 divided by 100, so this is 100, meaning you need 100 blocks. Meaning you need 100 blocks to store all this data. And here we have CPU, RAM, and hard disk.

Now see how it works. If CPU finds student detail from student table, finds roll number 1, now data can be inserted in two ways: sorted or unsorted, it can be ordered or unordered. So how to call data if I think that data is non-ordered? Non-ordered means there is no order, like roll number 1, 10, 20, 100, 200, 300, 5, 6, in such a way data is there. So when we call this data, how it works? 

Firstly, one block—the whole block—comes to RAM, meaning this whole block will go to RAM. There will be searching; if you got the answer, then it is good, and if you don't, then it is a miss, meaning hit or miss. And we'll send that block back, and then call for another block and return, and again for a 3rd block and return, then 4th block and return. Like this way, blocks are called in RAM, searched; if you get that, it is a hit, and if you don't, then it is a miss and returned back. 

So here indexing is used. We're not reducing this searching time—meaning the time taken by one block, the time taken to search in a block, indexing doesn't reduce it. In actual indexing, the number of blocks you're calling—we call it input and output I/O cost. Obviously, I/O cost, you're transferring the data, transferring data from hard disk to RAM. Now, obviously there will be some cost of transfer. There we have propagation and transmission time; you're calling data, we call this I/O cost. So more number of blocks you call, I/O cost will be more. I/O cost will be more, so time will keep on increasing. 

So what we have to bring in actual? We have to bring such a method that we call a lesser number of blocks. This is the main motive. I'll give an example of a book. I have a DBMS book, let's say Navathe. If there are 1,000 pages in that book, and you've to search a topic, let's say you've to search indexing, how you'll do it if there is not an index there? You'll scroll a single page and search. So how many pages you've to search in worst case? You may have to search all 1,000 pages, and in best case, maybe you get data on the first page only. Meaning on average, you can say 500, because if I consider the average case, you've to check 500 pages. 

But if you use an index, if you use an index, your number of pages you are shuffling will get reduced because the index is only 5-6 pages. First, you see the topic in the index, from that topic you can go to that page, and from that page you can search data directly. So in this video, I only want to tell you this, and what indexing does in actual: indexing reduces the number of blocks we are calling. So I/O cost is reduced. I/O cost is reduced by indexing; this is the advantage of indexing. 

So how it works and what it is, that we'll see in further videos. Here I've only told you about the basic introduction of indexing. Thank you.