Hello friends! Welcome to Gate Smashers. In this video, I am going to explain the introduction to PL-SQL. For this video, many students were requesting; their comments were coming, mails were also coming, that I should definitely make an introductory video on this. Actually, this portion is not important from a GATE point of view, because in GATE, SQL is important. But in other competitive exams, like the KVS exam, DSSSB exam, or if you talk about any other competitive exam where SQL is the main portion, or if you talk about Oracle certification, or now even in 11th and 12th the SQL portion is there, so over there also basic PL-SQL comes. That's why in this video, I am going to explain the introduction to PL-SQL.

We will start. First of all, what is the meaning of PL-SQL over here? Procedural SQL. Procedural SQL means when we study normal SQL, Structured Query Language, now this Structured Query Language, by default, it is of which nature? It is declarative, i.e., declarative in nature. What is the meaning of declarative? That language where we explain, where we write what to do. We just write what to do, but how to do, that we don't mention. Why don't we mention that? Because we just have to find out—that I am given a question, I have to find out name, salary, or I have to find particular detail. So in a single query, maximum SQL queries that you have seen, they are usually completed in 1-2 lines.

But now we are adding a programming flavor to it. Programming flavor means as we do in C programming: we declare functions, we give function definitions, we make procedures, triggers are there, over here we have cursors. All these points that we are now putting in SQL, and after adding them in SQL, you can say that it is an advanced SQL portion, and it is named as PL-SQL, Procedural SQL. So now what is this? This is declarative plus procedural. 

Now what we have to do in this, and how to do—you are going to explain both. What is the meaning of "how to do"? Like you have to find the sum of 2 numbers. Now to find the sum of 2 numbers, you declared it, let's say a, b, gave them values. But now "how to do" means you will have to make a function, you will have to give a definition like c = a + b, and you did printf(c). Now you have printed c. So what will be the output? Addition of both. Means you are explaining both what to do and how to do. So that becomes your Procedural SQL.

In this, which topics come? In this, topics come: function, procedure, trigger, and cursor, from which the most important are procedure and trigger. Which is the most important portion because somewhere most questions come on them. There is not a lot of difference between function and procedure; its working is the same. There is just some difference in syntax. 

But if we talk about any PL-SQL block—we also call this a PL-SQL block or PL-SQL code—see, in SQL whenever you will get a question, it will come like this only: "Write a query." They will ask some question after this, but always how it will start? "Write a query to find this." "Write a query to find this." But over here, you will get the question: "Write a PL-SQL code" or "Write a program." Means now ahead on programming, somewhere now you will have to write programs for the sum of 2 numbers, fibonacci series. You have to make a pattern. Any type of question that we do in normal C, that we do in C programming, that question only now you can do in SQL, and that will be your PL-SQL.

And whatever code is there, whatever block is there of PL-SQL, we divide them basically into 3 portions:

First portion is declaration, where we declare—as we simply declare variables in C: `int a`, `int b`. In the same way, over here we declare, but the way of declaring over here is opposite: first the variable name, then the datatype (`a int`, `b int`, `char` is there, `varchar` is there, other than that we have `number`). Any datatype, any variable you want to declare—so first of the PL-SQL code, in that first you have to do declaration.

After that, we have executable code. Executable code means what you have to execute. Like if we do the sum of 2 numbers, so in the sum of 2 numbers I have declared first of all—we will declare and start, like `a int`, `b int`, this way I have taken 2 variables. We will take one more, let's say `c int`. We have declared. Now after this, executable code. Executable code always starts with `begin`. In that, as we give normal brackets in this way `{ }`, over here we write `begin` and `end`. We write executable code between `begin` and `end`. So we did `begin`, and in `begin` we have written: suppose we have given the value of `a` as 10, the value of `b` as 20, and `c` we have written as `a + b`: `c := a + b`. And we write this `:` to pass the value. If we write normal `=`, what is the normal `=` over here? It will work like assignment. But if we want to pass the value over here, if we want to pass the value, then over there we will have to give `:=`. We use normal `=` to compare 2 things, whether they are equal or not. So this is the way to write the basic code. Declaration portion is there, after that executable code is there, in which between `begin` and `end` whatever line I want to execute, that we write over here.

And 3rd is over here, exception handling. Exception handling means if you want to raise any error, if you want to raise any error manually, then that error—like divide by zero error—so then you can define these errors in this exception handling portion.

You will write any PL-SQL program, in that somewhere you have to write about these 3 portions only in the code. So this is the basic introduction of PL-SQL, and in the next, we will discuss about procedures in the next video. Thank you!