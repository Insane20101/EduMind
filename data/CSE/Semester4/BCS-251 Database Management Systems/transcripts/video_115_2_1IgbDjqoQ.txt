Dear student, welcome to Gate Smashers. In this video, I'm going to example PL-SQL programmes for beginners. So I'll tell you that on PL-SQL, the first video I've uploaded a long time ago on the PL-SQL basic block, what it is, and its basic structure. I've told you about each and every point. Link of that you'll get in the description box. But here I'm going to start with programs. I'll discuss two programs in this video. First, we'll discuss one, and with that, I'm also telling you its execution. I'll show it here, don't worry. Don't think that how I'll execute on board; that I'll not do on board. You'll see the execution on Oracle Live at the end of the video. Here I'll tell you some important points. Even in the description box, all the major programs of PL-SQL, the file of that also you'll get there. 

So guys, I'm giving this much, so in return, there is a demand to subscribe to the channel if you haven't done yet, and if you've subscribed also, then please do it from other devices also. So I can only request you, that's all. So let's start with the video. 

So here, the first program is to find the sum of two numbers. If we talk about the sum of two numbers, see, whatever the programming logic of that remains the same somehow, but the major difference is of syntax. Here, what we have, this is called the declarative section, meaning here we declare. Then, from begin till end, we have the executable section where we write executable commands. 

Here we have to do the sum of two numbers: `a` int. Remember this only, that is a normal programming `int a` and `int b`. So here `a` int, `b` int, and `c` int, so we have to write in such a way. Then, after `begin`, we wrote `a :=`. Here assignment, normal assignment that we do in C++, here we do `:=`. So `:=` and here I've written `&a`. Actually, it is like this: `a := 10`. `a := 10`. Here, if you want to give a direct value, that also you can do, and if you want to take a value from the user, you can take it from the user using `&`. So I'm telling you these small points because sometimes questions on these can be asked in your interview or viva, so it is important there. On competitive exams, there are not many questions on these, so their chances are less. 

So see, `&a`, `&b` we used to take user input. Then `c := a + b`. Sum of both is inserted in `c`, as we do normally. This is important: `dbms_output.put_line`. The meaning of this line in actual is `printf` like in C, `printf`, and in C++, we write `cout` just to print the value. There is no shortcut of this line; this is actually this big line. You've to remember it as-is. Rest, again, if there is sum of `a` and `b`, we write this in single quotes and here `||` symbol (straight line) with that we've written `c`, and these brackets are closed, and here I've written `;`. Here, always `;` will come. Apply `end`, as this is your `begin` and this is your `end`. Open parenthesis and close that is here. So this was your executable part, and this is your declarative part. I'll show you this by executing, then you'll know that how this shows output. So there it is, sum of two numbers. 

Then, greatest of two numbers. In greatest of two numbers, we'll talk about `if` and `else`, then you'll have an idea also of `if`/`else` as to how we use it and what is the syntax. 

So see, `Declare` and `a`, `b`, two variables I've taken here, data type of both we've taken `int`. `begin` here, and the executable part starts from here. So from `begin` to `end`, there is all the executable part. So see, `a := &a`. If you want to give a direct value that you can give: 10, 20, 30, anything. And if you want to take it from the user, then `&a` and `&b`. If `a > b`, as we write normally, then, here we write `then`. Again, there is `dbms_output.put_line('a is greater')`, `else` `b is greater`, so print that. Or if you want values to be printed, then the same that we did here with this, anything that you want to print, `a` or here you want to print `b`, so this way you can amend this. So here it is. 

At last, I'll tell you an important point, that it is very important to `end if`. This is `end` of `begin`, but here `end` of `if` is also very important, otherwise, it will show an error. So `end if` is very important here. 

So this is the basic program. Now we'll see its execution. 

Let's see the execution part. So see here, the same program to find the sum of two numbers I've written here. So we'll copy this and move to Oracle Live. So on Oracle Live, you only need to log in by adding your user name and name and all. You've logged in, a user mail will come to you, so you can use it online. Here I've pasted. After pasting, I will show you a little by zooming. 

Like this we've pasted, and this from here we run. So as soon as we run this, an error came. This error, I'll tell you, it is not that I did it earlier and then I'm showing you; I'm showing you this purposely so that you get to know about errors also. Because if you learn programming through errors, then you can learn more, because you get to know what type of error is coming and how to remove it. 

So here the error is, there is an error in line number 6 and that `&`. So I'll tell you, that I already told you why we use `&`, we use it to take input from the users, but in Live, this actually doesn't work. So in Live, we've to give direct values, like we've given `10` and here I've given `20`. I will tell you that it doesn't work in Live, but if you download it in your system, like Oracle 10 or 12, it will work there. So I've given `10` and `20` value here, so we'll run it. And as soon as I run this, so here it is showing output, that is, sum of `a` and `b` is `30`. So whatever changes you want to do in this, like sum of `a` and `b` `=`, so that you can see properly. So this is our program, and sum of `a` and `b` `= 30`. So like this, we execute our program. 

Next, if we talk about another program, so see, the other program also: Greatest of two numbers. In greatest of two numbers, we copied the code and there we've just pasted it. You don't have to copy and paste code; code we need to write, but I've written there, so I'm pasting from there only. Please don't say this that I myself am doing copy-paste. There I've written it myself. 

So again here, there will be the `&` error. So I'll change its value prior only, let's say we've given its value `10` and `20`. Now we'll simply run this. So see, `A` value is `10` and `B` is `20`. So it is showing that `b` is greater. `b` is greater, only if you want to print the value you can do that by adding simply `||` symbol in this also. So we'll run the program again, so see, `b` is greater and value of `b` is `20`. So you can print in any way that you want. Note these small points if asked in your viva or interview, so you can answer this. Thank you.