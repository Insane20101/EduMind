Hello friends, welcome to Gate Smashers. The topic is Schema in a database. Although this is a very simple topic, actually I am making a video on this topic because this term is actually used in a lot of areas. It means you will find this term in many questions in the database, and in many places you will find it written in questions. 

So what is a schema actually? The normal definition is, a schema is actually a logical representation of the database. It means when we actually store the data in hard drives or store it in backend servers, the physical representation of the data is something else there. It means we are storing the data in the form of files, but the logical representation—logical representation means, if we talk about RDBMS (Relational Database Management System)—in a relational database management system, how do we represent the data logically? In the form of tables, or we call it in the form of relations. In the ER model, it is in the form of entities and relations. 

For example, if I say to create a schema of a student: Student is an entity, and its schema has to be designed. So what are your attributes in student? It means how can we design the student, what can be in the student? Student's roll number, student's name, student's address, meaning there can be a lot of columns. We can also call them columns or addresses. 

Similarly, if I say course, course can be called an entity. Let's say course is a table, so how can we actually design it? If I say course is a schema, then how can we design the course? What are the columns possible in the course? Course ID (there are many courses running in the university, what is their ID?), course name, course duration. What do all these make together? A structure, a lot of structures. It means you can take any entity and design their structure, and this is actually the logical representation. 

It means the data on the hard disk is not actually stored anywhere in the form of such tables as student's roll number, name, and address. But the DBMS, when the user accesses the data, how do they access it? In the form of tables. And when the user manages the data, in which form? In the form of tables. 

We saw three schema architectures. The three schema architecture has a middle layer, the conceptual schema. So what is the part of the conceptual schema? That we represent the data logically, that is the main thing. What is a schema? Logically representing data. 

But when we are actually implementing it, we can also actually implement it with SQL (Structured Query Language). How to implement it? Like we have roll number, name, address, so roll number can be an integer, name can be varchar or character, similarly address can be a character—it means we can code it. And especially in SQL, Data Definition Language (DDL). In DDL commands, all the different commands that come in Data Definition Language, whether it is `create table`, `alter table`, or `drop table`—for whom do we actually use all these commands? To implement the schema. 

This is just a logical representation, it's a paper representation. You can make it on paper from a diagrammatic point of view. It means in a way, we are diagrammatically representing it. But when we are actually implementing the schema, then we use SQL. And in SQL, we use data definition language or data definition commands to design the schema, to actually implement it. 

So this is all about schema. You must have seen many times, let's suppose a schema, let's suppose a table. So schema, means you can say that it is a structure, you can take it in the form of a table. There can be multiple tables in a schema which are related to each other. It means a schema can be a single table or can have more than one table; that is just a collection of tables. And this is just the logical representation of the database. Thank you.