Dear student, welcome to Gate Smashers. In today's video, I'm going to explain Views in database. So whether you're studying SQL, Oracle, or any database system, Views is really a very important topic, especially if we talk from a placement point of view. Since placement season is ongoing and many students were also requesting this, I thought to complete this topic in SQL. In SQL, we have basically DDL and DML commands, DCL commands, and apart from this, if we talk about nested queries and joins, I've already explained those in a very good way. So those topics which are left, I'm going to finish them with execution. I'll cover the execution as well as the theory part on how you need to crack it in an interview, and if you want to ask something practical, I'll cover all aspects. 

So guys, do like this video, subscribe to the channel if you haven't done yet, and if you already have, you can subscribe from other devices also. Subscribers are very important. Let's start. 

So what is a view in database? In many interviews—and as I used to do jobs earlier, so I took interviews there also—whenever it is asked, "What is a view in database?", a virtual table, within a minute the answer used to come: that it is a virtual table. And if, guys, we talk about a virtual table, there is nothing wrong in this, but this thing is also known by a 9th-class student. So we should know about this in full detail. You should not think about how much I should know and to which level I have to do it; rather than this, you should think from an employer's point of view: those who came to recruit you, what is their expectation, and further, to which level it can go? That's why you have to cover all aspects. 

A virtual table is the first word that you'll say, obviously. Next, if asked why it is called a virtual table, the answer is simple: the table we create using `CREATE TABLE`, in that we define columns that are called attributes, and that takes physical space. As we've taken a physical space on Earth—we occupy a place—the same thing applies if you are storing anything in a hard disk, or in an SSD, or anywhere; you're storing data at the backend. So when you create a table, there is a physical existence of that table that acquires storage. 

But a view is a virtual table. It looks like a table, but it is not actually a table. So if it is not a table, but it looks like a table, how does that happen? In actual terms, a view is a stored query. We call a view the result set of a stored query. So what we do—it is made in a very good way by `CREATE VIEW`. Like I've written: `CREATE VIEW` and view name I've written `V1` `AS SELECT`, I've selected, let's say, I've selected `ID` from—you can take any table, let's say the student table—meaning you assume there are many columns in the student table like student ID, name, father's name, mother's name, gender, and many things. From that, I've only selected one `ID` column and made a view of that. 

And now what will happen? When it shows like a table, it will look completely like a table. As you can see in the diagram, this is our base table—meaning the original and physical table from which we are making the view. From that, I've taken a small table or window, added some columns, and taken some rows. Now, in this view, there is no physical existence, meaning we're not storing this table anywhere. What we are storing in actual is this query that is written. This query will get compiled. This query will compile—obviously, I have to check syntax and semantic errors. After compilation, it is executable code that is stored in a way. So after compiling and executing, whenever you write `SELECT * FROM V1`, meaning you're selecting from the `V1` view, it will show you data in the form of a table. In actual, it is storing this small query rather than the result; we don't store the result. That's why it is a virtual table, and that's why it doesn't acquire any space.

Then, read-only and updatable views: views we make in two ways—one is read-only and one is updatable. I mean to say it is simple. This is my base table. From the base table, I've made a small view. Now what will happen? If anything changes in the base table, will it change in the view? There is no need to ask this. If you're changing anything in the base table and you have taken that column in the view, then obviously—like in this question, if you changed anything in the first name, and this thing is in your view also, then that will also change. Like here, its name is Steven, and you made it Steven Smith; you changed it in the base table. So in the base table at the backside—which I'm showing to you in blue color—you changed the name from Steven to Steven Smith. Now what will happen? In the view also, its name will change from Steven to Steven Smith. 

Meaning, on the base table, if you are deleting—let's say you've deleted Steven's data—then it will delete from the view also. If you insert something, it will come here also. If you delete something, that will go away here, and if you update again, it will reflect here. In that, you don't need to ask anything, because you can take an example from parents and children: obviously, there will be a parent and a child. If you change anything in the parent, that will for sure show in the child. It means in the child there will be its effect. 

If you talk about the opposite—like vice versa—if you change anything in the view, you are updating anything in the view, or inserting something in the view, or deleting something in the view, does it affect the base table? This we actually make in two ways: if you want that you change something in the view table and that changes in the base table, then that is an updatable view. But if you want that I don't want to be able to change anything in the view—it stops that—then obviously, if you've updated anything in the view and if you successfully updated that, normally that would be on the base table for sure. So rather than this, what you do is that on the view you insert, delete, or update—these three DDL/DML commands—you shut them down, meaning disable those so that they cannot be applied. So for this, we make read-only views. 

All this story first I'm telling you is theory, because it will make your base; then I'll show you this with execution also, so that's why it is called read-only views. 

Then, materialized views: this point is also important to remember because nowadays, like if we talk about data warehouses or if we talk about data mining, whenever we do things a little more advanced, they ask about materialized views. A normal view, we know what it is. A materialized view is a type of updated version; in this, they take a snapshot. It is like I need to take a local copy of data in a client-server architecture—meaning my data is on a remote server, and a copy of that I need to keep in our local server or local machine, or by caching any data, meaning data is on a server, and the output of that, or its portion, or a snapshot, you want to keep on your local server. So that is called a materialized view. This takes space, but comparatively less space; but this in actual takes space, if we talk about a normal view. So the story that I've told to you—that's it, no one can ask you more than this, this is more than sufficient. 

Then, this diagram I've already explained to you—that this is a base table and this is your view. Now, when we make a view, after making a view always remember that we can't apply any DDL command on that, meaning we cannot do any alteration and all on it. Only DML commands can be used if we make updatable views. 

Then, in this, sometimes it is asked from you: Can we bring data of one or more tables? Yes, we can bring data of one or more tables into a view, and apart from this, multiple tables' data can be combined into a single view—meaning some columns from this table, and some from this, and some from another, you can pick like this also. And on this also, when we make a view then more it will be clear to you, but in actual, from tables it's not that you have to pick all rows; you can pick some particular rows also. For example, data of students—from that you only need to make a view of those students who belong to Delhi, so use a `WHERE` condition there: `WHERE address = 'Delhi'`. So from the whole table, that Delhi view will form. You can decrease the columns—you can't increase it, you can show only those which are there in the table, but you can reduce them. The same rows, by default as it is in the table, it will come in that, or if you use a `WHERE` condition on rows also and do it on it also, then that will also happen in view. 

Then, what are the advantages? Advantages I've already told you, but these points you should know technically:
1. To restrict data access: Meaning, if it is asked from a security point of view, it is asked from you that a view provides you security, and this is its simple reason—to restrict data access, meaning don't give access to the original table to the user. Like if we talk about a university and college, there is its website, and on that, there must be its management system. So what happens in this? When you create that view, you don't give access to the original table; you make a view on that. It can be the original table also—let's say 20 columns—and by taking out only 4–5 columns from it, you give it to the user so that they work on this. So the user will feel that in actual, in this table, only 4–5 columns are there, but in real, there are many. Like a faculty database: as we make a faculty database, it has faculty name, address, mobile number, faculty qualification, and many more details can be possible. So it can be that we don't want to give all information to students—why should I give them all information on faculty? I want faculty name and the course they are teaching, and maximum we can give their qualification. But yes, it depends upon college or university protocols: if they want to show more, they can; otherwise, only required data can be given to the user. They will give it to the user, so what will the user feel? That in actual, there are only these columns, but at the backend base table, there can be many columns. So it is in access-restricted; it doesn't mean that you can access anything. It restricts that access.
2. To make complex queries easy: I'll give a simple example. Let's say you have 3 tables and from those you have to select a few columns from all, so by default what will you do? Joins will be used, or a nested query. So better than this is that you can make a simple view—some columns from here and some from here—it reduces that complexity. A simple example: you've certain sales data in which there is 2019 sales, sales of 2020, and 2021 sales. Instead of you applying queries and differentiating 19, 20, and 21, simple is to make a view. In one view, there is only 20 sales, and in another, 21 sales. So like this, you can make complex queries easy using the view.
3. To provide data independence: It means if there is any user, instead of showing all things to him, you can give a particular access to it rather than giving the full database access. So in this way, we can implement data independence.
4. To present different views of the same data: Again, the same thing—that I have different users, like in a university and college there is a student, one faculty, and one faculty head which we call HOD, after that dean, registrar, or many more, and its privileges will also be different. The privilege that a student has and that a faculty has are different. Faculty can upload your marks or let's say can change them, but you can only see this. If you've the power to change, then it will be like: faculty has given 10 out of 30, and you change it to 30—this will be a big conflict! So that thing, which we want to give or show to them based on its role, based on its privilege, we'll show it that only; apart from this, we'll not show it.

So these are the advantages of views. So after this, in the next video, we'll see how to create a view, how to modify it, and we'll show all types by implementing. Thank you.