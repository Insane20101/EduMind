Hello friends! Welcome to Gate Smashers. The topic is serializability. If we talk about the transactions topic in a database, the most important topic in that is serializability, and specially for those who are doing GATE and UGC-NET preparation, or who are preparing for other competitive exams, those people specially shouldn't leave this topic at all because from here there are maximum chances of questions coming, and the topic is very simple.

Actually, what is the meaning of serializability? Serializable means what is a schedule's ability to become serializable? Meaning, I am given a schedule. What is a schedule? A collection of transactions. Meaning, I have a lot of transactions. After gathering them, a schedule is formed. Can I make that schedule serializable? Can I make that schedule serial? This is the topic.

So if I talk about a schedule in which there are two transactions, T1 and T2. There are two transactions in the schedule, T1 and T2. And if in T1 I write, let's say, read(A), write(A), read(A), write(A). If I write it in this way, then this schedule, just from the diagram you have to check, do you think this is a parallel or serial schedule? This is a serial schedule because in this, T1 will get over first, and after that, T2 will start, and it will finish.

Meaning, if we represent this in a graphical way, then how can we make it in the graph? We have to make its processing. Meaning, how many transactions are there? Two: T1 and T2. So I have made two vertices. First of all, which started? T1. And which finished? T1. After that, we went on which? When T1 got completed, then we moved to T2. So I can denote it in this way. So we have made a simple precedence graph of this serial schedule. 

And what is the meaning of this serial schedule? That T1 will be completed, then after that, T2 will be completed. We can write this in one more way. If I say that there are two transactions, T1 and T2, it can also happen that first T2 will finish, and after that, T1. Now see, what is the schedule by seeing? Serial or parallel? Again, this schedule by seeing, it is looking serial only, and in actual, this is a serial schedule only. So serial schedule means first T2 completed, then after that, T1. So if I represent this, T1 and T2, but T2 completed first, after that T1 got completed. So if you want, you can see it like this, or you see it like this. Which schedules are these both? Serial schedule. Because in this, the first transaction is executing and completing, and after that, the second transaction is starting. In the middle, there is no interleaving. 

So can these schedules be made serializable? What is the need to make them serializable? It is already a serial schedule. This is also serial, and this is also serial. So a serial schedule cannot be made serializable. It is already serial. 

What will always be given in the question? A parallel schedule. Meaning, you will be given in this way: that one T1 is there, and one T2 is there. There are two transactions in a schedule. Read(A), let's say, read(A), write(A), write(A). This T1 and T2: read(A), read(A), write(A), read(A). 

Now, what is this schedule by seeing? Is it serial or parallel? It is a parallel schedule. Reason: if we make its graph, so from where did it start? T1. We went from T1 on T2. So we started from T1, we went on T2, and we completed T2 a bit. Read(A), write(A). We again came on what? We came on T1. So over here, a loop is created. Whenever a parallel schedule will be given, then what will be created over there? A loop. 

Now, after seeing this question, after seeing this complete schedule, you have to find out: is this schedule serializable or not? Is it serializable or not? What is the meaning of finding if it is serializable or not? If I tell you in simple language, then this parallel schedule, we have to find a clone. Meaning, find one clone that should be serializable. It should be a serial schedule. So if we want to convert this into a serial schedule, then how will we do it? We have two ways to convert it into a serial schedule. Either this parallel schedule convert it to T1 goes to T2, or convert it to T2 goes to T1. Meaning, if this write(A) I write it over here, so this will be T1 goes to T2. Or if I write read(A), write(A) over here, or I can write read(A) and write(A) above and write read(A) below, then what will this become? T2 goes to T1. 

So this is a parallel schedule. To check a parallel schedule as serializable, what you have to check is that what is equivalent to this? Equivalent to this, does any serial schedule exist or not? So that concept we say serializability. And for serializability, there are two ways: one is conflict serializable, and another is view serializable. Conflict serializable and view serializable. You can use the conflict serializable method. With that, we will come to know that can we make a serializable schedule of this or not? And there is one view serializable method. We will use both of these methods, and in any parallel schedule, we will check: can it be serializable, or it cannot be made? 

So first point you always remember: whenever serializability will be given, any question will be given, you will never be given a serial schedule. Always you will be given a parallel schedule. And equivalent to that parallel schedule, you have to find out a serial schedule. So what can that serial schedule be? Like T1 goes to T2, or T2 goes to T1. 

If there are two transactions. If we talk that there are three transactions, let's say that we have a schedule S. In that, there are three transactions: T1, T2, and T3. T1, T2, and T3. Now, T1, T2, and T3, what will be given? Parallel. Let's say read(A), read(A), write(A), write(A). Let's say read(B), write(B), write(B). Anything. In this way, some parallel schedule will be given. This parallel schedule, which is looking parallel by seeing, this is looking parallel. Can we make it serializable or not? Meaning, what do I have to find equivalent to this? I have to find a serial schedule. So for that, we use both of these methods. 

So if we will make this serial, then what possibilities do we have? What is the meaning of making serial? That first T1, then T2, and then T3. Or first T1, then T3, then T2. Or T2, T3, T1. Or T2, T1, T3. Or T3, T1, T2. And last is T3, T2, and T1. 

So these are six possibilities. Out of these six possibilities, if I convert this schedule and bring it into one possibility, then we can say that yes, it is serializable. Why did these six values come? Because three values are there. Over here, we have three transactions, so you have to find 3 factorial. So what is 3 factorial? Six. So we have these six possibilities. Out of these six, if you bring something equivalent to any one, meaning by seeing this looks parallel, but if we want to make it serial, then from these six possibilities we will have to make any possibility as true. 

To do that, we have two methods: conflict and view. So over here, I just wanted to give you an overview of what serializability is. Simple, always remember that serializability means one parallel schedule is there, you have to find a clone that should be serial. That is the point. You have to find a clone of parallel that should be serial. How to do it? That we will check in the next video. Thank you.