Hello friends, welcome to Gate Smashers. The topic is set difference in relational algebra. 

Here, if we talk about set difference, set difference also does normal work as we have already read in set theory. What we have read in set theory: $a - b$. What is the meaning of $a - b$? $a$ but not $b$, meaning the values of $a$ should come, but the values of $b$ have been deducted from it. This means the values of $a$ are completely there, and some values of $b$ are there, and from those values, what we will do is minus. We can also write it as $a$ but not $b$. This we have written in terms of set theory. Here also the concept is the same. 

So, if I talk about a set $S_1$ where values are $1, 2, 3$, and there is another set $S_2$ and there are values let's say $3, 4$. So if I write $S_1 - S_2$, what answer will come here? $1, 2$. This means $S_1$ but not $S_2$. What does $S_1$ mean? $1, 2, 3$. And from $1, 2, 3$, what should I do? Minus the value of $S_2$. What values are there in $S_2$? $3$ and $4$, but $4$ is not here, so we will minus $3$. So what will be the answer? $1$ and $2$. So this is the same concept, and this we will follow here.

One more important point I will tell you here: it is not commutative, meaning $a - b$ and $b - a$ are not equal. You can check here we did $S_1 - S_2$, so what was the answer? $1, 2$. If you do $S_2 - S_1$, meaning $S_2$ but not $S_1$: first of all, you see $S_2$. What is there in $S_2$? From $3, 4$, minus the value of $S_1$. What is in $S_1$? $1, 2, 3$. Now, $1, 2$ is not there, $3$ is there, so minus $3$, meaning remove it. So what is the answer? $4$. So $1, 2$ is different, and $4$ is different. Therefore, $a - b$ is not equivalent to $b - a$.

Now we are talking in terms of relational algebra. First of all, we have seen these two points. In union, the number of columns must be same, meaning the two columns between which you are applying set difference, first, the number of columns should be same, and the domain of every column must be same, meaning the domain, the data inside them, what the data should be, it must be same. Then you can follow the set difference. 

Here I have taken two tables: student and employee. Now you are applying set difference between student and employee, meaning when we will write, how we will write it? `student minus employee`. I am applying this directly without projecting any column. We are applying difference between two tables: `student - employee`, meaning from the value of student, minus the value of employee. What are the values in student? `1a`, `2b`, `3c`. Now from it remove `7e`. `7e` is not there, so how to remove? Nothing. `1a` is there, so remove it from here. This means all the values of student are there, but from there remove the values of employee. So what answer remains? `2b`, `3c`, because `1a` will be neglected from here. 

Now why I have written roll number and name here? By default, you cannot execute this query here, but when we execute this in SQL, by default, the column from the first table is shown here. So now to write it in a proper way, if you see from here, a simple question if I ask: find the name of the student or find the name of the person who is a student but not an employee. A simple question now came to my mind: find the name of the person who is a student but not an employee, meaning he is a student, but he is not an employee. So for that, what do I have to do? 

He is a student, but he is not an employee, so from student values, we will minus employee. But how will we write it? From the student table, what is better? You do the name $\pi$. $\pi_{name}$ means from the student table, you fetch one column, which is the name column. From this, minus them. From the employee table also, you find $\pi_{name}$. From the employee table also, I fetch the column name. What did we apply between these two? Minus. When we apply minus between them, the output which will come in the final, which we can represent in the form of a table, so there the name of the column will be `name`. If I talk about `ABC` but not `EA`, meaning `ABC` but not `EA`, so `E` is not there, `A` we have to remove from there. So what is the answer? `BC`. What answer came here? `B` and `C`. This means these two persons, `b` and `c`, are students but not employees. 

In the same way, you can do other questions also, meaning one who is an employee but not a student. In this way, there are many live questions which can come in the exam, so those questions we can do here in this way. 

The main points you have to remember in set difference: these two points, and second, commutative. It is not commutative. Union is commutative, but it is not commutative. So this is all about the set difference. Thank you.