Guys, we talked about selectors in CSS and we said that selectors are used to select an element on which style is going to be applied. Now, what kind of elements can we apply style on and how can we select those elements? For example, an element with an ID, an element with a class, or we can target an element by its name. We can also decide that if there is a particular type of element in a particular type of element, then only the styling is applied. I will explain the rules of applying this styling and the selectors to you in this video.

If you are enjoying this course, then write 'sigma patch opi' in the comment below. We have to maintain high energy throughout the course. I want to give you a heads up that this video is very important. This is a very important video for CSS. So, increase your excitement. Let's go to the computer screen.

Alright guys, I have come to my computer screen. The first thing I am going to do is create a folder for Video 17. Video 17 is one of the most important videos in CSS because if you understand how selectors work, your job will be very easy. So, I will create a folder for Video 17 here. An `index.html` file will be made, and I am going to use internal CSS here; I will not make it in a stylesheet so that you can see what I am doing. I will also enable word wrap and screencast mode so that you can see whatever button I press. I will title it 'CSS Selectors' and make a `div` here that says, 'I am a div'. If you have not accessed it yet, do play the playlist for Sigma Web Development. The source code is getting updated, so you do not have to worry about it.

Now, what I am going to do is a simple thing: I will preview it first. I want to make the background of this `div` red. You might ask why I want to make its background red. It is because I want to know how much space this `div` is taking. That is why I want to make it red. So, I will simply give the background a red color. I will first put a `style` tag here and say that the `div` should have a `background-color` of red. This is our property and value. I will tell you more about property and value later, but for now, understand this is a property and a value. What I did here was use an element selector. So, I will write 'element selector' here and comment it. In CSS, comments are written like this: `/* comment */`. Whatever we write in between those is a comment in CSS. So, I have added a CSS comment indicating this is an element selector. You will see here that its background is red. This was our element selector, a very basic selector.

Now, what are the drawbacks of the element selector I have made here? The moment I add another `div` here, what will happen? The background of this `div` will also turn red, and it is possible that you do not want to make this `div`'s background red. If you do not want to make this `div`'s background red, then what will you do? Definitely, you will use some other type of selector, and you will want to use a specific selector.

I can say, 'Do one thing: only make the `div` with `class="red"` red. Only that `div` should be red.' So, I have set `class="red"`. And because I have set `class="red"`, you can see here that if I write `.red` and then set `background-color: red;` (let's say I comment out the element selector because I do not want all `div`s to be red), I will show you that only this `div` will be red. So, here I used a class selector. This is our class selector. I will write 'class selector'.

The next one is the ID selector. You might have guessed it as soon as you heard 'ID selector.' Let us say I want the `#red` ID, or let's do one thing, let's make an ID with `#green`. If I write `background-color: green;`, then wherever I give `id="green"`, it will be green. You can see here, 'I am another div,' it does not have any background color, but as soon as I give it `id="green"` (I will show you, I will write here for one minute, I will give it `id="green"`), you can see that for the `green` ID, I have given `background-color: green;`, so its background color will be green. This is our ID selector. It was very simple, right? Yes, it is simple; we make things simple here.

Now, I will tell you what a child selector is, and the source code will work for notes. So, what is a child selector? If I want a paragraph inside a `div` to be blue, I would say the `color` of a paragraph in a `div` should be blue, and the `background-color` should be... let's say `background-color: brown;`. If I put a paragraph in any `div` (for example, 'I am a para inside div'), then what will happen? This child selector will be applied because this `p` is a child of the `div`. I have specified that if any paragraph is a child of a `div`, its background will be brown and its text color will be blue. As simple as that.

Now, I will show you one thing. If I wrap this whole paragraph in something else—understand this carefully. Actually, I will put it in a `main` or any other element, not a `div`. Let's say I use an `article` tag here. So now this paragraph is not directly in a `div`; it is now in an `article`. This property will not be applied, I will show you.

Now, this property is not applied because this selector means that if it is directly in a `div`—a direct child—then this selector will be applied. In other words, if a `div` has a direct child, then this selector will be applied. Here, it is not a direct child; it is an `article` and then a `p`.

But if I want the paragraph to be a descendant of any `div`, then this property will be applied. Then I have another way: I will simply use a descendant selector. What is a descendant selector? We write it like this: `div` (space) `p`. I am saying that the `div` should contain a `p` element, no matter how deeply it is nested. I will write `div p`. I am saying the `p` should be in the `div`, whether it is in an `article`, `main`, or `footer`; it should be in the `div`; it should be a descendant of the `div`. So, you can see that it is applied. But when I talk about a child selector, it should be a direct child. I hope you are getting the sense.

Now there is a universal selector, meaning I want to give all elements `margin: 0;` and `padding: 0;`. I will write `* { margin: 0; padding: 0; }`. This means all elements have `margin: 0;` and `padding: 0;`. See, everything is stuck here. Earlier, margin and padding were present by default, but now margin and padding are gone, as you saw. I hope you understood.

There are many types of selectors, but these are the most used selectors. I will tell you about more selectors in the coming videos, but these are the ones with which we will do web development. I do not want to confuse you more, and I want you to focus on these main selectors. These are the most important. Through these, 95% or 99% of websites are made, so let's focus on this for now. I hope you guys understood this, and you understood it very well.

There is one more selector which is also important, and that is pseudo-selectors. Now, what is this pseudo-selector?

Why is there no comment? Okay, for the universal selector, what is this? Why is the comment not working? Okay, it is working now. Here, what I will do, I will write 'pseudo, pseudo.' Yes, I know it is called pseudo. Do not write in the comments that it is not 'Pseudo'; it is 'pseudo,' just like it is not 'pneumonia,' it is 'pneumonia.' I know 'P' is silent here. So, why did they put 'P' when it is silent? Tell me in the comment section. When it was silent, let it be like this. I know that 'P' is silent, so why did they put it? Tell me in the comment section, tell me in the comment section! But now I will share my knowledge with you, and that is pseudo-selectors.

Now, assume I have a link. I will write `https://www.google.com` and label it 'Go to Google.' It is very good; you will go to Google and see what is new in it. Let's say its color is yellow (not white). So, I want `a:visited` to have `color: yellow;`. If I make its color yellow, will it be yellow? Yes. So, for anchor tags, if I want to see a yellow color after visiting, I can use a pseudo-selector. Here, you can see the color changing. After this, if you use `a:link`, it means a link that has not been visited, and suppose you want its color to be green.

So, any link I put, what will be its color? Suppose I do it like this: 'Go to Facebook.' And yes, let's capitalize Google's 'G'. Will it be green? No, it will not be green, because I have already visited Google. So, I have to write 'Facebook.' I will write 'Facebook.' What will happen if I do 'Facebook 2'? Yes, I have visited Facebook. If you remember in the initial few lectures, I haven't visited it. I have visited it. So, that is why it is showing like this.

Now, I will tell you about two more link-related pseudo-selectors. One is `a:active`. If I want the `background-color` to be red, then when I click on a link, it shows red. `Active` means when you are clicking. So, here it is showing on google.com, and when I click, its background will turn red. I know that ideally, you will not want to use this. But that was just an example to show you how it works. It is easy for me to make the background red because you will observe it directly.

Now, apart from anchor tags, do we have other pseudo-selectors? The answer is yes, definitely. One is `a:hover`. If I want that when anyone hovers over an anchor tag, its background color should be yellow (instead of red). I make its background color yellow. So, if I hover, you can see here that its background color is yellow. As I am hovering, I have written `a:hover` and `background-color: yellow;`. These were some pseudo-selectors.

Now I will tell you one more pseudo-selector. That is, if I want to have a `div` whose class is 'selector,' and I will write 'I am selector 1' here so that you do not get confused. Now, I will tell you about the `first-child` pseudo-selector, which might be a little tricky for you. This category is 'pseudo-selector.' So, for this pseudo-selector, if I write `p:first-child { background-color: aqua; }`, it means that wherever a `p` is the first child of its parent, the background color will be aqua. Like in this case, you can see that I have put a `main` tag here, and it is written 'P I am first' and 'P I am second.' So, here you can see that wherever the first `p` element inside the `main` tag is the first child of its parent, this styling will be applied to it. See here, its background has become aqua, and the second `p` has not.

But I will tell you again that such things are very rarely used when you do web development. So, you guys should do more research about such pseudo-classes. What I will do is put links in the description about these types of things. So, if you want to see additional reading material and pseudo-classes, then you will get the link in the description. Maybe I will put it in the pinned comment too. And if I forget, then please remind me that I have not given the link. I will give all the links to you. From there, you can read about these types of selectors.

But now we will move ahead in this course, and we will read about the main things, because we have to come to projects very soon. We have to make real-world apps, and we have to do backend and database. So, we have to build pace. I hope you are enjoying this course. If you haven't bookmarked it yet, do it now: click here and save it. Access the playlist, the GitHub code, like the video, share it. And name your village and uncle's land. Thank you so much for watching this video, and I will see you next time. Thanks for watching!