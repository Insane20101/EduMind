In this video, we will see how to add CSS to an HTML page. There are three ways to add CSS to HTML. The first way is inline CSS, which is used as a `style` attribute and added directly to HTML elements. The second way is internal CSS. Internal CSS involves placing a `<style>` tag within the HTML page and putting the CSS inside that tag. The most used method is external CSS, which means creating a `.css` file and putting all your styles in that file. In this video, I will tell you very closely what the difference is between the three, when to use each, and when to avoid them. I am very excited. Please like this video, and in the comment section, don't forget to write "Sigma batch." Let's go to the computer screen. Let's roll the intro.

I told you there are three ways to add CSS to any HTML page. First, I will quickly make a folder named "video15." I will right-click on it and open it with VS Code. I will create an `index.html` file and add a boilerplate template. If you haven't accessed the playlist yet, please do; accessing the playlist is very important. Now I will come back to my `video15` HTML code. Let's say we have a basic HTML structure. I've written "I am a good boy" here, and after that, I've put an `<h1>` tag with "About me." I've put "I am a good boy" inside a `div`. Suppose I've also put a paragraph; everyone is good in some way or another. Does this even make any sense? I will click on "View" and "Toggle Word Wrap." I will go to the command palette and toggle screencast mode. Now listen to me carefully.

If I have to style this here, there is a very shortcut method, which I also call the "desi method." I have a very quick and dirty way: I will put a `style` attribute here. First, let's preview it and see how it looks. This is how it looks. I have zoomed in to show you, but you will see it like this. That's fine. I am zooming in to show you so that you can see. Now, for the "About me" text, suppose I want to make the background yellow and the font red for some reason. Let's do it. A quick and dirty way is to put a `style` attribute here. After this, I will write `color` here and set it to `yellow`. You see, VS Code is helping me completely; it is supporting me in writing. Whatever I am thinking, it is giving me an auto-complete. That's one of the reasons we are using VS Code. If you use Replit or any online IDE, you get this kind of support. This is why we don't do coding in Notepad. Many people initially asked me, "Why don't we do it in Notepad? Why do we use this? What does this have that Notepad doesn't?"

So, this is fine. Now, let's say I want to add one more property. First, I will show that this property has been applied. You see, its color has become yellow. Now, let's say I want to add another property: `background-color: red`. The background will also be red. Whatever I want to do with CSS for individual elements, I can do here. Now, this is not considered a good practice. It is not considered a good practice. Why it is not considered good, I will tell you in a while. But I will tell you two more ways of adding CSS to a webpage.

I will write the title of this as "3 Ways to Add CSS to HTML." Our first method, which I have written, I will put it in a `div` and use an ordered list. I will write another list item, and the first method is inline CSS. I have told you about inline CSS; it means that by selecting any element and using the `style` attribute, the CSS will be applied to that element. This is inline CSS. Now I will tell you the second method. Here, I will write: "Second method is internal CSS."

The second method says: "Don't do it like this." I will revert this and remove the `style` attribute. Instead, we will say: "Don't do this." I will replicate this and remove this `style` attribute. Instead, write a `<style>` tag. Listen carefully: put a `<style>` tag in the `<head>` section, and write your selector and declaration there. So, our selector is `h1`. I will say, "For all the `h1` elements on this page, make the `background-color` yellow and `color` red." I think I made the color yellow earlier, so I will make `color: yellow` and `background-color: red`. It will still look the same as before.

But how is this different from inline CSS? With inline CSS, I have to add the `style` attribute to every single element. Do you understand? Here, if I have 100 `h1` elements on my page, I can apply this style automatically. But with inline CSS, I would have to write `style="..."` again and again for each element. I don't want to do that repeatedly. Even if I did put `style="..."` in every element, suppose I have to change it. For some reason, my boss told me that on our production website, the background color should be maroon, not red. What will I do? Will I go and change "red" to "maroon" in every element one by one? No, I want to bulk edit. If I am using internal CSS and not inline CSS, then I can bulk edit. All I have to do is change "red" to "maroon" in one place, and all the `h1` elements on my page will become maroon. However, if I had written `style="..."` individually in every element, I would first have to find all those elements, and then I would have to change them individually. That's why inline CSS is not used as much. But you should know what inline CSS is, why it exists, and how you can use it. You will use internal CSS, or the most common one, external CSS. I will tell you about that now.

The idea of external CSS is that if you are writing internal CSS, your `<style>` tag will grow very large. If I am writing CSS here and there, your HTML file will keep growing. So, why not write that CSS in a separate file? This is the idea of external CSS: whatever you are writing here, don't write it here; write it in a file. So, we will use a `<link>` tag. The shortcut for `link:css` will appear like this. I am saying that I have written my CSS in `style.css`. I have done something like this.

Here, I will show you that I can create `style.css` like this. As soon as I create `style.css` here and put these styles in it, I will show you that the styles are removed from my page – there's no red background, nothing yellow. But as soon as I link `style.css` here, what does the `<link>` tag do? The `link` tag says, "As a stylesheet, link `style.css`." This CSS will be added to my page. It's as simple as that. I hope you all have understood this very clearly: the difference between inline CSS, internal CSS, and external CSS. Now I will tell you when you have to use which one.

You should not use inline CSS at all; try to avoid it. I am saying, try not to use inline CSS in ideal scenarios. However, if inline CSS is automatically generated, I will tell you about that later. For now, as a beginner, you should understand that you should avoid inline CSS.

You will use internal CSS when your webpage is small and you want to write CSS very lightly. I will tell you a very good use case for internal CSS: when I used to write websites with Bootstrap, all the styling on my website was done by Bootstrap. But sometimes I had to do small tweaks. That's when I used internal CSS.

You will use external CSS when you are making a big, production-grade website. In most cases, if you're working on a serious project and making a production-grade website, you will want to use best practices. Using an external stylesheet is considered a good practice. Why? Because you can detach a stylesheet from an HTML file, and you can also attach it. You can tell your colleagues to work on the stylesheet while you work on the HTML, or vice-versa. This way, you can work very efficiently. Such things become very easy if you use an external stylesheet. In most production-grade projects and big websites, whether it's Netflix, YouTube, Quora, Facebook, Instagram, or WhatsApp Web, you will find external stylesheets.

Let me give you a revision. Inline CSS means you have put the `style` attribute directly in the element itself and wrote CSS there. Internal CSS means you have put a `<style>` tag in the HTML and wrote CSS there. External CSS means you have made a `style.css` (or any other name `.css`) file and linked it. I hope you have understood the difference between the three.

I know I am going a little slower because many people in this course are beginners. I am telling you again that we are going to raise the bar. We are going to make very dangerous, production-grade websites in the coming videos. So, tell me in the comment section below if you guys are excited or not. If you have not accessed this playlist yet, please do access it. I will keep updating everything in the source code; don't worry about it. That's all for now in this video. Thank you so much for watching this video. I will see you next time!