Guys, we have seen almost everything in HTML. In today's video, we will conclude HTML and talk about some miscellaneous things which will help you a lot in your web development journey. After that, we will start CSS from the coming videos. I am very excited to conclude HTML. Like this video and write "Sigma patch op" in the comment section below. Let's go to the computer screen. Let's roll the intro.

As you can see, I have come to my computer screen, and without wasting time, I will create a new folder which I will name "video 13". In this, I will right-click with Shift and open VS Code. I will create a file `index.html`.

Here I have given the file and made an `index.html` file. I want to tell you one thing: there are many things in HTML that you have to explore by yourself. I will tell you some things, and after this video, I will continue HTML if you want. I will turn on word wrap and toggle screencast mode. Suppose you want to write '3 is greater than 4' in HTML. If you try to write this in HTML, let me show you the preview of this. This '3 is greater than 4' is written here. Now suppose I write here, 'this is the code for paragraph' and I write `p`. I have written this here. I want it to appear, but you see here, it will not appear. Why not? Because how will HTML know that I want to write a paragraph code, or I want to use this `p` tag as a syntax? To avoid this, there are HTML entities.

What are HTML entities? Let me tell you. If you write `&lt;` for less than, you can write it as `&lt;`. Now, if you will write `&lt;` here and replace it, and the greater than symbol, you will replace it with `&gt;`. I will show you this. If you replace it with `&gt;`, what will happen is that it will appear here as it is written. As you can see, I will zoom in and show you. HTML entities are used to write those things that may collide with the syntax of HTML. What does it mean to collide? If we write the code of the paragraph tag here, how will HTML know that this is the syntax of HTML, or you want to write this thing as it is?

For this, there is a `<pre>` tag. If you put anything in a `<pre>` tag and you write `p this is a para`, if you write it like this, then it will come here as it is. As you can see, it will come as it is. And along with that, if you write new lines here, 'this is after few new lines and some more', if you write it like this, then it will come as it is, keeping all the spaces intact, keeping all the new lines intact. But here it is not that the tag will show up. For tags, we have to use entities only. This happens in the `<pre>` tag. We have seen entities, we have seen the `<pre>` tag.

If you want to put a copyright symbol, then you can write `&copy;`. As you write `&copy;`, then you will get to see the copyright symbol, as you can see. You can do this kind of thing with the help of HTML entities. So you try out all these examples.

Non-breaking spaces. You can use special symbols. As we saw copyright, `&nbsp;` is used for space and is very important. If I write 'copyright Code with Harry', then this space will be considered a fake space because the extra spaces in HTML are ignored. So if you see here, immediately after 'copyright', 'Code with Harry' came. But ideally, we don't want this. We want the spaces that I have put to come. So you don't put spaces, you put `&nbsp;`. The more you put `&nbsp;`, the more spaces will come. You see here, these spaces are coming. So whenever you have to put space in your HTML code, `&nbsp;` is something you should go after. How to put these non-breaking spaces, and along with that, the reserved characters in HTML like `&lt;`, `&gt;`, you can do all this with HTML entities.

After this, what is the quotation tag? The quotation tag helps you to write quotations. I will tell you the example of the quotation tag here. The quotation tag works like this. As you write this quotation text here, search engines will know that you are actually writing a quotation. You can also style it with CSS. If you want to cite anything, you can put a quotation. If you want to cite anything, you can put its URL. Similarly, you can also use the `<q>` tag, `cite` attribute, and put the URL here. If you want to cite any research article, or there can be other reasons, if you want to cite Wikipedia, then this tag is used. You can obviously style it with the help of CSS. So these are our quotation tags and HTML entities.

Then there are some HTML tags which are obsolete. The `font` tag is obsolete because we handle font with the help of CSS. We don't use the `font` tag. The `font` tag had many attributes like `color`, `size`. You can use the `font` tag, but it's not recommended that you use it. Then the `center` tag. It was helpful in centering things, but now I will recommend that we will center with CSS, Flexbox, Grid, etc., which is modern CSS. We will learn that. All these tags are those about which you should know, but you should also know that you don't have to use them. I hope that helps.

Then the `code` tag, the most important tag for coders. As you are seeing this website here, `codewithharry.com`, I have used the `code` tag here. If you are very excited about this, then I will tell you that I am using a library named Prism.css. Many people ask me, 'How to do all this?' I will tell you, Prism.js. What happens with this is that your code is highlighted like this. You can put code in your blog, etc. I am using this library. I will tell you how to do everything. I am also using React in this. We are going to read all those things later, so don't worry at all. But if you want to write code like this, then you have to use the `code` tag. If you go to its source code by right-clicking, then you will see that any code written is in `<pre>` and `<code>` tags. Any code is written in the `code` tag. See, I have put `<pre>` and then `<code>` tag. If you will get any kind of code on my site, then it will be in the `code` tag. Here you can copy the code and then you can run it.

You can also write your own multi-line code in `<pre>`. Why is `<pre>` put? So that the new lines are intact. Suppose I want to write a code here. Suppose I want to write a Python code here, or suppose I want to write HTML code. I will press Ctrl+N and I will make it HTML. I will enter by marking an exclamation mark, and I will copy and paste, and I will write in `<h1>` that 'This is HTML boilerplate code'. If I preview this, then here 'This is HTML boilerplate code' is actually rendered. If I want to write code as it is, then what I have to do is, I have to replace this less than sign. I will right-click, 'Change all occurrences', and what I will do here, `&lt;` I told you, `&lt;`. And with that, this greater than symbol, change it to all occurrences by right-clicking, `&gt;`. Now I will paste this code there. I cannot do as it is paste, because this thing is also very important. Now see, it will come here. Here, see, this code came as it is. So anyone who wants to copy can do it. You note one thing, that because of the `<pre>` tag, these spaces are also coming. If you want these spaces to not come, then you select them, press Shift+Tab. What happens with Shift+Tab? One indent is reduced. You can do this, and after that, press Backspace here, and I will remove extra space from here. And you see, your code has come as you want. You can style it with CSS. Prism will use CSS. I will tell you how to use these libraries. A lot of things are going to happen in the coming videos. I just wanted to tell you about some tags of HTML about which you should know.

After this, character sets. Character sets means which characters are you using in your page. Then canvas. What is canvas? Basically, the use of canvas is mainly done through JavaScript, but canvas is an element in HTML that we make graphical shapes.

I hope you understood HTML in this video. In coming videos, we will do CSS. HTML is concluded. I hope you are enjoying. All the source code is being uploaded on GitHub, so do access it. Along with that, if you are not accessing the playlist, then click here to bookmark or click here to save. That's all for now. Thank you so much for watching!