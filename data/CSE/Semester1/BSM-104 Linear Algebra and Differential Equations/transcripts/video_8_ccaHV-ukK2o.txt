Hello students! I am Dr. Gajendra Purohit, and you're watching my YouTube channel where I upload videos on engineering mathematics and BSc mathematics. If you're preparing for any competitive exam where higher mathematics is asked, then my YouTube channel will be very helpful. I've uploaded a lot of videos on linear algebra, including subspace, vector space, basis, dimension, etc. So click on the i-tab to watch all these videos. If you're preparing for CSIR NET and want to strengthen your general aptitude, then click on the i-tab to watch my videos. My first video that I started my YouTube journey with was "Rank of Matrix."

Whenever the elements of the matrix are in a way related to each other, then the determinant of the matrix is 0. The rank of the matrix is equal to the number of linearly independent rows. The determinant is 0, which means that the row is dependent; when we check, we'll get the last line as 0, so the rank of the matrix is 2. In a 3*3 matrix, when the determinant is 0, the rank of the matrix will be less than 3. We'll make sub-matrices of a 2*2 matrix from this 3*3 matrix, which are minors. When the determinant of these 2*2 matrices is non-zero, then the rank of the matrices will be $r$. We have at least one of the minors as non-zero, so the rank of this will be 2; thus, the rank of the matrix will be 2. Every minor of the form $r+1$ will have the determinant as zero; that is why the determinant of a 3*3 matrix is 0. 

For the rank of a matrix, we convert the matrix into an upper triangular matrix, and the number of non-zero rows in the upper triangular matrix decides the rank of the matrix. For example, if the rank of the matrix is 2, which means that there exists at least one 2*2 minor whose determinant is not zero. For example, if the rank of the matrix is 3, which means that there exists at least one 3*3 minor whose determinant is not zero. For example, if the minor of a 2*2 matrix is non-zero, then the rank could be greater than or equal to 2. If we check the minors of 3 and all these are zero, so the rank will be less than 3. 

When we calculate the rank of a matrix, we use transformations which are easier. Using tricks, we'll find the rank of the matrix in just 2 transformations. Elementary transformations do not change the rank of the matrix. In a matrix A, changing the row and column will not change the rank of the matrix. The rank of the product of matrices A and B will not be more than the rank of the matrix A or B. 

Each 0 entry occurs below a non-zero entry; such a form is called the echelon form. For example, 1 zero, 2 zeros, and 3 zeros—we use the same trick to find the rank of the matrix. Usually, questions are asked on the normal form, such as "find the rank and convert it to the normal form." I'll explain it using an example and questions. 

To find the rank, we have two methods: using the determinant and the transformation method. If we find the determinant and if the determinant is 0, then the rank is less than 3; the determinant is 0, which means that the rank is less than 3. We pick a 2*2 minor; the determinant of this minor is non-zero, so the rank will be 2. Since the 2*2 minor is not vanishing, the rank of the matrix will be 2. But as per my recommendation, it is better to use transformations. 

The trick to apply transformations on a 3*3 matrix is: we'll make the first elements of R2 and R3 0 using the transformations in R1. In the second step, we'll make the middle element in R3 0 with the help of the middle element in R2. This form is called the echelon form. With this, we know the rank of the matrix is 2. After knowing the rank is 2, we use a trick—i.e., we write the unit matrix of 2*2, and the rest of the elements will be 0. This will be the normal form of this matrix; we need to transform the matrix from this to this, and as the rank is 2, so the unit matrix will be of 2*2 and the rest of the elements will be 0. Whenever we need to reduce it to normal form, we'll make these two elements 0 using the first element of R1. We'll make these 0 with the help of R1. Now we'll make this element 0. If the R3 would have [0, 0, 0], then the rank of the matrix will be 2, but since we have 15 in R3, the rank of the matrix will be 3. Further, we can apply column transformations to reduce it into the normal form. 

To find out the rank and nullity of the matrix, we'll see, like in this example, the rank will be the number of non-zero rows—i.e., 2. And the nullity will be 1, which is equal to the number of rows which are zero. For example, in this question, the nullity will be zero because we have no row which is completely zero, and rank is 3. There is a theorem that says rank of a matrix + nullity of a matrix = $n$ (order of matrix). We'll use the same concept in this question: the number of non-zero lines are 2, so the rank of the matrix is 2, and the number of zero lines are 2, so the nullity of the matrix is 2. We can also use the theorem, put the values, and find the nullity of the matrix. 

When we calculate the determinant of this matrix, this will be 0. We'll take the 3*3 minor, and if any of these minors is non-zero, then the rank of the matrix will be 3, and the determinant of this minor will be non-zero, so the rank of this matrix will be 3. We used the rank concept because using transformations can be a lengthy method with lots of calculations, so it is better to use the determinant concept, and in this pattern usually the determinant is 0. When there is 1 as the first element, it is helpful to calculate, and we know that applying transformations does not change the rank of the matrix, so we interchange the row. Now we apply the transformations. Now we know that the rank of the matrix is 3. 

First, we'll find the normal form and then its rank. As we did in the previous question with rank 3 and a 4*4 matrix, the normal form will be a unit matrix of 3*3 with the rest of the elements 0. Now we need to reach from this to this. We'll interchange either the row or the column; here I'll interchange the column. Now we know that the rank is 2, so the normal form will be this. Now we need to reach from this to this, so we'll apply the column transformation. 

I hope you must have understood the concepts well. If you like the video, then like the video, subscribe to the channel, and press the notification bell. These are videos on general aptitude as per CSIR NET. This is my series on real algebra. This is my new channel where I'm uploading videos on CSIR NET. This is my old channel which you can also check out. Rank of matrix was my first video, and this video is an updated video of the same.