Dear students, welcome to Gate Smashers. In today's video, I am going to explain Transport Mode and Tunnel Mode in IPsec. In the last video, I gave you a basic description of IPsec, telling you the main and important points. What I told you in that video is that we use IPsec in two ways: first, Transport Mode, and second, Tunnel Mode. So we'll see in this video what is different in transport mode, what actually happens in tunnel mode, and related to this, many questions have started coming in competitive exams, and even college and university exams also ask about this. I am going to explain this to you at a very basic level with examples. 

First, we'll discuss transport mode. What happens is, you know that we use IPsec on the Network Layer. The message coming from above through the application layer to finally the transport layer—when that message comes, whatever header it has, whether it is TCP or UDP, whatever header is added—we do not require it. Whatever the requirements were from above, we added the header, whether TCP or UDP, nothing different. When that packet from here reaches the Network Layer, meaning when this packet from here will reach the Network Layer, then what we will do in the Network Layer is add the header of IPsec and the trailer of IPsec. So here we added the header, and here we added the trailer of IPsec: Header, Trailer. And whose have we added? IPsec's. And why did we add it? We already knew it. We have to achieve confidentiality, meaning we have to do encryption; we have to achieve integrity, meaning we have to use hashing. Whatever our purpose was, what we did is that we took that packet and took it below, and on that we added the header and trailer of IPsec. 

Now what will happen? Will this packet go to the layer below? No. Till now we haven't added the header either of IPv4 or IPv6. In actual, we have to send those packets to the destination. So, to send it to the destination, we have to add the header of IPv4 or IPv6. So we encapsulated this whole thing, and before this, we added the header of IPv4 or IPv6, whichever is required. According to that, we added its header. Is it clear? Whenever you study online or wherever you study, this thing does not get clear: what is the actual difference between them? So now you know it: we took packets as they are and added IP before it. Meaning the packet came from the layer above; whatever TCP or UDP header was there, leave it. What do we do to it? We encapsulate it into the header of IPsec. We put our message into the envelope of IPsec. 

Now the message is made. The new message is made. What do I do to this again? I put it in the envelope of IPv4 or IPv6 because, actually, this will route when we add the header of IPv4 and IPv6. Till now we haven't added it. Finally, we have added the header of IPv4 or IPv6 to it. Now we have sent it to the layer below. This is called Transport Mode. 

Then, if we talk about Tunnel Mode, tunnel works in VPN when we use a virtual private network. So in that case, tunnel mode is used. IPsec is also used for VPN purposes, but how do we work in tunnel mode? There is a little difference. Here also, the same packet came from above from the transport layer. Whichever header is already added to it, whatever payload is, whatever it is, we took this up as it is. And what is the work of the network layer here? That the packet, the data which came from above—what do we add to it? We add the header of IPv4 or IPv6 to it, meaning it tells where it has to go, how it has to go. We have told all the things in this. Meaning before this whole packet, the IPv4 header is added. Now, has it become an IP datagram or packet of IP? We can call it an IP packet? Yes, it has become an IP packet because a header has been added before it. 

Now what have we done? We encapsulated this whole thing, meaning it contains the header also, and the data that came from above is as it is in this. In this, only data came from above. Here, data and the IPv4 header are also added to it. We encapsulated that whole thing, and what did we add before it? We added the IPsec header and IPsec trailer. 

Okay? Also, we got security. After getting security, when this packet will go below and will go on the next route, the next road, how will the router know what the destination address is? Because the IP header is hidden inside—whatever the original header was is hidden inside. Above it, there is IPsec. Here, what will we do again above IPsec? We will encapsulate it, and after encapsulating it, finally, we have added the header of IPv4 or IPv6 to it. 

Okay? Now you might be thinking that now it has two of them. Outside also we have IPv4, and we have it inside too. So, guys, that's why it is called Tunnel Mode, because this we use for end-to-end, meaning we use it for host-to-host. We use it for router to router if I want to give security, or for server to server, for gateway to gateway. 

You can understand like this: this is a source, in between this is a router, this is a second router, and here I have a destination. So whenever we use transport mode, what is the purpose of transport mode? From here it secured the packet. After securing, it will reach here directly. The router does not have anything to do in between. It only has to show the route—that you go from here and you go from here. That's it. 

But here what have we done? Here, the original packet including the header, we have hidden it. We have put an extra header above it, and that packet goes here. After that, the header which was outside tells it that you go to this router. It told it to go on that. 

What do I mean to say? You can say that it provides more security because it tells at every node that you have to go to this node, you have to go to this node. That's why we call it Tunnel Mode, because the original packet or original header, it is also hidden. It is also hidden. What was here? The original header is totally outside, and here the original header is still hidden. The header at the outermost is basically for here to there, from here to here. Meaning the one that will be outside—in this, first I will add R1, filled its address, and after that R1, I will put R2 address, after that R2, I will add D. The original which has to reach D—that we have hidden in there. 

So like this, the tunnel mode works. Although it is a little bit more complex, it is a little bit more complex to achieve, but obviously, you know the use case: where we use transport mode and where we use tunnel mode. Thank you!