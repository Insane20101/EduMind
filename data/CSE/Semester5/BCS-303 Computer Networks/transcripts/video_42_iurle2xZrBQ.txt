Hello friends, welcome to Gate Smashers. The topic is Class A in IP addressing. This topic comes under Classful Addressing. Classful Addressing means where we have divided IP addresses in 5 classes: Class A, B, C, D, and E. The concept of Classful Addressing came around the 1980s where all IP addresses were divided into 5 classes: Class A, B, C, D, and E. If we see before that, before 1980, the concept was that IP addresses are of 32 bits. For IP addresses of 32 bits, fixed 8 bits were used for network ID or for network numbers—at that time, before the 1980s, they used network numbers—and the remaining 24 bits represented host bits, host bits or host number, meaning how many hosts in which network was represented before the 1980s. So there they fixed 32 bits, with 8 bits for the network number. A simple example of this is the network number 10, which is also called ARPANET. In number 10 ARPANET, 10 is represented for the network ID and the remaining 24 bits are for the host. This means in one network, how many hosts are possible? 2 raised to the power of 24. 

At that time, what was the problem? At that time, the internet was not popular. Because the internet was not popular, this concept was okay at that time, but in the 1980s, the demand for IP addresses started increasing, meaning scalability started coming. For scalability here, if I talk about 8 bits, near about 2 raised to the power of 8 means 256 networks are possible, which is a very small number. And if I talk about the host, in 1 network, 2 raised to the power of 24 number of hosts are possible, which is in crores, meaning so many hosts are possible in one network. At that time, companies were not so big that they would need so many users or hosts, but now in today's age, there are many companies which need many hosts, and many companies are such that they need a lesser or limited number of hosts. So in the 1980s, they used the concept of Classful Addressing. In this video, we are going to discuss Class A, and the maximum content of Class A we are going to discuss in this video. That's why it is very important because it is Class A, and on classes you always get a question in GATE or UGC-NET. This topic is very simple; if you see this once or twice and try the questions, then you will feel this topic is very easy. I will try to deliver this topic to you in a very easy way. 

Here we talk about Class A. First, I have represented here that the IP address is of 32 bits because we are talking about IPv4, version 4. These 32-bit IP addresses here I have divided into 4 octets. Why have I done this? Because to represent IP numbers, we use Dotted Decimal Representation. There are various representations available like Dotted Decimal and Binary Representation, but this is widely used; this is the most widely used, that is Dotted Decimal. What we do here is we divide 32 bits into 8 bits, 8 bits, 8 bits, and 8 bits. 8 bits means 1 octet, because 1 octet means 8 bits. We have made 4 octets here, and in those octets, in each octet, there are 8 bits. 

Now, if I talk about Class A, then in Class A the first bit, the very first bit in the first octet, is always 0. That is fixed. This position has already been fixed; you cannot change this position, and you cannot change this bit. Here, a question arises: why is the 0 value fixed here? Because I am saying IP addresses are 32 bits, meaning how many IP addresses are possible? 2 raised to the power of 32. Out of these many IP addresses, how will I know that this is a Class A IP address? To represent an IP address of Class A, in any IP address, the first bit is 0; from that, we will know that this is a Class A IP address. That's why to represent each class there are unique bits. You can call this bits or prefix, because this works as a prefix. When an address comes to a router or destination in which there is 0 in the first bit, by default they will know that this is a Class A IP address. 

If you see carefully, one bit is fixed, that is 0. How many bits remain? 8 bits here, 8 here, 8 here, and how many remain here? 7. Total, how many bits remain? 31. This means in Class A, how many IP addresses are there? This is the first point: 2 raised to the power of 31. This is the number of IP addresses in Class A—that is 2 raised to the power of 31. 

Now we talk next. Like we discussed in the first video, an IP address is made of 2 things generally: Network ID and Host ID. The first bits, MSB bits, they represent—this way you can see—a 32-bit IP address in which some MSB bits represent the network, and the rest of the bits represent the host. This means in which network we are standing, when we connect to a network, what is the IP address of that network? To know that, here we talk about Class A, that in Class A the very first octet represents the network ID. This is very important: the first 8 bits represent the Network ID. The remaining 24 bits represent the Host ID. I mean to say that in one Class A address, the first 8 bits represent the Network ID, and the remaining bits represent how many hosts or users are in that network, which is represented by the rest of the 24 bits. 

Now, if I talk about the Network ID, then in the network ID, look carefully: in the first octet, this bit is fixed, meaning you cannot change this bit. How many bits remain? 7 bits. In these 7 bits, you can change the values. You can make them 0 or 1. If we see it, I have 7 bits. In 7 bits, how many permutations are possible, or how many variations are possible? 2 raised to the power of 7. What can be there? If we simply say the first address, then in all these places I made all 0s, or the first can be this, or after that, as we run binary bits, the same way we can normally check it. The first address is all 0s. What can be the last address? 1111111, meaning seven 1s (1, 2, 3, 4, 5, 6, 7). Because the first bit is 0 and is fixed, how many remain at last? 7. Minimum, how many are possible? 0. Maximum, how much can it be? All seven can be 1s. 

The second question arises here: what is the number of networks in Class A? How many bits are used to represent it? 7 bits. Although it is 8, this bit is fixed so we cannot change this—it is 7 bits. That means how many numbers? 2 raised to the power of 7. What is 2 raised to the power of 7? It is 128. Yes, this is the first point: 128 numbers of networks are possible in Class A. But here a second point I will tell you: the first address, that is all 0s, is not used by any of the organizations, meaning it is not given to any organization; it is with IANA, the Internet Assigned Numbers Authority. They don't give this address to any organization. There is one more address, that is the last address. The last address is 0 followed by seven 1s. What does this 0 followed by seven 1s become? 127, and all 0s become 0. If you represent this in decimal, what will be the value in decimal? 127, and this becomes 0. Neither is 0 used, nor do we use 127 to give as an IP address. This is not given to any network—both of them. How many total possible IP addresses are used? 128 - 2, that is 126. How many of these are used? 126 are used because the first one, with 0, and the last one with 127—127 is reserved for loopback and diagnostic functions. But other than that, as many networks can all be given to some organization so that it can create multiple hosts in itself. 

Now we know that the number of networks possible is 126. In each network, how many hosts are possible? Let's say if I fill a value here, let's say here we take 1000000. This overall value is 64. This is 64, and 64 means that our network ID is 64. Now, these remaining 24 bits—in these you can fill either 0000... then 000... and 1 at last. How many combinations are possible, how many variations are possible? 2 raised to the power of 24. That means in each address, in each network of Class A, how many hosts are possible? 2 raised to the power of 24. Out of these 2 raised to the power of 24, 2 addresses are also not used. Why are these addresses not used? You will know this too. Now we are talking about the number of hosts possible, that is 2 raised to the power of 24 minus 2. I will tell you why this minus 2 has been done. 

If we see here, 64: is 64 a Class A address or not? How to know that first? To know that, we know that the range of Class A is 0 to 127. Any address, if it is given between 0 to 127, then you can simply say that this belongs to Class A, because to find out the network I don't need to find out the host. Only from the network bits can I find out which network it belongs to. 

Now we'll take a simple example. Let's say there is a network of—we'll take an example of Google. If Google wants Class A, obviously Google is also having one of the Class A IP addresses. They have a lot of IP addresses, but let's say they have a Class A IP address and the name of that address is 64.0.0.0. Here, the first point you have to keep in mind is that when we see any IP address, first you have to see the first octet in it. From the first octet, from the first 8 bits, you will come to know which class it belongs to. Now you know that the first octet value is 64. Does 64 come in the range of 0 to 127? It does. So without seeing this, you can say that this belongs to Class A. 

Now in Class A, what does this mean? What does 64.0.0.0 mean? The whole world knows you by which IP address? This IP address. This IP address represents the whole network, because outside this network is the whole www, meaning it is the entire worldwide web. The entire worldwide web knows you by which IP address? 64.0.0.0. 

Now what will be in this IP address? In this IP address, there will be many hosts. Many users will be there, many hosts will be available. How many hosts are possible? We just discussed this: 2 raised to the power of 24 hosts are possible (2 raised to the power of 24). If 2 raised to the power of 24 hosts are possible, then in this we can—whoever is working in Google, when he gets an IP address, if he connects to the network, he will get which IP address? 64.something, because 64.0.0.0 is representing Google. If you are asked that in this network what will be the last IP address, the very last one? That will be 64. What is this octet? This 0 represents eight 0s. If you put all 1s in place of this, means put eight 1s, that is the largest value. Eight continuous 1s are 255. In this 0 also, talking about this octet, meaning 1, 2, 3, 4, 5, 6, 7, 8. In this octet also, 1, 2, 3, 4, 5, 6, 7, 8. In all three octets, you put 1, then the last address becomes 64.255.255.255. The first address becomes this, and the last address becomes this. If we say in this IP address how many total hosts were there, it was 2 raised to the power of 24. 

If you see carefully, 2 addresses out of them—the first one, which is the first one? This one. This is the first IP address of this class. This first IP address you cannot give to any host. Why can't you give it? Because this IP address represents the whole network, so no host will get this IP address. Second is this one: 64.255.255.255. This will also not be given to any individual host because it is used as a Broadcast Address. Broadcast Address means if in your company, in Google, if someone from outside wants to send a packet—and broadcast means what? In this company, everyone should receive that packet, in this company, in Google here. How will everyone receive that packet? If in the destination address I write some IP address here, let's say 64.0.0.1, then it will go to 1 host; that message will not go to all hosts. But if from outside to entire Google, to the whole network, I want to send a packet, I want to send a message, then what destination address will I write? This one: 64.255... We call this address Broadcast Address, or we also call it Directed Broadcast Address. Through this address, all the hosts will get this message. 

Now, if I ask you a simple question: let's say 64.0.0.1. What is 64.0.0.1? Normally we see it just like that, but if you see, what does 64.0.0.1 mean? The first host in this network, first host—that address will be this, because 64.0.0.0 is this network's ID, but in that network what will be the first host? 64.0.0.1. 

If you are given any IP address, let's take a random IP address: 64.0.0.8. If you are asked, what is the network of this IP address? Meaning this IP address comes in which network? How do we know it? Through a Default Mask. Default Mask helps to find out the network ID of IP addresses. The Default Mask of Class A, always remember, is 255.0.0.0. This is the Default Mask of Class A. 

Now how will this help? How will it help me to find out which network 64.0.0.8 belongs to? What do you do? You can use it as is, or normally open it. You will have to do AND with this and this. You will have to perform an AND operation between the Default Mask and this. To perform that, you simply represent this in binary. 64 is 0 1, 3, 4... 0 you can write as eight 0s, but there is no need to write. If you want to expand it, then you can write eight 0s. This one also, if you want to expand it, you can write 0s. I will expand 8 as 00001000. This is 8. Now the Default Mask also: what is 255? 11111111. Remaining is 0.0.0 (00000000). 

Now what to do? AND. When you do AND, the AND of 1 and 1 will always be 1. The AND of 1 and 1 will always come to 1. Remaining values will become 00... This will be 0, and this will also be 0. Here, if you see carefully, normal AND operation: the AND of 1 and 0 is 0. If you again convert this into decimal, this will become 64.0.0.0, so you will come to know that this IP address is some host's IP address. This is which host? Eighth host in this network. This host belongs to which network? It belongs to this network. How do we know this? By performing its AND operation with the Default Mask. This you will have to expand, and you will have to remember how we represent 64 in binary. The trick to this I will tell you when we solve questions. 

This is all about Class A in IP addressing. If I give you a general overview of this, in Class A you are asked sometimes: first question, if any IP address is given to you, then you will be asked whether it belongs to Class A or not. Second question you can be asked: how many numbers of networks in Class A? That is 128. But out of those, how many are used practically? 126, because 2 are not used: the 000... meaning 0.0.0.0 and 127.0.0.0. The 0s written here internally represent 8 bits because we take octets. Neither 0.0.0.0 null address is used, nor is this used because this is used for loopback and this is a null address.

126 IP addresses, 126 networks are possible in Class A. And in every network, how many hosts are possible? 2 raised to the power of 24. But practically, how many can we use? 2 raised to the power of 24 minus 2. 

Why do we use 2 raised to the power of 24 minus 2? Because the first address in any IP address represents what? The network. And the last host IP address, that is the .255 one, this we use for what? Broadcast. So neither can this be used for a host, nor can this be used for a host. That is why we subtract 2 from this. 

Always remember the value of the Default Mask, that is fixed for Class A. Thank you, this is all about Class A.