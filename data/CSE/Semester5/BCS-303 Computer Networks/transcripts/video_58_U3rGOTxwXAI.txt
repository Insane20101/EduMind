Hello friends. Welcome to Gate Smashers. In today's video, we are going to discuss IPv6 headers. Earlier we had discussed about IPv4 headers. In this video, we will discuss all the important points about IPv6 header, which will help you a lot in competitive exams or in college and university level exams. 

The first point that we will discuss is what is the use of IPv6 when we already have IPv4. IPv4 came in 1970. It became very famous. Even till this date we are using IPv4. Then what is the need of IPv6? Why is IPv6 becoming popular? The biggest reason behind that is the IP address in IPv4 was 32 bits in size. Meaning, the number of IP addresses possible is 2 raised to the power 32, which is a lot of IP addresses, but up to a certain time. 

Today the scenario is of IoTs, meaning Internet of Things, where we want that our IP addresses should not only be limited to mobiles—that we should connect through Wi-Fi or connect wirelessly, and then we get an IP address and get connected to the internet. We want not only mobiles and laptops, but the home appliances which we use at home—like we use an AC, we use a fridge, we use a microwave—we want that by connecting through IoTs, we can give them an IP address and control them from anywhere. You must definitely have seen ads about this. ACs are already available in the market. 

So what happened because of this? The number of IP addresses required suddenly increased a lot. The IPv4 had a limited number of IP addresses because many were reserved, and that fell short. That's why we use IPv6, in which we have a 128-bit address. 2 raised to the power 128 IP addresses are possible, which is 2 raised to the power 96 more than the previous. This value is really a huge value. 

Now let's start. In IPv6 header—I have told you in IPv4's header what a header is and where it is used. If you see the header here, it is really small compared to an IPv4's header. Why? Because by refining and normalizing, they have scaled down the size so that we can use irrelevant things in option. Meaning, in IPv4 we had options, a padding field, and some extra fields. But they have excluded those fields here, although you can use them. But how? This is what we'll discuss here. 

So the first value here is the version. Version was of 4 bits there, and here too it is of 4 bits, and its value is zero one one zero. Meaning it is six: IPv6. So for version six, its value is always zero double one zero. It means for any packet, if the first four bits are zero double one zero, then it will find out that this is an IPv6 packet. 

Next is the priority field. It is of 8 bits. We can also call it priority, or we can call it traffic class. I had told this when we last discussed IPv4's header in detail. I had also mentioned it in that. Please check that video too, because before understanding IPv6 you should know about IPv4 so that you can compare both. I have already given the link in the description box. 

The meaning of traffic class priority is congestion control. If some packet is getting stuck in congestion, then I can set the priority there. The sender can set a priority that if a packet is coming in congestion, then according to some priority, we can forward them, and the one with a lower priority can also be dropped. It has differentiated services and ECN. The two values that we had discussed are present here. So the main motive here is deciding the priority. 

Then we have the flow label. Flow label is of 16 bits. The actual use of flow label is real-time data processing. Why? Because today's time we are dealing with real data. The meaning of real data is data which does not have any delay. Meaning, your data should immediately reach, and there should be minimal loss in data—meaning complete data should reach. 

But what is IPv6? IPv6 is a datagram service because the service is at the network layer. Similar to IPv4, it is also datagram. It is connectionless. Your packets will go from different paths and combine after reaching the destination. So obviously, one might go well on one path, and another path is closed, then those packets will not be able to reach. 

So here the meaning of flow label is that we convert datagram service into a virtual circuit, use it like a virtual circuit. The meaning of virtual circuit is that when there is a source and a destination, the source and destination are connected through different routes. It does not matter. In a virtual circuit, what we do is that we decide beforehand that all of our packets follow one path, rather than going on different paths, it will follow only one path. 

So if on the way, a packet comes close to the router, and if the packets have the bit of the flow label, then it only moves, whatever the direction is, in that direction. So that rather than going from different directions, the packet moves in one direction. So we use reservation here because if your resources are reserved, the probability of losses is minimized. It works like TCP. We use flow label for real-time data. 

Then payload. Payload length, as we saw total length there. Here too we have payload length, so 16 bits. It is the same: sixty-five thousand five hundred thirty-five. But the normal packet that goes is this one, maximum sixty-five thousand five hundred thirty-five size. But the extra option here is of jumbograms, meaning you can also send jumbo packets, maximum up to 4 GB. But for that we use extension headers, like it is written here, Hop-by-Hop option. This extension is used, and through that we can send jumbo packets. Meaning up to 4 GB of data could be sent by using IPv6, which was not available in IPv4. 

Then next header. This part here is important because the next header contains all the extension headers. Its main concept will be cleared from here. Base header: in IPv6, the base header is of 40 bytes. A fixed header of 40 bytes, like I have written here. 40 bytes means 320 bits fixed. If you count these total bits, it will add up to 320 bits. But the 40 bytes here is fixed anyways. 

Like each packet has a base header attached to it, it is a mandatory field. But apart from that, we can add extension headers. Extension headers mean if you want extra functionality. Where is your data? Here. You can send data and the base header. But if you want extension values, if you want to use some extra functionality—like using fragmentation, using static routing. Then in source routing, you have to use extension headers. So the value of the extension headers is mentioned here. So whatever value you put here, what it means is that after this there is one additional header, which we call an optional header. Your data will come after that header. 

Then hop limit. Hop limit is similar to Time to Live in IPv4. Time to Live means how many hops can one packet go. Whichever particular intermediate node it will reach, its value will decrement one by one. In case the packet gets stuck in an infinite loop, it will not create congestion. When its value becomes zero, we will drop that packet. We will feel that the packet is creating congestion and it is stuck in an infinite loop. 

Then we have source address and destination address: 128, 128. Actually, this particular row is of 32 bits. If you add eight plus four, twelve. And here we have total 16. In place of 16 here, actually the flow label is of 20 bits. Twenty plus eight, twenty-eight, plus four, thirty-two. So here we have 32 bits. And see this: eight plus eight, sixteen, sixteen plus sixteen, thirty-two. So this one field is of thirty-two. The source address we have is of 128. That's why we have made lines here, because you can consider this 3D. One, two, three, four layers are there. So thirty-two into four is one twenty-eight. It is just for representation. You just have to remember normally that the source address and destination are of 128 bits. 

Next we have the optional header. The extension headers that we are using here—meaning the first one is the routing header. The meaning of the routing header is that when you want to decide from your end which packet should go to which router to approach which router, you can put the information of the routers in packets. It will go only to those routers. We can call this one way: the sender is deciding himself that my packet will go through this path. Like, if you are sending a courier, the courier will reach the desired destination, but you are even deciding through which particular cities the courier will travel to reach its destination. This concept is used in the routing header. 

What is this 43? If we mention this value here, writing 43 in binary, then it will understand that after the base header, next comes the routing header. 

Then hop-by-hop. Hop-by-hop means the values used in this. Each particular hop—meaning all the people coming in the way, all the hops that will come. You want to provide information to those hops. To achieve that, we use the hop-by-hop option. 

Next we have fragmentation. Fragment is the same thing, like we do fragmentation in IPv4: dividing the packet into many small packets so that if the underlying network does not support it, then by dividing into these small packets we forward it ahead. But here is a little change. In IPv6, intermediate nodes—meaning if there is a router on the way—cannot fragment. The one who will do fragmentation is the source; it will send after doing it itself. So if you do fragmentation, it has the same fragmentation offset, flags, etc. But we have put this in an option. Here if you mention the value 44, it means the packet is fragmented. So do remember, after the base header is the next header. It is also there that you can put multiple, but it has a full criteria. You cannot mix anything. You have to put them one by one, and finally at last you have to put data. 

Then authentication header. Why do we use authentication? For authentication, when users want to send passwords, to keep the integrity of data, for hash values. Like we have the checksum there, here we use the authentication header. 

Then destination options. We want that some particular value should only be read by the destination. No one should open it on the way. In the destination header, if the value is 60—if you mention 60 here—then the intermediate nodes can only open the base header. In the extension header, no one can open the value of the destination header. Only the destination node will check it. 

Then encapsulating security payload. This feature is new. Why is it used here? Because in today's time, security is the main issue. So if in your environment, if in your network, you want to bring in security, what do we do for that? We use encryption and decryption. What values are you using to encrypt? How are you encrypting? You can mention all those values in the header. The value is 50. 

And all these—there are many extension headers, but the ones popular currently are these. So here we get many questions on this. If you are talking about college and university-level exams, then these are asked in two-mark questions, or full five- or ten-mark questions can come. And if you want to talk about competitive exams, especially the difference between these: What is the difference among the headers of IPv4 and IPv6? Which value has what particular use? 

So this is all about IPv6 header. So guys, if you liked this video, then please like this video, share it as much as possible, and please subscribe to my channel. Thank you.