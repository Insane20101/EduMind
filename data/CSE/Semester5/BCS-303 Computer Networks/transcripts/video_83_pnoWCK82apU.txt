Hello friends. Welcome to computer network class. In today's class I am going to explain about application layer protocols. Usually application layer has many protocols. But if we talk about, specially from competitive exams point of view, one of the most important is DNS, HTTP protocol. Other than that there is FTP protocol, SMTP, POP protocol. These are majorly important from competitive exams point of view. Because you can even write a complete book on application layer protocols, but these are the majorly asked protocols. There are small points about them that you should remember in your notes, keep in your mind, that you should know if something is asked on these points. 

And you don't have to go in much detail, because if you go in detail of HTTP, FTP, then they have their separate book about them. Each protocol has a complete book on itself, but they are not asked in competitive exam. In any competitive exam, they have asked about ports, that what is the port number of this protocol, what application uses this port, which transport layer does this protocol use. Mainly these 3-4 points are important, but I will tell you some extra points apart from this, which will give you some extra help.

First, if we talk about HTTP protocol, the port number used is 80. You must have heard HTTP, Hypertext Transfer Protocol, which is widely used to transfer and fetch the webpages on www. Meaning as you open the browser, after opening the browser, what are you using? You are accessing webpages. If you are accessing webpages from web servers, either accessed through Google or through Facebook, whichever, you must have seen there at the starting you will find usage of HTTP protocol. So HTTP protocol, how to request webpages, how to upload data, post on server—HTTP protocol is used for all these works. Port number is 80. 

If we talk about, if someone asks that whether HTTP is a reliable protocol, HTTP protocol is not reliable on itself. Meaning if we see its header, if we talk about its functionalities, so it is not reliable on itself. Meaning it wants that data should not be lost; in case some data is lost, it wants that data should be retransmitted. But in it, it doesn't have any functionality inside it. So whose functionality does it use? It uses TCP in transport layer to achieve reliability. Remember these small points. 

Then it is called inband protocol. Inband protocol means what do we do first? We send command. We don't access webpages directly. First command is sent, meaning first you are trying to connect, trying to sync, listen the connection, all the commands. Command and data both emerge from same port. Remember it is the inband protocol. Inband protocol means there is just one port number. From that port commands will travel, from that port data will travel, which is not there in the FTP, File Transfer Protocol. We will talk about it after this. 

Then, whether it is a stateful or stateless. So stateful and stateless means: stateful means whoever is accessing through HTTP, HTTP will store its information. Means you can say information, that which user did, what is accessed, for how long it is accessed. Storing meta data like this. So HTTP doesn't do this work. It is a stateless protocol. It doesn't save information. What is the reason for that? There are million, billion requests, and all the requests are going through HTTP. So if it keeps saving all the request, all the information, big delay will occur. 

But there are many companies, if we talk about, like Amazon, Flipkart. If we talk about these companies, these companies use the concept of cookies. Concept of cookies—what they do on client side, they keep cookies, in which all the information, like when you turn on Amazon, if you checked a product on Amazon and you closed after that, then what will happen? That information will be saved on cookies. Even your username, password, your address, what did you access earlier, the webpage you went on, each and every information is what they saved in cookies. So the same thing is shown to you the next time. The user does not have to fill the same information again and again. You get help from this somewhere. You check this on daily basis. You already know about this information. So by default HTTP is stateless, but many companies try to make it stateful on their end.

Then if we talk about, it is used in two ways. It has 2 variations, which is non-persistent and persistent. Non-persistent means connection. My connection is non-persistent. And you can say another thing. Suppose you open a webpage. Let's say you opened IRCTC. In IRCTC you saw reservation page. You filled some information on reservation. Then you checked the payment option. You checked information about trains. So whenever you open any amount of connections in IRCTC, like first you went to reservation, then you went to catering, then checked the trains, then for each new connection, for each new webpage, same amount of connections are created at backend. Meaning the number of webpages that you open internally, same number of links get opened. And as you closed IRCTC from top, as it closes the connection is immediately lost. Connection lost means when you open IRCTC for the next time, fill the username again, and fill the password again. 

Similarly what happens, like in HDFC, ICICI, in online banking too, when you turn on the webpages and fill the information, if you stop the transaction in between, if you open again, then you have to fill the information from the start again. This is the non-persistent. 

If we talk about persistent, a simple example of persistent is Gmail. Let's say you filled username and password and closed it. Now if you open after one month or one year, mails are opened directly. You don't have to fill username, password again, till you have logged out from another product or system. Meaning its connection is persistent. It will be maintained after closing. I am not talking about logout, when you closed the connection from top, that connection is persistent and will continue. But in non-persistent it will terminate.

Next we will talk about various commands. Small commands like HEAD, GET, POST, PUT, DELETE, CONNECT. If we talk about HEAD, HEAD is basically metadata. If you are accessing a webpage or data, that webpage has data, but it also has metadata. Meaning the information related to data. Means when a webpage is being accessed, basically all the information related to it comes under HEAD. If we talk about GET, complete webpage has to be accessed, which is a widely used command. POST if you want to post some information, if you are filling a form, you are giving some information to webpage. PUT works in the same manner. You have to upload some information. DELETE means you are deleting some information. CONNECT means you used HTTPS. HTTPS is working on Secured Socket Layer. If you want to make a connection with server, that is basically made through CONNECT. 

And the difference between HTTP and HTTPS is it works on Secured Socket Layer, in which username and password is checked in each request. Because if there is chance of hacking, if someone is trying to hack the traffic, trying to hijack the session, so that chances are minimised. So what does it do? It checks username and password authentication again and again. HTTPS, if in today's time if you see, maximum website that you open, even if you enter http google dot com, but it goes to HTTPS by default, just for security purpose. 

So here it is discussing the same thing: client requested server for the webpage, and it gave a particular webpage, and all this work happens with the protocol, through HTTP protocol. It made a request to other server, so the other server also responded. This is the same information about GET command. What HEAD command basically does, where we are not doing to a document, information of the document, we are accessing the metadata of document. POST, PUT, TRACE, echoes meaning the coming request, meaning the request that is coming, to check that. TRACE, CONNECT, OPTIONS—inquire about the available option. You should know this small information.

Next we will talk about FTP protocol. FTP protocol and HTTP protocol, if we see, this is file transfer. Meaning if you want to transfer on the remote side, then you use the FTP protocol. Its real life application is like FileZilla, Techtia. What these multiple applications do—let's say you are sitting in Chandigarh, and you have to transfer a file online, or let's say you have to host a website, then while hosting a website, you have to send the file on the server. When you will send the file on server, then what happens? You use File Transfer Protocol. 

What File Transfer Protocol does is it makes two parts in your machine: one is your machine, the other it will show is the server's machine. Now what to do? You first use port number 21, which is used to send control commands, meaning to establish a connection, sending the data. And after that port number 20 is used for sending data. You will connect the data through the control commands, form a connection first, after that you can transfer the data. So by simply dragging, dragging from your system files will get sent to its system. 

File Transfer Protocol is not inband basically. We don't call it inband. HTTP is inband, it is not. Why it is not? Because it uses two port numbers: with one number control commands, with second number data. So control and data can be used parallely in FTP. 

Then we talk about control connection is persistent. Control connection is persistent, meaning if you stop along the way, even if you delay in between, you didn't send data for some time, didn't send information for some time, so control connection will remain established. But data connection is non-persistent. Data connection will be lost. You have to again establish data connection. So data connection is non-persistent for security purpose. 

It is non-inband, that I have told you. Reliable? Yes, FTP. It needs reliability. Why reliability? Because files are being transferred. Even if a single byte is lost, file may not even get open. File will always open when all the bytes are received by you. It also uses TCP protocol to achieve the reliability. 

And it is stateful. Stateful means if two users are transferring files between them, you transferred file on remote login, so which file, when transferred, what was the size—each and every information is saved. Information is saved on its end, because in future, through this information it could be found out that what happened in the past. It is like history. Information is saved like history in stateful. It is reliable, non-inband. Apart from that FTP has mainly all these points: data connection is non-persistent, control connection is persistent, non-inband, reliable, and stateful.

Next we have SMTP and POP. First, Simple Mail Transfer Protocol and Post Office Protocol. Simple Mail Transfer Protocol is simple mailing. Today Gmail, Yahoo Mail, Hotmail, we use many mail servers. These all are web based mails. But at the back who? At the backend, how are data mails transferred? Through SMTP protocol. 

If asked between SMTP and FTP, FTP is synchronous. Synchronous means if two people are transferring files between them, both should be active. But it is not in the case of SMTP. What happens in case of mail? It is not like that the person you are sending the mail to should also be live. No. You simply send the mail. Whenever the person is live, when he is connected to net, he will read the mail. So this asynchronous is a very good feature in SMTP. 

It uses port number 25. It is basically used for pushing the mail. SMTP is used to push the mail, and POP is used to pop, to withdraw the mail. How does it work? I will give a little overview of its working. First we talk about what is a mail account. You say that I have an account, I have a Gmail account, I have a Yahoo account. Basically what is an account? You are provided some space to store data. Like Google is saying that I provide 15GB, 25GB space. That is your space. That space is called account. Now you use it properly, save the data, anything. Similarly there can be some other mail, another mail could have provided space to a person. 

The first entity that comes is mail client. What this mail client does is it fetches mail from the user. To fetch mail from user, a mail client or mail agent provides help. And from this mail client the information goes to MTA, that is Mail Transfer Agent. And from Mail Transfer Agent it goes to another mail server, meaning it went to another Mail Transfer Agent, and from there who picked it up? Mail client picked it up, and it is shown to user. 

So the work of SMTP basically is—SMTP basically, the mail that has come, we use Outlook, different-different browsers, from there we sent a mail, that mail went to client, mail client pushed that to Mail Transfer Agent, from there it got pushed to other Mail Transfer Agent. Let's say you are using Google, you are using gmail.com, and the other person is using yahoo.com. So basically there is @gmail.com and @yahoo.com. Right. Here @gmail.com has its own Mail Transfer Agent. Similarly yahoo.com has its Mail Transfer Agent. 

Now you are a normal user. User can use different-different. Like I opened of Gmail, there a web-based page is opened. From there you composed a mail. That mail is sent to mail client. From there it got pushed to Mail Transfer Agent. From there it got pushed to other Mail Transfer Agent. From there we do POP, Post Office Protocol. So what POP does, it picks up the mail from Mail Transfer Agent and gives it to mail client. Mail client will show to user. So as you enter your username password, you will see the mail. 

At last note a small point: POP3 protocol is used. It also uses two ports. Port number 110, this is the default POP3 port number, which is non-encrypted. And 995 is the port number which you need to connect using the POP securely. If you want to use securely with encryption, then this one. Without encryption by default, the one that is used is POP3, which is 110 port number. 

And at last, in today's time, we send images, videos, we send a lot of multimedia data. So by default MIME protocol is used in this, Multipurpose Internet Mail Extensions protocol. So this is more than enough. A separate video is provided for DNS. Remember these points for all of these, which is more than sufficient. You can go in more detail for your knowledge, but from exam point of view, if it is NET exam, GATE exam, any exam about computer science, no more than this is asked. Thank you.