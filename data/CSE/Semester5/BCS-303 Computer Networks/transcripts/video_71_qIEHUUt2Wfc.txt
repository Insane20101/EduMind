Hello friends, welcome to Gate Smashers. The topic is TCP connection establishment. Like we saw in the video, whenever we send data through TCP, first TCP establishes the connection. We generally call this connection establishment the three-way handshaking method. We will discuss the 3-way handshaking method properly in this video, how TCP establishes connection, and what values it sends between the sender and receiver. 

Firstly, we are talking about TCP. In TCP, what is the connection establishment? If A is establishing a connection with B, A is sending its information to B and it's telling B to reserve some things, means to fix some things like buffer, CPU, and bandwidth. Reserve them because after that A will send a message to B, and B will also send some information to A. It will send information to make the connection, and will tell A to reserve buffer, CPU, and bandwidth so that the connection establishes properly. It is just like a telephone network. TCP always works like a telephone network. When we talk to someone, there is first a dial-up. Dial-up is what? It is a connection establishment phase such that once the connection is established, after that my packets—meaning the data—are properly sent by two-way communication. That is also called reliability. Reliability means there shouldn't be a loss of my packets, there shouldn't be data loss. In case there is data loss, then re-transmission should be there. So that's why wherever we need reliability, there we will use TCP only. That's why TCP is a majorly used protocol because whenever we need reliability, whenever we need the 100% guarantee for the packet to reach, then definitely we have to use TCP. 

Now we are talking about TCP connection. Here I have a client machine and a server machine. Generally, we use TCP at the time of client-server communication, where a client machine—like my laptop—can work as a client machine, and if I am accessing a service or web server like Google, Google is working like a server and my machine is working like a client. Meaning, from my machine to some other machine, data is being transmitted. 

And the second important point that we discussed is that TCP works in full duplex. Full duplex means the client is sending data to the server, and the server can also send data to the client. That is called a full duplex mode. Meaning, if A and B are communicating with each other, then the communication will be in two ways. 

Now, here I have two machines: client and server. So what is the first point? Let's say, from my system, I have opened a web browser. I opened the browser. After opening the browser, the operating system will assign a port number to me. But these port numbers aren't always from the well-known port numbers; some ports are registered with IANA. The port number that I'll get, that port number is nearly about the 50,000 to 65,000 range, and that port number is assigned by the operating system. 

Now I am transmitting a message from my port number. That is the first message to establish the connection—meaning it's not transmitting the data, this is not the data. We will transmit the data afterwards. The first packet or segment we're sending, this segment is for connection establishment. So here my machine is in an active open phase. Active open means I'm ready, I'm ready to make the connection. First, the client should be ready, because the servers are running 24×7. Google won't ever tell you to make a connection with it; I have to make the connection first. So my machine is active, and a packet will be sent from my machine. 

The diagram which you can see here, this diagram is actually a header. I discussed about the TCP header in the last video—what are the values, what are the uses—you can check that video again, it will become properly clear by that. Otherwise, you can pay attention here properly, I'll tell you what the values are and what their particular uses are. 

So first of all, I've written here source port number. The source port number of my machine is A, and the destination port number is 80. Because whenever we access web pages, web pages are always accessed on port number 80. So we can write the port number as 80. 

The important point here is the sequence number. Meaning, as the client sends the message, it will always use a sequence number. This sequence number is used by it; it's a number, it's always a randomly generated number, it's never from 0. If all machines start from 0, then all machines will use sequence number 0. So that's why we use a random sequence number here, the value of which can be from 0 to $2^{32} - 1$, which is a very huge number. So here the sender/client sent 9,000. I generated a simple number randomly and sent it. 

Along with that, the sync flag: I made the sync flag value 1. Sync flag means I want to synchronize with the server—meaning I want to make a connection with the server. So I made the sync flag 1. This is a flag bit, the value of which is 1 bit. Its value can be either 0 or 1. So I'm sending it after activating. 

Along with that, window size. Window size means 12,000. 12,000 is what? It is in bytes. Meaning the client is sending the size of its window, that the size of my window is 12,000 bytes. This is a very important point here: why do we use window size? For flow control. The sender/client sent the size of its window—meaning the size of its buffer—that my buffer can accept a maximum of 12,000 bytes. That is the client's receiving end. Meaning, the client told its receiving capacity by itself; it advertised it itself: "Don't send me data more than this." 

Other than that, options and padding. The last option we have is options and padding. In that, it told MSS. MSS means Maximum Segment Size. The maximum segment size is 1,200 bytes. Now, what is a maximum segment size? Whenever we use a connection, then at the data link layer—meaning, whenever my connection will go down from TCP to the network layer, and then to the data link layer—at the data link layer we generally use Ethernet. And the maximum segments in Ethernet—meaning, the size of one packet—should be less than 1,500 bytes. 

If you mentioned your window size as 12,000 bytes, it is simple. I have said that I can accept data of a maximum of 12,000 bytes. Then it may be possible that the server sends only one segment of 12,000 bytes, but my network doesn't support only one packet of 12,000 bytes. It can support small packets together which make 12,000, but it is not necessary that my only one packet is of 12,000 bytes. That means my maximum capacity is 12,000 bytes, but the size of one packet is 1,200, so send me 10 packets of 1,200 so that my window will be full. In this way, the client is telling the server indirectly that it should be 1,200 bytes. If one packet is 1,200 bytes, and if you send 10 packets of that type, then my window will be full. My window means my buffer will be full. 

Now this message and full information is sent by the client to the server. The server is in passive open mode. Meaning, the server is running 24×7, it's open, but it is in passive mode because the client has just sent a request. The server will store all this information at its own end, and then it will generate a packet. It will also generate an acknowledgement. That's why we call this—when the first information is sent, we call it sync, because we're trying to just synchronize with the server. But the server is sending us sync + acknowledgement, because it's full duplex. The server will also send its information to the client: "This is my capacity, this is the size of my MSS," plus acknowledgement: "Your packet has been received, I am sending you the acknowledgement also and my information along with that." 

Now the server, which was in passive mode, will come into active mode. But before coming into active mode, it sent its information: "Okay, my port number is 80, and this is your port number, and the sequence number is 3,000." Meaning, now the server which is sending data is using sequence number 3,000, so that the client gets to know that my data will now start coming from 3,000. Meaning, all of these are bytes, because it is a byte streaming protocol. Every byte here is counted as a number. 

So the server also told its sequence number. It is also a randomly generated sequence number. And the acknowledgement—we've already discussed acknowledgement. It is always $x + 1$. Meaning, if the client has sent 9,000, then the receiver/server will send $9,000 + 1$, that is 9,001, which is the next expected byte. Because this number 9,000 has been used, it won't be used again for the data, because we have used 1 byte just to synchronize. Now the server is sending that "I have received your 9,000, now start sending me data from 9,001." 

Along with that, it made the acknowledgement flag 1, meaning now my packet is also doing acknowledgement. This is the acknowledgement: "I'm acknowledging that I have received your 9,000, now start sending me data from 9,001." Along with that, sync—sync means the server also wants to receive some data, the server also wants to get some data and share some data. It's working in full duplex mode, so the server also made its sync flag 1. 

Along with that, it also advertised its window size. Meaning, its buffer can accept a maximum of 18,000 bytes. But MSS—MSS is again important, because the network at its end, that network/buffer accepts a maximum of 18,000 bytes, but in that network the size of one packet should be—it mentioned it as 600 bytes. So that means, simply calculate, $18,000 \div 600$, which is 30. Meaning, the server is telling the client that "You can send me up to a maximum of 18,000 bytes, not more than that, otherwise there won't be flow control." For flow control, the server said that 18,000 bytes should be the maximum, but don't send one packet of 18,000 bytes; you can send a maximum of 30 packets of 600 bytes, then my window will be full. It did its advertisement and also told its maximum segment size, i.e., 600 bytes. 

This packet will be received by the client. So it is a two-way process, but this is a three-way communication. Now the client will again send data, and the sequence number used here is 9,001. 9,001 means—I would like to tell you a point here, you can find this in many books or in some online material you can find 9,000 here, because the sequence number we're writing here, we already wasted a sequence number here—meaning it is not a kind of waste, but we have used one sequence number just to make the connection, we used that sequence number. So you can also write 9,000 here, but I am not writing 9,000 here because the server is expecting 9,001. Then why should I write 9,000? When it was expecting 9,001, then definitely I will write 9,001. 

But the important point here is, this 9,001 sequence number isn't wasted. When you will start transmitting data after that, this number can be reused; you can use this sequence number again for data transfer. But you can't use the previous one, it's already utilized. So that's why I have written 9,001 here, because when the server is expecting 9,001, then why should I write 9,000? 

So it sent 9,001. Along with that, there's the acknowledgement. The server sent its sequence number 3,000, sorry. So the client is sending 3,001 here. Meaning, your 3,000 sequence number has been accepted. Now, start transmitting data from 3,001, that is the next expected byte. Along with that, ACK—it made the acknowledgement flag 1 because it is again an acknowledgement. It is an acknowledgement of what is received from the server. 

So if you see this in generic form: 
First, a synchronize packet is sent. 
Then after, synchronize + acknowledgement, both things—synchronization is already there plus acknowledgement is also there. 
And in the 3rd, we only send acknowledgement, that the data which was sent by the server is properly acknowledged/saved. Accept acknowledgement from my side, now the data can be transferred further. 

After that, the data will start transferring. So this is the three-way handshaking method. This is all about the three-way handshaking connection establishment by TCP. So if you liked the video, please share it with your friends, and please subscribe to my channel. Thank you.