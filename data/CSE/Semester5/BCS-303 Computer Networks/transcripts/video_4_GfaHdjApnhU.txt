Dear student, welcome to Gate Smashers. In this video, I am going to explain the TCP/IP protocol suite, which we also call the internet protocol suite. In this video, we will also discuss TCP/IP versus the OSI model. All the points are useful in your competitive exams, even in your college university exams, and in interviews. Anywhere you are asked, you can easily answer. 

Now, if we talk about TCP/IP versus OSI, I have already explained to you very well about OSI. There is a seven-layer architecture: physical layer, data link layer, network, transport, session, presentation, and application. But if we talk about the TCP/IP protocol suite, then we have both 4-layer and 5-layer architectures. First, let's see what the difference is between the 4-layer and 5-layer architectures. 

In the 4-layer model, we have four layers: the physical layer and data link layer are combined to form the network access layer, the network layer is called the internet layer, the transport layer is called the transport layer or host-to-host layer, and these three layers (session, presentation, and application) are combined and called the application layer. What are the 5 layers in actual? The physical layer and data link layer are separate, and the others are the same. 

Now, why is there this difference? The major reason for the difference is that OSI was actually a pure theoretical model. Both of these are fundamental models. "Fundamental" means that if we talk about the internet, about how communication works in the internet, then we always talk about the TCP/IP model. But this OSI model is a fundamental model in every communication; you can say if you want to read the network very well, then you should have good knowledge about this. However, the OSI model is full theory, whereas TCP/IP is a complete implementable model. 

Who developed it? ARPANET developed it, and it was funded by DARPA, which was the defense agency of America. They funded it, and ARPANET actually implemented it for use. So, this is actually an implementable model, and there is no documentation type of work. That is, the OSI model was full theory and TCP/IP is full practical. When we work in a practical approach, we don't work on theory or documentation, therefore they didn't work on this. 

So, in many books and in many materials, you will get 5 layers, and in many, you will get 4 layers. In actual, the 4-layer model is more important, and we should have good knowledge about it. But this depends on your college or university: if they follow books by Forouzan, they use this one; if they have Tanenbaum, they use the other one. So this depends on what book you are reading. 

However, the story of both models is the same. If we talk about the application layer, obviously, what is the major work of the application layer? Process-to-process delivery. Process-to-process delivery was here also, and it is here also. Along with this, the work of the presentation layer—let's say if I am using different encoding systems, then to synchronize them, encryption, decryption, all that work—and to create sessions and maintain them, all that work now comes under this application layer. Other than this, to generate data, whose work is it? Where is the user? Here. Meaning, he is using some application, either SMTP if I want to send mail, FTP if I want to transfer files, or Telnet for remote login. All applications and all protocols are the same as they were in OSI. 

Then, if we talk about the transport layer, what is its use? Host-to-host delivery. The TCP model is the major one because what is its name? TCP/IP protocol, meaning Transmission Control Protocol we are using, and Internet Protocol (IP) network protocol we are using here, in which there is IPv4 and IPv6 also. So that came in your internet layer. In the transport layer, you have majorly TCP, UDP, and SCTP. So you want to make it connectionless or connection-oriented, that you know, and that is your host-to-host layer. 

Then, in source-to-destination, where we talk about IP addressing, adding the IP address in the header of the source and destination, that is taken care of by the internet layer. Then finally, we have the physical or data link layer; their responsibility is that within your own network, within one node to the second node, how data is to be sent. That work is done by the network access layer. 

So here is the diagram of the same protocol stack architecture. The user is here, in this way it will go, and one by one the header will be added and go to the destination from here. Meaning, this is a stack architecture, so the same Last In, First Out (LIFO) case is there as it is in OSI. Now, you can say the user generated some data, he went to the application layer, from the application layer to transport, then from transport went to the internet layer, and from there he went to the network access layer. 

So in the network access layer, let's say I have a node or a router. On the router, which two layers will be there? One is the network access layer, and the other one is the internet layer. In this way, the packet will move and go to the next one. And what will happen in the next? The same network access layer and internet layer, so again the packet will move, and when the receiver comes, then again it will move through these four layers. So the architecture is the same. In OSI, we talked about the physical layer, data link layer, and network layer, so here within these two main layers, the whole story moves. 

These main protocols like IPv4, IPv6 came under this; IGMP, ICMP all came under this. If we talk about MAC protocols, error control, flow control within the local network, that comes under the network layer. And if we talk of the transport layer, I have already told you about TCP, UDP, and all that. The data terms we use here are: data at application layer, segment at transport, packet at internet layer, and frame at network access layer, which we were using as it is in OSI. 

So the main difference you have already figured out: whenever we talk about an implementable model in the internet, which model do we use? The TCP/IP model. You know the OSI model has been developed by ISO, and this TCP/IP model was developed by ARPANET. If we talk about other than this, it supports client-server architecture and peer-to-peer architecture. What is meant by client-server? A centralized authority is there, the server, and all clients are attached to it, and they send their request and response. The meaning of peer-to-peer is that there is no centralized authority; all are mutually connected, and data is transferred in a distributed way. Both are supported. 

So this is the main story of TCP/IP. Out of this, no one will ask you; this is enough. Thank you.