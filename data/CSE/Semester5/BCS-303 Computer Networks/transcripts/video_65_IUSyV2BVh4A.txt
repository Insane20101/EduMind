Dear students, welcome to Gate Smashers. In this video I am going to explain ARP, i.e., Address Resolution Protocol. In this video we will discuss all the important points related to ARP that will be beneficial for your competitive exams and even your college and university exams. 

ARP stands for Address Resolution Protocol. We have to do resolution of the address; what is meant by this is that we are converting a logical address into a physical address here. ARP is the protocol of level 3 or layer 3. If you talk about OSI or TCP/IP, either you call it network layer or internet layer, it is its protocol. This converts an IP address into a MAC address. I will clear all the scenarios to you with an example.

Let us say this is network 1. Let us say I have a switch through which different devices are connected. Let us focus on network 1 for now. Let us say in network 1, A wants to communicate with C. A wants to communicate with C means A wants to send a message to C. A can have the IP address of C. How will it have the IP address? Because it is public. You will get a public IP address; even if it doesn't get one, it can actually take it from the default gateway. Actually, there is the address of the default gateway. That means A is connected to the default gateway, the switch, so it will have its IP address for sure. 

So what do we do actually? When A wants to communicate with C, the IP address is there, but is communication possible without a MAC address? Because you need a MAC address in a LAN. Now what is a MAC address? It is a physical address. What is that? The 48-bit address on the NIC card which is not public. How will A get the address? What will A say to C? Let us say A has to send some message. A will first find C's MAC address. A will first, let us say, enter the IP address of the source. It entered its IP address A. And let us say it entered its MAC address also, that my MAC address is A, it's of 48 bits (and you know it is of 32 bits in IPv4). And along with that, in destination—what is the destination? C. It entered the IP address of C, but it doesn't know the MAC address of C. So in this case, the MAC address of C isn't known, so by default here, this message will be sent to all. 

So it actually does all f—all f means the actual broadcast message of 48 bits. So the request of ARP, it is always the broadcast message; the request is always a broadcast. That's why you can say that if you write ff.ff like this 12 times of 48 bits, then it will be broadcasted. So this message will be sent to the switch; it will send that to everyone. 

When it will send that to everyone, when C will receive this packet, it will get to know that there is its IP address, meaning it wants to communicate with me. So it will reply then. When it will reply, then the reply is always unicasted. It is just like in a class you go and ask, "Who is Varun?" So you asked all, meaning your message is given to all the 100 students sitting in the class: "Who is Varun?" But only Varun will stand up. What is meant by this is: request broadcast, reply unicast. Just for example, that there's only one Varun in the class, otherwise some will think that, "Sir, there are two Varuns." Just for example I'm telling you, because a MAC address is unique, so it's always unique. If you don't want to take a name, you can say, "Roll number—that roll number, stand up," so that roll number will respond there. So here is the same story. 

So now C will respond in a unicast way; this is the communication. There can be four different cases. 
1. Host to host: If there's a host and it wants to communicate with a host, this is one case. 
2. Host to router: Host is there and let us say it wants to communicate with the router, meaning with the gateway, it wants to communicate with the default gateway. Let us say A wants to communicate with the default gateway. What will be the reason for that? Maybe let us say A wants to communicate with Z, meaning A wants to communicate with Z which is in some other network. In that case, you'll have to go out of the network. So you know that you'll have the IP address of Z, because the IP address tells you the location, but actually the MAC address is the address of that machine. So in that case, you'll have to do that: A will first enter the IP address of the default gateway, but it doesn't have the MAC address. So A first sent it a broadcast; it gave its MAC address. When it got the MAC address, it filled it there and got there. Now it has to send it forward. So what it will do—it doesn't have its MAC address, the IP address is there—so it will enter its own IP address, MAC address, and their IP address and send that. And since it doesn't have its MAC address, it will respond to that, "This is my MAC address." Now, to get the MAC address of Z, again it will broadcast, and the reply will come. 

So what is meant by this is that communication will be afterwards; first you have to find the MAC address. After that, you can cache that MAC address in your machine so that later on you don't have to send messages again and again. Later on, you can just pick from the cache that this is the MAC address of the machine. So the communication goes on like this. 

So, host to host, host to router, you can say router to host, or router to router. In these four ways, communication can be done. 

Finally, if we talk about the header information—often some theory questions come about the header, so you should know:
- Hardware type: Hardware type is your field of 16 bits. This all is of 32 bits, hardware type is of 16 bits. Like if we're using Ethernet, so the type of Ethernet is 1, so in that case we'll input Ethernet type as 1.
- Protocol type: This is also a field of 16 bits. So which protocol? Either you're using IPv4 or IPv6. So if you're using IPv4, then 0800, in that case this is its value, hexadecimal value.
- Hardware length: Means the hardware you're communicating with, its length. Its length means—there's no length of the hardware, hardware length means the MAC address of that hardware. So let us say, normally if we talk about the MAC address, obviously it is of 48 bits, which we represent with 6 bytes. So actually you'll get 6 bytes. So hardware length, if you're talking about Ethernet, so in the case of Ethernet we'll put value 6 here.
- Protocol length: Protocol length, it's IPv4, so its value will be 4. The value of length will be 4.
- Operation: If you're requesting, then its value is 1; if you're replying, then its value is 2.
- Source hardware address: Then the same story, source will enter its hardware address, Ethernet address of 6 bytes.
- Source IP address: Source will enter its IP address, in case of IPv4 of 4 bytes, meaning 32 bits.
- Target hardware address: Now I don't know the target hardware address. To know that, either enter ff ff here and broadcast that, or you can write 000 if you want to send that message to a particular person directly.
- Target IP address: And you can enter the IP address of the target here in this case.

So this is the whole story of ARP, which works like this. So this story of request-respond, plus related to headers—especially if we talk about the GATE exam, they recently included this, it may be possible that some theory question comes. Thank you.