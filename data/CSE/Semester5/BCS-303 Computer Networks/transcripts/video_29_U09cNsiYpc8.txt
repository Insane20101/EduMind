Hello friends, welcome to Gate Smashers. The topic is Parity, Single Parity Bit. We use the Single Parity Bit method for error detection. And although this is a simple method, and we call it least expensive method. Means if we consider very high cost, cost in the sense it is not of money here, cost in the sense if we want to send more data, we want to send less redundant bits, means some application which says that we can bear little error, like audio, if we send audio in which there is little problem in the sound, if it says that we can bear it, then this type of data in communication we may not use, if we want to use then what is the least expensive method? That is Single Parity. 

Why? In Single Parity here we use m+1 funda, m+1 means, m is the number of bits or you can say m is the number of message bits. Means the message you are sending has how many bits? m. And in that you have added only 1 bit. If we talk about Double Parity or CRC or Check Sum, in that we send more number of bits with the message, we send more number of redundant bits. So we are sending less extra bits here, therefore it is not very costly. It is least expensive and it is a very simple method. 

Here we can use Even Parity or Odd Parity. So this is Single Bit, and in Single Bit Even Parity is mostly used. Even Parity means that the number of 1s should be even. Means in your data word the number of 1s should be even. Although you can use Odd Parity also. Odd Parity means the number of 1s should be odd. Let's say if I send data 1010. Generally the data words that we send is not of variable length. When we send the data, if we are sending data of same length, then same length means in 2 raised to power. Let's say a data word is of 4 bits, 8 bits or 16 bits or 32 bits or 64. So we generally try to send in 2 raised to power because it is Binary Bit, because we are talking about data communication in the form of 0 and 1. If we talk in the form of 0 and 1, then what is the base of Binary Bit? 2. So therefore we use 2 raised to power more. 

So if we have data 1010, then you count number of 1s. How many? 2. Then here in Even Parity which bit we will add? 0. Reason? Because already here number of 1s is even. So the redundant bit, the extra bit that you are sending, this 1 bit extra you are sending, its value will be 0. If you are sending data let's say 1110, this data if you are sending then count the number of 1s, 1, 2, 3. There are 3. So the extra bit you want to send will be 1. Why? Because in the entire code word this is your data, and when you add 1 it becomes a valid code word. Then in this code word number of 1s are how many now? 4. This is called the Even Parity. 

Now here it can detect all the single bit errors in the code word means if I am sending this data, let's say 11101 data if I am sending, then in this data on the way, if due to any problem, like I discussed in the previous video that why we need detection or why actually error occurs. Due to any noise, due to any natural effect like lightning or thunderbolt, then because of that if any 1 bit changes, let's say if I change the first bit, then what will this become? 01101. So receiver will see okay, number of 1 should be this code word. How many number 1s are there in data? 2. So if number of 1s in data are 2, then parity should be what? 0. But how much is the parity? 1. So from this it will understand that some change has occurred in this. Means some change has occurred in data. So from this it can detect that there is some kind of error. But at which location change has occurred, that it will not know. Means it can detect but it cannot correct. 

Along with this if we change in double bit, let's say 11101, if I send this data and it reaches the receiver as 00101, means first 2 bits we changed, you can change any 2 bits, let's say first 2 bits if we change then what is received? 00101. Means the last one is parity. So it first checked in data word, 0010. How many 1s are there? There is a single 1. So means what should be in parity? 1. And look what is in parity? There is 1. Why? Because what is total number of 1s? 2. Means this is even parity. If it is even parity then receiver will feel that sender has sent this data. But sender had sent what? 11101. So here detection cannot happen. 

So means what can it detect? Single bit. Along with this it can detect all other errors, if number of bits changing are odd. Means if 1 bit is changing, or 3 or 5 or 7... If these type of numbers bits are changing, then it can detect. Let's say if 3 bits I change, if I change first 3 bits. Like I send 11101, this is the parity bit, and 00001 reaches receiver, means I change the first 3 bits, you can change any 3 bits. So if I change this here, when receiver counts it will see number of 1s in the data word. How many are there? 0. So what should be the parity? 0. But what is the parity? 1. So from this it will know that some error has occurred. So it can detect all errors if number of errors is odd. Means its length is odd. So these are the major points related to single bit parity. 

One small point I added here. In this question students have many queries, it troubles students a lot, funda of Hamming Distance. I have brought here the funda of Hamming Distance. Hamming Distance means if you are given this data word, let's say these are 4 bit data words from 0000 to 1111 I have all these strings. All possibilities I wrote. And here in red color I have also counted their parity. Let's say 0001, how many 1s? One. So I put one more 1, it becomes even. So here if you see in all code word, this becomes valid code word. This is what? Code word. So if you carefully see this whole code word, parity is already attached to it. 

So here if we talk about Hamming Distance, what is Hamming Distance actually? Hamming Distance is... Hamming Distance is what? You have to perform EXOR operation on any 2 code words. Let's say 0000, 1111. Take any two values. When you perform EXOR on them, EXOR means 0 in same values and 1 in different values. So 1 will be in different values, so these 4 values come here. So what is its Hamming Distance? 4. 

Let's say if I take 0101. And we have data 1000. If I have to calculate Hamming Distance of these 2 code words, what will it be? EXOR is applied, 1101. So how much is Hamming Distance? Count the number of 1s. How many 1s are here? 3. So Hamming Distance here is 3. 

So if you check Hamming Distance for this entire code word, in all these strings, some will have 2, some will have 3, but minimum Hamming Distance will be how much, I will tell you but you have to count. Minimum Hamming Distance will be, in this case, 2. Minimum Hamming Distance, let's say, see this 00000 and here is 11110. So what is the Hamming Distance between these? Between this and this. It's 4. Similarly if in some others, let's say if you count between these two, it is 2. If you check all this, then you will find out that minimum Hamming Distance is 2. 

So funda of Hamming Distance here is if minimum Hamming Distance is d, then it can detect d-1 bits. It can detect error in d-1 bits. Means if in this case you are asked that we send this data word, this code word, can it detect errors d-1 bits? See this, what does d-1 mean here? 1. Because what is the value of d? Minimum Hamming Distance is 2. So d-1 means 1. 

Why it can't detect 2 bit error? Why it can't detect d number of errors? Reason is see, if I take 00000 meaning of d is you can change any two bits. If I change any two bits. Let's say I change first two bits, then what will it become? 11000. And look, 11000 is already present in the data. So receiver will not know in this case that 11000 is a valid code word, so if you change any two bits then that value will be among these valid codes. So from this receiver will not know whether an error has occurred or not. 

But if you change any one bit, let's say if in the first case, I change only one bit. Let's say I change the first bit, then what does it become? 10000. See in the entire valid code word, is there 10000 anywhere? It is nowhere. So from this receiver will know that there is some error in this because this data is not matching with any code word. 

Many times this type of question is also asked that if I have these code words then calculate minimum Hamming Distance, or how many bits error it can detect? So remember a simple funda, calculate minimum Hamming Distance, whatever value you get, let's say d, then it can detect d-1 errors. So this is also one part of this video which troubles students many times that from Hamming Distance how we calculate how many bits it can detect? So that's why I have brought this in the video, that's why video became long because single bit parity is a small point, but overall it will help you a lot from the point of view of UGC-NET and GATE. Thank you.