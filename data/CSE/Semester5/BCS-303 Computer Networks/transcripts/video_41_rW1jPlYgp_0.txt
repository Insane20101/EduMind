Hello friends, welcome to Gate Smashers. The topic is Network Layer. Network Layer is the third layer from below in the OSI Model, which takes data from the Transport Layer and passes it further to the Data Link Layer. Now, what are the responsibilities or functionalities of the Network Layer? This is what we have to discuss in this video. Meaning, if I have a source machine—all these layers are working in the source machine—how is the Network Layer helping in the transfer of a message in networking, what are its responsibilities, what are its functionalities, and what are its roles? This is what we are going to discuss.

The first and very important role is Host-to-Host delivery, or we can also call it Source-to-Destination delivery. Host-to-Host delivery, Source-to-Destination delivery, or it is also called Machine-to-Machine delivery. Machine-to-Machine delivery means, from the source machine to the destination machine, whose responsibility is it to transfer the message? The Network Layer. Let's understand with an example. Here I take Network A, and I have Network B. In Network A and Network B, let's say different topologies are used. Here I use a hub topology, and here I use a bus topology. Now, what is the responsibility of the Network Layer? How can a machine in one network communicate with a machine in a different network? Meaning, here is a machine, let's say A2. A2 wants to communicate with B2. Now, there won't be direct communication here. Direct communication won't happen between A2 and B2. Meaning, they will have to send the message through a channel, a media. So let's say this is the media. Now, from A2, the message will first reach R1. From R1, it will reach R2, and from R2, it will reach B2. So from A2 to R1, the responsibility of transferring the message is of the Data Link Layer, which we had discussed before; it works on node-to-node delivery. But here, it's not node-to-node delivery. Here, the work of source-to-destination delivery is of the Network Layer. 

How does it perform Host-to-Host delivery or Machine-to-Machine delivery? By using addresses. The addresses are known as Logical Addresses, which we also call IP addresses. IP addressing. This is a very important point: how does Host-to-Host delivery or Source-to-Destination delivery get performed? By using the Logical Address. Logical Address means that a Logical Address or IP Address is basically a combination of two things: one is a Network ID, and the other is a Host ID. Meaning, we know two things from one IP Address, that in an IP address... in an IP address, network and host... host means machine. From the IP address, I will know in which network to send the message, and in that network, there are many hosts, many machines, many servers. In that network, on which machine to send the message is the responsibility of the IP Address, and who uses this IP Address? The Network Layer uses it to send the message. So that's why we call it Host-to-Host delivery. Meaning, how the message is passed on in between—that is the job of the Data Link Layer. The Network Layer's job is to transfer the message from A2 to B2. How to transfer? By using IP addressing, where A2 will give the IP address of the source, and along with that, it will put the IP address of B2 in that message. Now, how we find these IP addresses and how we use them, we will see in detail further. The main important point here is that Host-to-Host delivery is performed using IP addresses because from the IP address, I know which is the network and which is the host in the network.

The other responsibility of the Network Layer is routing. This is again a very important part. In routing... I'll first write the methods here. There is the RIP method, the OSPF method; these are different protocols, Routing Information Protocol... These are the methods which are used to do the routing. Now, there is a difference. In RIP, we use Distance Vector Routing or Link-State Routing, which are full-fledged protocols. Protocols means what here? Protocols mean a set of instructions. Meaning, the message going from the source machine to the destination machine will follow what instructions. And it is very important to follow a protocol because all other people can use their own protocol. Meaning, we will make our own protocols and deliver the message. But due to that, the message will not be delivered properly. So there should be a standard protocol so that the source uses the same protocol and the destination uses the same protocol, so that communication will be fast, convenient, and efficient. 

Now, what is the meaning of Routing? When the message reaches R1 from A2, first the message comes to this router, then this router will decide—this router, this machine will decide—how to route the place where the message is to be sent. Route means we have to decide the path here. It is possible that this router is connected further with different networks, because this diagram that I have represented is of the internet. Internet means a connection of different networks. So if these are all different types of networks, then a router is not necessarily connected to only one or two networks; it can be connected to multiple networks. So this packet is to be sent on which router? Let's say it is to be sent on R2. How to decide this? By using routing. And we try to follow the shortest path so that our message reaches the destination soon. 

Here I am using the word router, I am using the word router because a router is a device we use on the Network Layer. On the Network Layer, we use routers; on the Network Layer, we can also use bridges; on the Network Layer, we can also use switches; we can use firewalls also. But the most important devices used in the Network Layer are routers and switches. Meaning, every router will definitely have these three layers. But in bridges, basically, there are two layers: the Data Link Layer and the Physical Layer. So in the Data Link Layer, we basically talk about which device? Bridges. Here we are talking about routers. So routers are such intelligent devices that they can decide themselves. Meaning, the source sent a message; the source said, "Let my message reach B2." It said, "Let it reach from this route." But after that, the message will come to R1, so the router has to decide what the best route is. The router has this intelligence. How? Because it has routing algorithms working. Why? Because the Network Layer works on routers also, so the router will decide what the best path is, then R2 will decide what the best path is. That is called routing. This is the major responsibility of the Network Layer. So Host-to-Host delivery, routing, works on Logical Addresses.

The next responsibility or role is Fragmentation. Fragmentation. Now, what is Fragmentation? Whenever we send a packet—because the terminology we use on the Network Layer is packet. Here we use packet switching. Meaning, we also use switches in the Network Layer. Switches are also intelligent devices which basically help... If switches are not there, then through many wires... like in a mesh topology, we use many wires... means if we have multiple devices, then to connect them I will have to use many wires. Switches make the wires less, so here we can also use switches. 

So when this packet goes from one place to another, there I may need fragmentation. Why is fragmentation needed? Let's say a host sends a message to a destination. But that message goes first to the R1 router. Now, the R1 router, let's say it sends the message to some other router... it sends the message to some other router. Now, that router may have its own capacity. Capacity means what size of packet it can accept. Like in CSMA/CD Ethernet, a message of 1500 bytes maximum can be accepted. But if my message size is very big, means if my packet size is very big, then it can't accept it. Meaning, the intermediate node will not be able to accept the large packet which is not... its buffer size is less, and the packet size is larger. 

So what will the router do? It will not send all the data at a time; it will divide the message into fragments. It will create multiple fragments according to its capacity and further pass on the data. So how fragmentation is performed—that also we will see in details further. This is the major responsibility of the Network Layer.

There is one more responsibility, although it is not used very much: Congestion Control. Congestion Control. We talk about congestion in the Network Layer, but we basically use congestion on the Transport Layer to control congestion. Congestion means it is possible that we filled the network completely. Meaning, the capacity of the network—how many packets it can transfer further—if I fill the network completely with packets, meaning if all nodes at a time send an infinite number of packets, then the network will be filled with messages. That is called a congestion situation. 

Now, how to control this congestion? We handle congestion control less on the Network Layer; we basically use it on the Transport Layer. But still, for congestion control, we use the Leaky Bucket method or the Token Bucket method; these are different methods which control congestion. Or we use ICMP or IGMP protocols here. If a message is going and if a problem occurs in the message, then the router sends an acknowledgement to the source that there is a problem in the message. Or if it is sending at a very high speed, then they send an ICMP packet to reduce its speed of sending the message. We will talk about ICMP and IGMP in further details. Here we are only discussing what the Network Layer's responsibilities are, what its roles are. So these are the major roles of the Network Layer, in which the most important is Host-to-Host delivery, which is done by using the IP Address. 

So this is all about the Network Layer. Thank you.