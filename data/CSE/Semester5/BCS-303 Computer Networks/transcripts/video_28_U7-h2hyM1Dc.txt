Hello friends, welcome to Gate Smashers. The topic is Error detection and Correction. 

First of all let's talk about what is an error. Simple meaning of error is the data which sender sends, if receiver does not receive the same data, it means there is a kind of error. Means if sender sends data 101, and on the way the data changes or whatever the reason, and receiver receives data 100, there is an error in the data. So here the first point is that how we will know that error has come? Because sender... let's say sender and receiver are very far away, sender is in India and receiver is in USA. Then how will receiver know that some problem has occured on the way? 

For that we use the error detection method. Error detection method means that atleast we know that there is some error in the data, because if I don't know that there is some error in the data, then receiver will simply accept this data. But if you look carefully, what had the sender sent? 5. If I convert this into a decimal value it becomes 5. And here if we talk, it becomes 4. So data changes completely. So if we talk this way that atleast I know that there is some error, then we can use error detection method. 

Then comes error correction. Error correction is an advanced step of error detection. Means I know that there is some error. I also want to find out in which bit there is an error. So this bit is in error state, so we can correct that bit. So this is the basic funda of error detection and correction. And this error detection and correction, if we talk about the OSI layer, then where does it occur? It occurs in the Transport Layer and in the Data Link Layer. So we are talking from the point of view of the Data Link Layer, where we check bit-by-bit is there any error or not. 

Next point if we talk about here, what are the types of error? Single bit error and Burst error. In error, it is also true that it depends on the application. If application let's say sends some audio, if we talk about audio or video, then many times what happens is, some content in audio and video comes with error. Means some change occured in the content, then there was little problem in audio, but we bear it. But specially in text, if there is an application like WhatsApp, FaceBook, where we are chatting, or sending real time data on the basis of text, then there we cannot bear this error. So this basically depends in the application. So application, it can detect and correct error if it wants to, if it doesn't want to, then it may bear some error. 

Single bit error means, as the name suggests, there is an error in one bit. Like this example that I gave here, if sender sends data 100 and receiver receives 101, means there is change in any one bit. In the entire data block, whatever packet we send, whatever block we send, in that block only 1 bit has changed. That is called a single bit error. And generally what is the reason of occurrence of error? The reason error occurrs is that when we transmit the data, we transmit to long distances. So when it travels to long distances, many times energy is lost, attenuation occurs or there is also a natural effect, there are environmental effects also, thunderbolt or because of lightning, or many times what happens is some machine which makes a sharp noise on starting. Because of these reasons many times there is change in bit. And if we talk about digital data, the simple meaning of digital data is 1 becomes 0 or 0 becomes 1. In this the meaning of data changes. So if the meaning changes then that data is of no use to the receiver. So what we are talking about is how will receiver know, that is error detection, and if it can also correct, that is called correction. So in single bit, means one bit is changing. 

Burst error, which normally occurs, burst error is what? Where more than one bits are changing. Let's say if I send data 101010 and receiver receives 111010, or let's say 111011. This is the type of data receiver is receiving. Here burst error means more than one bits are changing. Like this bit has changed here, and other than this, the next one we have, this bit is also changed. First bit 1 same, next bit changed, third bit same, fourth bit same, fifth same and sixth same. So here I changed 2 bits. This is a simple example, you can frame any example you want. Wherever more than one, two or more than two if there is change in data, change in bits, that is a called burst error. 

Here if you are asked, although it is a simple point, but if you are asked what is the length of error. What is the length of error? Then how do we find the length of error? If the bits in between are not changed, they are as it is, even then we count it. How? See this, first bit that changed is this, 1, 2, 3, 4, 5. So we count the duration from here to here. So this entire duration means this second bit changed and then this sixth bit changed. So what is this entire duration? Duration is of 5 bits. So what is the length of the error? 5. But how many bits actually changed? 2. But because of this the meaning of the entire data will change. 

Let's say if the bandwidth of a channel is 1GBPS, then for how much duration the error should last? Means it is a simple point that if let's say A and B, two nodes are connected to each other through a channel and the bandwidth of that channel is 1GBPS. 1 Gigabits per second. If I don't talk about delay here, if I don't count propogation delay, then in that case it is asking what is the duration of the error? Means if an error is occurring in this, then for how long should it occur? So how will we find this? It has a simple meaning that 1GBPS means 10 raised to power 9 bits. 10 raised to power 9 bits you send in 1 second, you transmit. Means in this one bit, 10 raised to power 9 bits will reach from here to here in one second, if I don't count delay, this constraint I carry. So time of one bit is how much? 1 divided by 10 raised to power 9 seconds. Which is also 1 nanosecond, means this becomes 1 nanosecond. So means if error occurs for 1 nanosecond, means its duration is of 1 nanosecond, then definitely there are chances of single bit error occurring. But the error doesn't occur for 1 nanosecond or 1 microsecond, it occurs for longer duration. That's why burst error is more common compare to the single bit error. 

If here another little point is asked that if let's say error occurs for 1/1000 seconds time. Means there is an error, due to any reason, any cause of error, and the duration of that cause is 1/1000 seconds. In this same question. Then how many bits, how many bits will be corrupted? How many bits will be corrupted? How many bits will come in error? You simply multiply this by 10 raised to power 9. Means 10 raised to power 6 bits will be corrupt. These many bits will be corrupt if error stays for 1/1000 seconds. Means these are very simple points, but many a time what happpens is because of simple points we get questions wrong or we miss out a point. So you have to keep these points in mind. 

Then we have the detection methods. If we talk about detection methods then which are the detection methods? Simple Parity in which we talk about even or odd parity, there is 2 Dimensional Parity check, there is Check sum and CRC which is the Cyclic Redundancy Check. So these 2 methods in this are very important methods, very important because we actually use check sum on transport layer also generally, and CRC is a very important method. All these methods have their own advantages, their own limitations also, which we will discuss in further video one by one. And for error correction we use Hamming Codes. 

All these methods are based on which technology? What are they based on? That base is redundancy. Redundancy means... Means if I send, let's say there are 8 bits of data. If I send all 8 bits of data, then there is no way to check whether error has occured or not. I always have to send extra bits. Extra bits means that if I have a block of 8 bits, then I cannot send data of 8 bits, I will send 6 or 5 or 7. Remaining bits will be what? Redundant or you can call it extra bit which are used for what? For error detection or correction. 

So in the next video we will see all these methods. So this is the introduction of Error Detection and Correction. If you like this video then please like it, share it and please subscribe my channel. Thank you.