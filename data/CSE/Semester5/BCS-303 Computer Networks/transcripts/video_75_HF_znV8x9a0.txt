Hello friends! Welcome to Gate Smashers. The topic is UDP, that is User Datagram Protocol. In the transport layer, we have seen that two protocols are very important, that is TCP and UDP. So we are talking about UDP in this video—where we use User Datagram Protocol, and we'll discuss the UDP header. 

First, I want to tell you about UDP: it is a connectionless protocol. What is meant by connectionless? Let's say a client wants to make a connection with the server, if a client wants to transfer data to a server, or from any one machine to another machine. What does TCP do? First, it establishes a connection, and after establishing the connection, data transfer starts. But UDP is connectionless; it means we're not making any connection here. It's not like a telephone network where first it will dial up, then give a dial tone, and then there will be a connection. No, it's like a postal service. If I want to send any mail, if I want to send a letter, then without making any connection, I'll simply write the letter, put it in the envelope, and simply post that letter. We're doing the same thing here. UDP is just like that service; it works like a postal service. What can we assume the letter is? That is the data, which is known as payload in technical terms. Payload means pure data. We will put that pure data into an envelope. What is that envelope? The TCP header (or UDP header, respectively). Each and every protocol has its own envelope. Envelope, I'm taking it just as an example; the header is used by which that letter reaches its proper destination. 

And if I want any functionality other than that—let's say if I'm sending four or five letters, the four or five letters that I'm sending, they should be in order; none of those letters should get lost—we have different functionalities. TCP follows all those functionalities. But for UDP, the first important point is: without making a connection, we will write the data, and that data will be sent to the server. I'll simply send that data to another machine without making a connection. Due to that, the problem I am facing is—well, we're not talking about the problem here, we're simply talking about characteristics—so that is unreliable. Unreliable means if I am sending some packets and out of that one or two packets get lost, UDP doesn't care about that loss, so it is unreliable. Other than that, there is no ordering. Ordering means if I am sending three or four packets—Packet 1, 2, 3, 4—there it may happen that first I have received Packet 3, received Packet 4, and Packet 2 reached later. Why? Because there is no sequence number; we're not using any sequence number here.

There were many options in the TCP header, but in the UDP header, there are only four options. 

First is Source Port. Source port means we've already discussed about ports, that for end-to-end deliveries we use port numbers. What can be the maximum port numbers? 0 to $2^{16} - 1$, which is 0 to 65535. This is the range of ports, out of which from 0 to 1023 are well-known port numbers. After that, some of them are registered port numbers, and then there are some normal port numbers which we use in our system. If I am sending data in my system, which application is sending data? That application will have a port number. How do we get that port number? When we open a browser, as we connect to the internet and open the browser, the operating system assigns me a port number; it means that browsing application got a port number. Now, if I am accessing the web server through that port number, or accessing FTP, or simply sending a mail there, there are different port numbers. Like if I am sending a mail, maybe I will use port number 25 for mailing, meaning I will use a destination port number. Source port and destination port—these two options are mandatory. Otherwise, end-to-end delivery, which is the responsibility of the transport layer, will not be achieved if I do not fill the source and destination ports.

After that comes Length. Length is what? 16 bits—what I have written here is bits, meaning a length of 16 bits. But it does not mean that the length is 16; the actual length is $2^{16}$, and here it is represented using 16 bits to represent the size. But what is the actual size? The actual size is $2^{16}$. So if I talk about $2^{16}$: if I have a number of 16 bits, in that 16-bit number, the maximum number can be 65535. Like if I tell you that I have 4 numbers, let's say 4 bits, so how many numbers did I make? $2^4$ means 16 numbers are made in total. But in 4 bits, what will be the maximum number? That will be 1111, that is 15. It means if 16 are my total numbers, what is the range? 0 to 15. In this, 15 will be the maximum number, and 0 will be the minimum number. Here also, I am writing the same: $2^{16}$ means where it is in actual? 0 to 65535. What is $2^{16}$? 65536. But what range of numbers has been made? 0 to 65535. From this, what is the maximum number? This one. So the maximum size of the segment can be 65535, which we can call the total length. It means the total length of the whole segment can be a maximum of up to 65535, which is known as total length also. 

But in total length, there are two things: one is the header, and the other is the payload. Payload means pure data, which comes from the application layer. So if I talk about the header, the UDP header is always fixed at 8 bytes. So if it has a header of 8 bytes, then the pure data can be calculated by subtracting 8 bytes from this total bytes: so, $65535 - 8 = 65527$. 65527 bytes—this is the size of the payload. It means you can calculate it normally. This is the maximum value; data can be less than this, but data cannot be more than this because a header of 8 bytes is compulsory. Without a header, you cannot send the data.

After that comes Checksum. Checksum is used for error control, meaning we use checksum for detecting errors. I have already mentioned here: how do we calculate checksum? We calculate it in the same way in TCP and UDP. We take the UDP header, take all the values of the UDP header—the source port, destination port, all the values I have taken—other than that, we took that data plus a pseudo header of IP. Combining all these three, we are calculating the checksum. Checksum is what? Checksum is like a hash value, meaning we will find a unique value of data. As something changes in that data, that unique value will also change, due to which the receiver will get to know that the hash value I am receiving and the one I am calculating, there is a difference between them. Through this, we'll know that there is some error in the data. 

Here, if I talk about pseudo header, what does pseudo header of IP mean? The IP datagram—the header we use of IP in the network layer—that IP header, we don't use the whole IP header here. We only pick up only those values which are fixed, meaning which remain the same, like IP address of source, IP address of destination, and for TCP/UDP, we use upper layer protocol. It means there are 3 or 4 fields which remain the same in the IP header throughout the transmission; we'll only take those values. That's why we are not taking the entire IP packet; we are just taking the pseudo header of the IP, meaning those values whose entries in the IP address don't ever change—like the source IP address and destination IP address that do not ever change on the router, but many other values change. When we discuss about IP addresses, there you'll understand it better. Here, if we just see a general overview, we calculate a unique hash value by taking these three things and we send that value along with data. As the receiver gets that data, the receiver will also take these three things, and after taking these values, he will again calculate the hash value. That hash value and the one the sender had sent—if those both match, it means there is not any error in the data. If the hash value of the receiver and the one the sender had sent are different, it means that there is some error in the data.

However, here is an important point. We've just talked about the fact that UDP is unreliable, meaning if there's an error or any packet gets lost, then UDP doesn't care about it, neither does it care about the ordering. However packets reach, they can reach through different paths, or from the same path also. Basically, different packets follow different paths. What happens due to that is packets get lost. But TCP always works on a dial-up connection; it makes a connection first, and all packets will follow that connection path only, due to which packets do not follow different paths. But in UDP, if I am sending data from A to B, one packet may reach through another path, one packet from another path; packets transfer through different networks, due to which there are chances of packets getting lost. 

If it is unreliable, then is the checksum field mandatory? No. This checksum field is optional in IPv4. This field is optional, but in IPv6, which has been recently launched, in that the checksum field is mandatory. But we're more discussing about IPv4, where this field is optional, meaning whether you use it or not, then also it will run. So, if we see, the main are these three values which are used mostly in the UDP header. 

If UDP is unreliable—meaning packets are getting lost, or there are some problems coming in packets, if packets are reaching unordered also—then which are the applications that use UDP in a real-time environment? That we are going to see in the next video. Thank you!