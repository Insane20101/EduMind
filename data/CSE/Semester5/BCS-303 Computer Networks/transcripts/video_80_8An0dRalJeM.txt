Hello friends, welcome to Gate Smashers! Many students requested a video on a list of important protocols and their corresponding port numbers that would help you at the last moment before going to the exam. So here, I have brought the same. It has a list of important protocols, their corresponding port numbers, together with the transport protocol that they use. I will explain these three things in this video.

The first column that you can see is the name of the protocol. All these protocols are the protocols of the application layer. In the application layer, we have thousands of protocols, so you don't have to remember these thousand protocols or remember their full form. But these important protocols—in what terms am I saying it as important? I am saying it in terms of the number of times questions have come on them. If we talk about any exam in computer science—NET, GATE, KVS, any competitive exam—in those exams, maximum to maximum these protocols were asked. So I am not discussing about their importance, their performance, their throughput; I am talking about their importance as in the number of times questions have come from this and the chances of them coming. Even if you remember all these protocol names and their port numbers, it is more than enough.

Second, we have here is port numbers. We generally use port numbers in transport layers. In the transport layer, we talk about that the system allocates a port number of 16 bits. 16 bits means the total number of port numbers possible is 2 raised to the power of 16, which is 65,536. In this, we call 0 to 1023 well-defined port numbers. Well-defined port numbers mean that they are used for standard applications and standard port numbers. So you cannot remember 1024 port numbers and their applications; that's why even if you remember only these, it is more than enough.

Third, it is written "Protocol" about transport protocols, meaning which application uses which transport application. In the transport layer, we only have two transport protocols: TCP and UDP. These are the main protocols. We already have HTTP, but we generally use TCP or UDP—Transmission Control Protocol and User Datagram Protocol. TCP is called connection-oriented, and UDP is called connectionless. How do TCP and UDP work, what are their descriptions? I have already made videos on this. I have given the link in the description box, please check them so that you know how TCP works, what its applicability is, what its applications are, where UDP is used, and where it is not used. You will find this in that video.

First, if we talk about echo, we generally use the echo protocol where the client sends the server a request and the server responds to that. This total time is called round-trip time. So what is this total time, what is this round-trip time? To find that, we use the echo protocol, and an administrator uses the echo protocol to find out if we are connected to the server or not. You might have heard about ping; ping is generally used with the ICMP protocol, but it also serves the same purpose: that if we are connected to the network or not, if we are getting a response, that means we are connected to the server. If a response is not coming, it means that the connection is not established. We come to know if the connection is made or not, and it is also known how much time it will take to respond to our request. This round-trip time is told by the echo protocol. Echo protocol is used for TCP as well as UDP, meaning the echo protocol can use TCP and it can use UDP as well. If your connection is TCP, then it will send to TCP; if it is UDP, it will send to UDP echo. Its port number is 7.

Then FTP—FTP is very important, that is File Transfer Protocol. If you want to transfer files, to transfer files we use the FTP protocol. In this, we have port numbers 20 and 21. Some students get confused: why are there 2 port numbers? Because generally, the protocols that we have are inline and out-of-line protocols. Out-of-line means they transfer data from different port numbers, and the control statements, the control commands, for them they use a different port number. FTP does the same: for data, it uses port number 20, and port number 21 is used for commands—commands that we generally use. FTP commands are start command, quit, stat, help. If we want to send all these commands, for that we use port number 21, and to transfer data, we use port number 20. So these 2 port numbers are reserved here. Which protocol does it use? TCP. What is the reason behind that? Because reliability is needed. The complete file should be transferred; it should not happen that some part happens and some doesn't. No, if the complete file is transferred, then only it will open. Wherever reliability is required, where you want reliability, TCP is used there.

Then we have Secured Shell, meaning the SSH protocol is there. For SSH, we have port number 22. For what purpose do we use SSH? Where security is required, we use cryptographic functions. Here, if two people make connections—meaning if the client and server are connected here—they are connected through a secured key, meaning they are connected by exchanging keys, and in one way, a tunnel is formed. We use tunneling here, and what happens because of tunneling? The unsecured channels—because what is the internet? The internet is unsecured. But in unsecured and public networks, how can we do tunneling, make a private connection? For that, we use Secured Shell, SSH. It is used from a security point of view. The port number is 22, and it also uses the TCP protocol, that is connection-oriented.

Then Telnet. For remote login, we use Telnet. This is also very important. You will find questions on this many times, and its port number is 23. It also uses a TCP connection.

Then we have SMTP, Simple Mail Transfer Protocol, that we use for mailing, and the port is 25. On this, you will already find a question: what is the port number? 25. It also uses a TCP connection, meaning it requires connection-oriented.

Then comes DNS, Domain Name System. Domain Name System is again important, and its port number is 53. But what is important in this? What transport protocol does it use? It uses UDP, User Datagram Protocol. When do we use the UDP protocol? When we want speed, meaning we don't want to establish any connection, we want to send a request quickly, and we want a response quickly. Where we consistently want speed, there we use the UDP protocol. So here in DNS, why do we use DNS? We have the domain name, but we want the IP for that. To find that server's IP, we simply send a request. Although it can also use TCP—it can use TCP—but generally, UDP is used by default. Why? Because we send a request that this is the domain name that I have, I want its IP address. We want a fast result, a consistent speed is required; for that, we use the UDP protocol.

Then DHCP, Dynamic Host Configuration Protocol. DHCP is used if a dynamic IP is required. Meaning, generally we use static IP in labs, in computer labs in colleges and universities. In those PCs, a static IP is given, static IPs. So whenever you establish a connection, then already an IP is given in that; in that case, DHCP is not required. But if your administrator wants to assign dynamic IP addresses, you can use a local DHCP protocol. You will give your MAC address, a request will be sent from your computer that "My MAC address is this, I want an IP address." Then dynamically, from a list of IP addresses, DHCP will provide an IP address. In the case of static, we don't use it; it is used in the dynamic case. This is again outbound. Outbound means we use two port numbers: 67 and 68. Meaning a different one is used for data transfer, and a different one is used for control statements, same as in FTP. It also uses UDP because there is no need to establish a connection in this. Again, you want speed, that you gave the MAC address and you want an IP address, you don't want to waste time. So where you don't want to waste time, there is no concept of reliability; that's why you will use UDP here, it will be a better option.

TFTP, that is Trivial File Transfer Protocol. Here we talked about File Transfer Protocol; here we have Trivial File Transfer Protocol. Trivial File Transfer Protocol is used when we want that, let's say there is a client, and the client wants to send some files to the server, and if the client wants to send files to the server without making any connection—meaning it does not make a connection, it sends the file directly—then in this case, your data may also get lost. But if your application says that there is no issue, then you definitely use TFTP, which is a Trivial File Transfer Protocol. Because FTP will first make a connection, use 3-way handshaking, and after that, it will transfer data. In this, more time will get spent because of reliability, and if any packet gets lost, it will send it again. But TFTP is fast, it uses the UDP protocol, and its port number is 69.

HTTP, again this is very, very important: Hypertext Transfer Protocol. All your web pages, all your browsers—in all those browsers, you would have always seen whenever you send a request to any application or server, through what is this request sent? Through HTTP protocols. All the web pages get transferred through the HTTP protocol. It uses port number 80, and it also uses TCP, meaning connection-oriented based.

Then we have POP, Post Office Protocol. Post Office Protocol is generally used to pop mails. Meaning, your mail has already come to the server; now you have to pop the mail to the system. For that, we use POP. We use SMTP to push the mails, meaning to send the mails, meaning to withdraw the mails, to bring the mails into your system. POP, Post Office Protocol is used. It has different versions. If we talk about version two, that has port number 109, but version three, the latest version, uses port number 110. It also uses TCP. Obviously, when SMTP also uses TCP, these two work parallelly—one to send the mails, another to fetch the mails.

Then we have NTP, that is Network Time Protocol. NTP has a simple concept, that there are multiple clients connected to a server. If they want to synchronize their time, their clocks—obviously, if you are connected to a server, you want that the time of the server is synchronized with the time of your client's machine. To synchronize this clock, we use NTP, meaning to find the time, Network Time Protocol. So it uses port number 123. In this also, there is no need for a connection because you are simply finding out the time, so obviously, the UDP protocol is used here.

Then HTTPS. The HTTPS protocol is very important because it is a secured version of HTTP. Nowadays, you will find most applications on HTTPS. Although you don't need to write HTTPS, even if you write HTTP... still, we don't even write HTTP, we never type it in the URL, we simply type the domain name, like Google, Amazon, Flipkart, SBI, PNB. This is what we normally type. If you see carefully, your connection will be on HTTPS. Why? Because HTTPS works on secured SSL, that is Secured Sockets Layer. Its work is the same as that of Secured Shell. It uses cryptographic functions; your request will not get sent directly, it will be sent after getting converted into cipher text, with authentication, meaning your username and password will be authenticated, then you will get a session. You can get your work done within that session. Whenever your request will go with cipher text conversion and your username and password are always checked, meaning we use HTTPS. All the PNB, SBI, Flipkart, Amazon, all the payment portals, all of them automatically work on HTTPS, and its port number is 443, and it also uses a TCP connection.

Then RIP, that is Routing Information Protocol. We use Routing Information Protocol, Distance Vector—you must have heard this. We use this in the IP layer, the network layer. In that, we use distance vector routing, link state routing. So distance vector is a concept in which information is sent to the neighbor nodes. But its actual implementable protocol, that is RIP, and for link state, we use the OSPF protocol. Here, the protocol number for the RIP protocol is 520, and the transport protocol it uses is UDP, because it doesn't form a connection, it just sends its matrix to its neighbors—that with what distance I am connected to another node. This is what it simply sends; it doesn't form a connection to send matrices. Simply, a broadcast concept is used here.

So these are all main protocols. Again, I want to tell you that you have thousands of protocols, and in that, these are the protocols that I have brought on which questions are asked already or there is a chance for them to come. You can remember their corresponding port numbers and the protocol that they use. You can also remember that. Again, it is not fixed that it will only use this, like I said in this: Telnet or SMTP, in DNS—not only UDP, you can also use TCP. Because where UDP is being used, there TCP can also be used. Because UDP says that "I make work fast," and TCP says that "I will form a connection first, I want reliability, I want security." There, some delay of time occurs. So where UDP is being used, TCP can also be used there. But where TCP is being used, there UDP can also be used—it is not guaranteed. 

So you can read about all these protocols in the description, especially the ones I said are important that have a star mark. Please check all the links in the description box, because I have given links to all the important videos there that will help you a lot. Thank you.