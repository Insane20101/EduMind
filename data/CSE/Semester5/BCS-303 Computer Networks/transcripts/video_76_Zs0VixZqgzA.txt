Hello friends. Welcome to Gate Smashers. The topic is UDP application. 

We have seen that UDP is a connectionless protocol, which means it does not establish any connection and directly sends the data. But TCP always transfers after establishing a connection. Due to this, the problems UDP is facing are because of not making a connection. It is unreliable, and second, the packets are not synchronized because there is no synchronization in the header field, which means packets can reach out of order. Also, it may happen that some packets get lost. 

Which are those major applications that will use UDP rather than TCP? We are discussing those applications. 

First is Query Response Protocol, which means applications that are working on a query point of view, meaning one request, one reply. I am requesting, I am querying, and from there I get an answer. In this type of application, it does not require any connection establishment. If we see a simple example for this, it is DNS, Domain Name Server. If I am writing any domain name on a browser, we generally remember the domain name, not the IP address. It is very hard. If I am writing a domain name, where will that domain name go first? To the DNS server: what IP address corresponds to that domain name? It will reach me. For this, I am not required to build a connection because what were we wanting there? Let's say simply I am writing a domain name, say google.com. While writing the domain name, I will not be thinking that first it will make a connection, then I will receive the data. I am simply requesting what the IP address of Google is. What is the response? This is the IP address of Google. Wherever the one request, one reply will apply, there we will use UDP. 

Why? Because it works on less overhead. It doesn't have overhead. Overhead means the data we send, the overhead with that data is the header. We have to send normal data. What overhead I applied to that data? Header. What extra information I applied? Header. How much extra information does TCP apply? If you see in the TCP header, many fields are there which TCP adds to the data. But in UDP, there are only four things which it adds, out of which checksum is not even a mandatory field. This means, on an average, if we see, there are only three things which UDP adds to the data. Due to this, overhead is very less in UDP as compared to TCP. 

Other than DNS, DHCP, Dynamic Host Control Protocol, also uses this method. What do we do in that? If I have a MAC address but don't have an IP address, then I'll say that my MAC address is this. What is my IP address? This means I am asking a simple question, and there I get an answer. Before asking that question, I am not making any connection with anyone. If you see, generally, if you are going into a market and if you want to ask an address—that I want to go to that shop, what is the address of that shop?—before that, you'll not ask that where do you belong to? I belong to there. Have you seen that shop? Nothing! What will I simply ask a stranger? What is the address of that shop? And what will that person say? Go to that particular street or go to that particular area. So, one request is coming, and one reply is coming from there. That is called a Query Response Protocol. So, UDP uses this type of protocol rather than TCP. 

Second is Speed. This is a very important factor. Where I want speed, wherever I want high speed, there I will use UDP. Because of less overhead, UDP will work faster. What can be the applications? If I am using an online game or Voice Over IP, in these applications, speed matters. Here, if I use TCP, TCP uses Flow Control and Congestion Control. In congestion control, it also catches speed, but after a particular speed, its speed decreases all of a sudden. Why? If in case packets are going—meaning from one machine to another machine, packets are going continuously—even if a single packet gets lost, it will try to retransmit that packet, TCP. It will try to retransmit again. As it will try to retransmit it, the speed factor here will come in use. So, that's why we will use UDP, because here I do not want any congestion control method which reduces speed all of a sudden. There we use AIMD, which means Additive Increase Multiplicative Decrease, which means first we increase speed exponentially in powers of 2. After a particular point, after a threshold, when that value reaches the threshold, we bring down the speed suddenly. But here we want to maintain a constant speed. If speed is not constant, we'll not use UDP there, so when I want a constant speed, there I have to use UDP. And what is that? Online games. 

Otherwise, what will happen? You're playing a game, and many times the game pauses for one to two seconds. In that time, it might happen that other players will kill your player, or anything, whatever particular game you are playing there, your player got out. If the speed factor is a little bit down there, then your enjoyment of playing the game will be ruined. That's why we will use it in these types of applications wherever I need speed: UDP. 

Third is Broadcasting and Multicasting. Broadcasting, multicasting uses—like I will tell you with RIP, that is Routing Information Protocol. This is the protocol of the network layer, and in this, we use distance vector routing. What is distance vector routing? After every 30 seconds, up-nodes will share their routing table to neighboring nodes. This means one node shares its routing information routing table with its neighboring nodes, where we are broadcast or multicast—meaning if one person is sending data to multiple receivers at once. 

If I use TCP here, then you see yourself: this person, before communicating with all of these people, will have to establish a connection, connection, connection, connection, connection. This means how many connections he will have to establish! What is meant by establishing a connection? It will have to keep buffers. He will say to him also that keep this much buffer, to him also, meaning how much buffer I will have to keep reserved already just for transferring one message? But we cannot bear up this much in RIP. We simply want to broadcast or multicast a message to a group. So for that, we'll simply use UDP. We will not say to anyone that keep buffer reserve, buffer reserve, bandwidth reserve, memory. We'll simply send the data to a lot of people. Multicasting means we are sending it to a similar kind of group. Broadcasting means to all the people, maybe all the people present on that network, to them we're sending. 

If we take an example of RIP: if I am using UDP here, then here buffer will not have any use. Otherwise, if I am using TCP, if one client is keeping a buffer of 1MB, so if there are 1,000 clients, space of 1,000MB will have to be maintained already, after that communication will start. Just for starting a communication, you have to do all of these kinds of formalities. 

After that comes Continuous Streaming. Continuous streaming means, as we have just talked, that we're talking about continuous speed—meaning my packets come in continuous speed. Wherever I talk about online streaming, like in Skype or YouTube, when you see a video on YouTube or you're doing a video chat on Skype, there you will never want that your video pauses or your streaming gets closed. What we always do is that streaming just continues, video keeps playing. Streaming continues a little forward of that and video behind it, so what we are saying in that is that packets stream continuously. UDP supports continuous streaming. 

And if we—like I will tell you a simple example—that in case you are playing a video on YouTube in HD mode, means if you are playing it in high definition mode, so why speed is low in high definition? Because in high definition, even if a single packet—what is in the packets? Pixels. Even if a single pixel drops due to any reason, so TCP will stop right there. It will re-transmit that particular pixel, after that video will play, meaning it will move. So that's why if it plays in HD, speed is low. Streaming reduces. But if we are playing without HD, we can say that by default we're using UDP, where we are not concerned about clarity; we want that just speed maintains, streaming goes fast. In that kind of application—meaning in Skype or YouTube—we will want to use UDP rather than TCP. 

Otherwise, what will happen in Skype also? You are doing a video chat, and all of a sudden a black screen comes, meaning the person you are chatting with just disappears. As this happens, you start worrying connection is broken or something else. But UDP takes care of all of that, that whether clarity is low, but your speed, your continuity is maintained by using UDP. 

And last is the small point: UDP is stateless. Stateless means there are two types of protocols: stateful and stateless. Stateful means if you are making a connection, saving the values of that connection, that is stateful. But stateless means if you are connected to thousands of clients, then you have to go stateless, because if you are connecting with so many people—if we take a simple example, HTTP. The HTTP protocols are stateless. What does stateless mean? You open and access thousands of web servers; HTTP does not store all that data. But if you talk about Amazon or Flipkart, Amazon and Flipkart will always be stateful. Which ever pages you have clicked on Amazon, whatever items you have selected, how much you have gone—meaning you have selected the item also, put it in the cart, checked it, color also checked it, size also, and at last when you came to payment, then you stopped there. So Amazon keeps a track on each and every thing, and Flipkart also. This means all of these applications work on stateful; in this, somewhere overhead increases. But UDP is stateless, means it maintains overhead as low as overhead can be, UDP. 

So this is all about various applications of UDP. Thank you!