Dear students, welcome to Gate Smashers. In this video, I am going to explain the IPv4 options and padding field. Let me tell you, all other fields of the IPv4 header I have explained very well with examples. If you have not checked those videos, in the description box and even in suggested videos, you will get the link. Quickly like this video and subscribe to the channel if you have not done it till now. If you have done it, you can get it subscribed from other devices. Subscribers are very necessary, so let's start.

See, options and padding is a value, as it is clear from the name itself. Options means that in some packet or some fragment we use it, and in some we don't use it. Its size is from 0 to 40 bytes. This means we know that the header of IPv4 has a minimum size of 20 bytes and a maximum of 60 bytes. If the header of IPv4 is of 20 bytes, then you can say that there are no options; there, the options will be of 0 bytes. Let's say if the header is of 60 bytes, then you can say options will be of 40 bytes. It's simple. 

Now, what is the next question here? Now we know that options are from 0 to 40 bytes, some use them and some don't use them. Why are they used? What is the reason, or what values does it have which we can use? That we will see in the next line. First of all, what do we have here in options and padding? The record route option. What is meant by record route option? It is a very simple thing. Let's say I have a source, and that source is connected through routers. The source is connected to the destination through a router. This is my destination, source S in the middle, and routers R1, R2, and R3. So what will happen? As a packet moves from source towards destination, in that packet, the source address will be there by default, and the destination address. As that packet reaches R1, what will happen inside it? Record route will happen. 

What this means is, record the route means from which router this packet went. From R1, the IP address of R1 will be recorded here; then it moved to R2, then the IP address of R2 will get recorded; then it moved to R3, so the IP address of R3 will be recorded. So this is actually the story of record route. But let me tell you, what you may be asked here? Here, I know that in IPv4, the only IP address I have is of 4 bytes. In IPv4, the IP address is of 32 bits, a logical address, so 32 bits you can also call as 4 bytes. 

So if one address is of 4 bytes, then you can calculate the options are of maximum 40 bytes. The options field of how many maximum bytes can it be? Of 40 bytes. And if one address is of 4 bytes, then how many routers or how many values of address can you store? You can say simply 40 divided by 4, that is 10. So you can say this 40 divided by 4 is 10. This means that if a maximum of 10 routers come in the way, you can record their address, not more than that. So if we look rule-wise, then we may have to use some more values here only to tell that we are using this value; to activate also we have to use some bits. Therefore, we can say we can't use the 10th, but we can store the addresses of nine routers in case that is asked. In this way, you can use that value. 

But what is the problem here? The source address you have—ISP, Internet Service Provider has given you an address—now when that packet is going on the way, there are various ISPs, different networks. So these options, everyone does not allow; not every network allows these things that the address of their routers gets stored, due to security reasons, you can say. So this option was there in IPv4, but now in IPv6, this option has been removed, maybe because of security reasons, you can say all the routers that are in the path, you are unnecessarily recording their IP addresses. Therefore, we are not using this option, but if you are asked, then you can say that this is the meaning of this option.

Then, if we talk about the next one, it is source routing. What is the meaning of source routing? Here, the source itself will tell that from where my packet will go. This means you have to go from Delhi to Mumbai, or you have to go from one place to another place. So you define it before, that my packet will first go to this city, then to this city, then to this city, then to this city. So that is called source routing. This means what will happen here? In the packets, the source will inform beforehand that my packet will go through R1. 

Ok, so let's say we make a diagram in this way, you make a diagram in this way, ok, then you will have more clarity. So in this way, there are multiple routers, ok, and in this way, they are connected to the destination. Now, let's say your packet came out. Now you can say that my packet should go through R1, then through R2, then go through R3, and then reach D, means after coming from S, go to R1, then go to R2, then R3, then go to destination. There are other routers also—R4, R5, R6—but the packet should not reach there. So you define first that my packet will go through R1, R2, and R3. So if the source defines this thing itself, this is source routing. 

But what is the problem here? This thing we cannot use as a user; a network admin can use this thing. As a user, we don't have this option. In case you are asked in any exams or competitive exams, then you can write the answer in this way. Within this, there are two types: strict and loose. What is meant by strict? Total path, complete path up to the destination, all the routers that are in the path, you define them beforehand, that the packet will move through this path. And what is the meaning of loose? You specify a few; you mention first go through R1, after R1 you had written to go through R3, means it can go to R2, or through here, or through here, means it can go through anywhere, meaning you have not defined the total exact route, you have defined a part of the route, so in the middle, the packet can follow a route on its own. So this is your loose source routing.

Now, finally, come to padding. What is the funda of padding? Let's say we always say that in IPv4, the size of the header, the length of the header, we always mention in scale of 4, that should always be in multiples of 4. But if in case the size of your header is not in multiples of 4—because it can be anything from 20 to 60—so let's say its size is 23 bytes, so 23 is not a multiple of 4. If you divide by 4, you will not get the exact value. So what would you do here? You add an extra 1 byte to it; you add eight extra zeroes. The size will become 24 bytes. So what is 24? A multiple of 4. So when you divide this, you will get the exact value. So what does it mean to say? The funda of padding is that if your header is not in a multiple of 4, then you make it so. Nothing more is here, and nothing more will be asked. Thank you.