Dear students, welcome to Gate Smashers. In this video, I am going to explain the difference between persistent and non-persistent HTTP. To explain this, I will give a real example and a numerical example to clear all points. 

Persistent HTTP is which we call as HTTP 1.1, and non-persistent HTTP is called HTTP 1. Right. Competitive exam, college university exam, interview, this type of question could be asked anywhere. I have already told about HTTP. HTTP, FTP, DNS, POP, I have already told about them. Link to that will be in the description box. Here, I will explain only persistent and non-persistent.

See what persistent means. Persistent means adamant. If we talk about a simple point, you send a request, you get a response, even after that connection remains established. Connection will not discontinue. Non-persistent means it is in a hurry. As one request and one response comes, it breaks the connection right away. 

Now, which connection am I talking about? Remember the first point: either HTTP is persistent or non-persistent, it uses TCP protocol. It uses Transmission Control Protocol because it wants reliability. It uses TCP. Either you talk about this or that, what do we make first in TCP? We make a connection. After making the connection, we transfer the data. Making connection is the first thing, here and here too. 

But what persistent means: one object came to you, you don't have to make connection again for the second one, you will get the second object, you will get the third one. In case of non-persistent, you got one object after making the connection, then make a connection again on another object, then again make a connection, then third object. This is how non-persistent works. 

Server leaves connection open after sending response, which I have already told you. It requires two RTT, meaning round-trip time. Now, what is round-trip time? Round-trip time is two into propagation delay. You left your house, after roaming you came back to home again. The time taken is RTT. And what is propagation time? That you left your house, reached the destination. That is propagation delay. We are talking about round-trip, meaning source to destination, destination to source again. Meaning you can say a cycle is called one RTT. Remember this point.

If we talk about in persistent case, how will it work? In persistent's case, let's say this is client, this is server. What will client form first? It will form a connection. To form a connection, it sent a request, a response came. One RTT is used to make connection. This thing works as it is here. Meaning you will form a connection here, one round-trip time will be spent here. After that, what will happen? What is in persistent case, and what is in non-persistent case? 

Let's say from client, from client machine, you access the webpage, because we use HTTP to access the webpage. Now what will happen? Let's say to access the webpage you formed a connection. What will you do after forming a connection? As you form the connection, first the HTML text file, the base file, which is the base of the webpage, will actually get transferred. So obviously, first you formed a connection. After forming the connection, you said that I need the base file. You will get that base file. One extra RTT will be used for the base file, which we also call http/txt or .txt file. What actually happens to maintain cookies or to maintain web cache? It helps in that. It will occur in both the cases. In this and in this it will happen. Your basic webpage, the basic webpage will take one RTT to come. Getting the point? 

So the story is same in both: base file comes, here also base file comes, same is happening. Now let's say there are two images in the base file that I want to access. Meaning, in that base file, in that webpage, there are two objects further that I want to access. So see what will happen here, what will happen in case of persistent. 

Your connection is established. You have received the base file. Now what you will do? One object, two object, three object, any number of objects. Two files, paragraph, video, whatever, that will take one more RTT, and all objects, meaning all the objects that it has, two images, three images, you will get all of them. This is how this story happens. 

But what happens in non-persistent case? You get the base file, you get the HTTP, txt file. Now after that, connection has to be established again. One RTT will be spent. You will establish the connection again. Now image number one, because you accessed one image, so you get one image, and it will take one RTT. I am not counting transmission time. You can count the transmission time because transmission time is message size divide by bandwidth. So obviously you can add the transmission time in this, but I am explaining from the round-trip time point of view. So you will get image one through this way, the first image. 

Now what will happen? You have to access another image, another object. So again connection has to be made. Again one RTT will be used, then you will get the second image. Getting the point or not? So you must be understanding the complete story. You must be able to visualize what is the difference between persistent and non-persistent. 

So this is the actual difference. One request, one response here. One request, one response came. Again form a connection. One request, response came. Again form a connection. But here is nothing like that. That's why by default, obviously you could also tell, overhead will obviously be less in this case. Here overhead will be more. 

And anyways if we talk about, either you talk about Internet Explorer, seven, or Google Chrome, talk about Firefox, Opera, all these use persistent. Although we can use the concept of parallel connections, this is the basic difference. 

So what happens in this? In persistent case, we don't [wait] for the long time, not forever. If we talk about Firefox, in that 120 or 125 second idle time is present. If you are idle for that duration, connection may get terminated. These are some extra points that you can note. Maybe it could be asked in interviews. So this one provides 20 percent more performance as compared to this because connection has to be formed again and again. So this is the main difference between persistent and non-persistent HTTP. Thank you.