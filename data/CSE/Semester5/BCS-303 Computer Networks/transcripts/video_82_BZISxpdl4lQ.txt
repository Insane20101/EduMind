Hello friends, welcome to Gate Smashers. In the last video, we discussed how DNS actually works. And if we talk about DNS, what are the different levels of servers in DNS? So here we are talking about the last point that I told in the last video: How is a DNS query solved? Either by the recursive method or the iterative method. 

Meaning, it is a very simple concept. If we first talk about recursive, recursive means that the machine—if we call it host or machine—as soon as I turned on the browser in the machine, as soon as the browser turned on, what did I write? In the last video, I took the example of cricinfo, so I am taking the example of cricinfo. What did I type? I wrote HTTP://www.cricinfo.com. So cricinfo is a domain name. As we search the domain name, we need its corresponding IP. 

First comes step one. Step one is that we will search the IP of cricinfo in the local cache, or we call it a DNS resolver. Here I will tell you the most important point, which is that here we use DNS caching. DNS caching means that your request does not go to the root servers again and again, because it will take a lot of time. So what happens is your ISP does local cache. The most used, most popular ones it keeps there. 

As we search cricinfo, if I got a corresponding IP there, I will revert back the IP, and my query will be solved. But it is a simple case; you can call it the best case. But we are talking about what happens if I open a site for the first time. For the first time, the DNS will not be in the cache. We have to properly find it to bring it into the cache, starting with the root server. In that, we use recursive or iterative mode. 

If we talk about recursive, how does recursive work? First, we searched in the local cache. It was not in the local cache. Then my proper searching will start. You can also call it a DNS resolver; the resolver will resolve the query. What will be the second step? The request will go to the root server. The root server will find that you want this particular cricinfo. It is dot com. Dot com means commercial. After that, it will go to the top level domain server for the generic names. Your request will go there. Then after the request is sent there, that request will be forwarded to authoritative servers, which I told you in the last video, meaning that the actual IP addresses are available on authoritative servers. The request will go to the authoritative server. That will be decided by the top level servers. The request will go on the authoritative server. You got the IP address from the authoritative server. So here the IP address is received, but it has not reached you yet. 

In the fifth step, it will give it back to the root server. The root server will respond back to me. It will respond, which is step number seven. And finally, in step number eight, what happens? I will receive the IP address of cricinfo. Now I have it. Now what I will do, or what my machine will do? What does that particular IP address mean? An IP network has a network ID and a host ID. The network at which we want to find it, we will find it at that network. That network would be somewhere else. We will go to that network and search the data. So this is how the recursive mode works. 

If we talk about iterative mode, how does the iterative mode operate? In iterative mode, the concept changes a little. If we talk about iterative mode, the first request is the same for the local cache. If it is found, then that's best. Otherwise, I will send a request to the root server. The request will reach the root server. Who will the root server find now? The root server will say to go to this commercial address, this top level domain server. But what is its address? Because this server is also a host somewhere. Each server is in a network or in a host. What is its IP address? It will send it to me. Meaning it will give it to the local cache or DNS resolver here. 

Now the DNS resolver—here is step number three—now the DNS resolver will go there to that particular address. And after going to that particular address, this network will say, "Do you want cricinfo's?" Then it is on cricinfo's authoritative server, and this is its address. Whatever the address is with me now, then my request will go to it directly on the authoritative server. 

Now what will the authoritative server say? Now you are looking for this IP. You want this IP. This is the IP address. And then finally, the IP address will be given back. So this is how the actual thing works. Both have the same steps, but here we reduced the responsibility of the root server. As soon as the request went, the work of the root server is over. But in recursive, the root server should be present until the end, because the responsibility of the root server is to handle the request again. So you can compare both on this basis: What is the basic difference between both? 

Now, the most important point related to DNS: If we talk about DNS, the question is asked many times, does DNS use the TCP protocol or the UDP protocol? DNS uses the UDP protocol because the UDP protocol is fast. TCP first works on three-way handshaking. I have already made a video on this about where UDP is used. In that, I have explained why DNS is actually used, because we want a fast reply here. My main motive is to search data on cricinfo, to watch the result of a match; it is not my main motive to handle the backend routing. This is happening in the backend. So I want the work happening in the backend to be very fast, which cannot be achieved by TCP because it works on three-way handshaking. UDP will work faster. That's why UDP is actually used in DNS. 

So this is the basics of recursive and iterative modes. This is all about DNS. Thank you.