Resource Allocation Graph. Resource Allocation Graph is the most efficient and convenient way to represent the state of the system. Means in our system how the resources are allocated to the processes and how the processes have been assigned multiple resources. To represent that we use the Resource Allocation Graph that is also known as RAG. Means if in our system whether there is a deadlock or not, to represent that it is the most suitable method. That is the RAG, that is Resource Allocation Graph. 

Like a normal graph, it also has two things, one is a vertex, one is an edge. Means every graph has two things, edge and vertices. So in the vertices, there are 2 types of vertices Process Vertex and Resource Vertex, means whatever the number of processes in our system, means whatever number of processes which are running in our system, all the processes are represented with the vertex and they are generally represented with a circle. And Resource, means whatever the resources like CPU, register, memory, monitor, printer, whatever the resources we have in our system like in our laptop, mobile or tab. So all the resources are represented again with the vertex but we represent in a rectangle. 

And resources are of two types, one is a Single Instance, one is a Multiple Instance. Means in our system there can be a resource which is only 1, means that is present only single, like a CPU, monitor. In our system CPU, monitor, generally nowadays in our systems we have the dual core, quad core systems, but if we talk generally then we can say that in our system we have 1 CPU, 1 monitor, kind of. But some of the resources are which is multiply present. Means they are in multiple numbers that is, we can say, Register. And we can have more than 1 printer which is connected with our system so that can be a multiple instance. You don't need to cram it, this is a normal thing, like multiple and single instance resources. 

And Edges, what they represent? Edges represent 2 things, one is a Assigned Edge, one is a Request Edge. Assigned edge means like we are having a resource, resource is connected with the process. And if there is an edge which is moving towards the process, if there is an edge which is moving towards the process, it means that resource is assigned to a process. Means, that process has already taken that resource. And request edge, how we represent it? If the arrow is going towards the resource, means from the process it is going towards the resource, R means resource, P means process, then we call it a request edge, means here P is requesting for a resource and here P has already taken a resource R. This is how we represent in a normal RAG. 

Let's solve one example, like we are having 2 processes P1 and P2, we represent the processes in the circle and we represent the resources in the rectangle, both are vertex, but we are having the single instance. This example is of single instance, means we are taking resource of single instance. Means only one instance is there means one instance of R1 and we are having only one instance of R2. We can say like R1 is a CPU and R2 can be a monitor, that is the simple meaning. 

So P1 here, what it represents? It represents P1 has taken, means assigned, P1 has already taken R1, but it is requesting, request for the R2, because edge is moving towards the resource, it means it is requesting. And here P2 has taken R2 because edge is moving from the resource towards the process, it means R2 has already been assigned to P2 and there is one more edge from P2 to R1, it means P2 needs R1, P2 requests for R1. This is how we represent the RAG, normal RAG. But here we have already assumed that this is a single instance. Means only one R1 instance is there, only one R2 instance is there. 

Now what is the advantage of this? Means why we represent like that? The main reason is we have to check whether in this system there is deadlock or not. This is the mostly asked question in the GATE and other competitive exams where we will be given a RAG and by using that RAG, by looking at that RAG you have to tell whether there deadlock exists or not. That is only the question which is asked most of the time. 

So let's examine this one first. Let's say that we have one R1 and R1 is taken by P1, P1 is requesting for R2, R2 is taken by P2 and P2 is requesting for R1. This is what? A kind of circular wait. And circular wait is one of the necessary conditions for the deadlock, means in your system there must be a circular wait if you want a deadlock in your system. So yes, there is a circular wait, there is a circle, circle means like a loop, means if we start our journey from R1, can we come back to R1? Yes, we can come back to R1. That is called a cycle. 

So can we say that if there is a cycle in the RAG then there will be a deadlock? Can we say that if there is a cycle in the RAG, then there will be a deadlock? So let's examine this, here we can solve it normally also, by looking at the graph we can easily say there is only one resource of R1, only one instance of R1 which is taken by P1. But P1 needs one more resource that is R2. 

So can we say that P1 will get executed? No. Why P1 will not get executed? Because P1 says I am having R1 but I need R2 to get executed. P1 has taken one resource and waiting for another resource so hold and wait is also there. Here is hold and wait, it is holding R1 waiting for R2. And at the same time P2 has taken R2 and waiting for R1. P2 is saying, can I say that P2 will get executed? No, P2 will also not get executed because P2 has taken R2 but to get executed P2 needs R1. But how P2 will get R1? No, because R1 is already taken by P1 and how P1 will get R2? No, because R2 is already taken by P2. 

So means both the processes are waiting on happening of some event, what is the event? P1 is wishing that if I get R2, I will get executed but his wish will not get successful because R2 is already taken by P2. And P2 is wishing that if I get R1, I will get executed, but how will he get R1? Because R1 is already taken by P1. So both are waiting for 1 resource and who will fulfil that request? Nobody will fulfil. So that will be a deadlock situation, because nobody is going to fulfil their request. So that is called a Deadlock situation. 

So it is a simple graph so we can easily say that there is a deadlock. But there is another method which is definitely giving you the guarantee whether there is a deadlock or not. And you should follow that method because if the graph will become complex, we will see some more examples. If the graph will become complex, then it will be very difficult to analyse manually, by looking at the graph. 

So what is another method? Let's examine that, so let's say how many processes we have? 2. In this diagram, how many processes we have? 2. P1, P2. Let's make the diagram. Allocate and request. Circular wait is there. Let's move this so you can clearly get the diagram. So 2 processes are there, P1 and P2. And you just write allocate and request, what is the allocate and request? Resources. 

So let's say, how many resources we have? 2. R1 and R2. So by looking at the graph, allocate, allocate means already holding that, already getting it, so which resource is P1 holding, already allocated? R1. Means P1 is already having the R1 and P2 is already having the R2 because P2 has already taken the R2. So just write 1, 1. 1, 1 means P1 has taken R1, P2 has taken R2. So whatever the allocation is there we write 1. 

Then we write the request, request for the same resource that is R1 and R2. So P1 is requesting for R2, you can clearly see P1 is requesting for R2, so P1 is requesting for R2, so in R2 we can write 1. P1 is not requesting for R1, so we can write 0. P2 is requesting for R1, so just write 1, 1, 1, and P2 is not requesting for R2 because it is already holding that, so just write 0 in that. 

Now after filling this information now you need to check the availability. This is same like the bankers algorithm, I'm just applying the bankers algorithm, for more details you can just refer to my video bankers algorithm, so you will get more knowledge, otherwise, you can follow that also. 

So availability, availability means how many we have currently available, currently how many I have available, so R1, is there any resource of R1 available? No, only 1 was there which is taken by P1. So 0. R2, do I have any resource of R2? No, so it is already 0. So means I have R1 and R2 both are 0, 0. currently I am having R1 0, R2 0. 

Now by looking at this availability you need to check if can I fulfil the request? can I fulfil the request of P1 or P2. What is the request of P1? 0, 1, means 0 resource of R1 needed but 1 resource of R2. Can you give? No, we can't give because we have 0, 0 available. We don't have even a single resource of R1 and R2, both are nil. So currently availability is 0, means I am not having any resource with me, then how I can fulfil the request? I cannot fulfil the request of P1. 

Can I fulfil the request of P2? No, I cannot fulfil the request of P2 because P2 needs 1 resource of R1, do I have? No. So means neither I can fulfil the request of P1 nor I can fulfil the request of P2 because my current availability is 0. So in that situation you can clearly say there is a deadlock situation. So as we have already calculated but now this is a kind of fool proof method which will definitely give the right answer in all the cases. So you can clearly say there is a deadlock in this system. So in this RAG there is a deadlock. 

So this is how we represent, but again, this example is of single instance. So let's solve one more. Let's solve the second question. I'm just making the diagram over there, this, table. Let's say how many processes we have? P1, P2, P3. Again write the allocation and request. Visible? Allocation and request. 

So here how many resources I have? R1 and R2. R1, R2. R1, R2. This is the tag means we are now examining this RAG. So just write down all the processes at this time, all the processes, write here. And allocate and request, and all the resources, how many resources? R1, R2. So R1, R2, R1, R2. 

Now allocate P1, let's say P1 P1 has allocated any resource? P1, check, yes, P1 already has R1. 1 instance of R1 is taken by P1 because if the edge is going towards the process it means it has that resource, so means that resource is already present with this process. So P1 is having R1. Any other resource? Whether P1 is having R2? No, so just write 0. 

Check P2, P2 is having which resource? P2 is having R2 because edge is moving towards the process, it means R2 is already taken by P2, so R2 is already taken by P2, so just write 1 and R1 is not taken by P2, so just write 0. 

Check P3 has taken any resource? P3, No. There is no edge moving towards the process so neither P3 has taken R1 nor R2. Any instance is not taken by P3. 

Now check the request, request of P1, whether P1 is requesting? No. P1 is not requesting for any resource. If it is not requesting simply write 0, 0. 

Check for P2, whether P2 is requesting for any resource? No, P2 is also not requesting any of the resource, because there is no edge moving from P1 towards any resource. None that is moving towards P2. 

So P3, check whether P3 is requesting? Yes, here P3 is requesting 1 resource of R1, 1 of R2. See this one. P3 is requesting that 1 of R1, so means we have to write 1. 

Now check the availability. Check the availability. Check the availability. Do I have any resource of R1 available? No. Because only 1 instance which is already taken. Do I have any resource of R2? No. Only 1 instance already taken by some process. So how many available? 0. 

But by using that availability can I fulfil the request? Yes. P1. P1 is requesting nothing, so if it is not requesting anything, it will get executed. Because P1 is having R1 and P1 is not saying that I need something, if it is not saying I need something, it will simply execute and when it gets executed, it will terminate, we call it terminate. 

So when the termination is done then what will happen? All the resources will be taken back. So when P1 will get terminated, so we write here P1 will get terminated so whatever the resources of P1, they will be taken back. So 1, 0. Means 1, 0. Now the new availability, new availability will be 1, 0. Means 1 of R1 I have now and 0 of R2 I have. This is what the new availability, here. 

Now see, can I fulfil the request of P3 now? No, still it is not possible because I can give 1 resource of R1 to P3 but P3 needs 1 resource of R2 also, but I don't have any. 

But can I fulfil the request of P2? Yes, P2 needs nothing. P2 is having 1 resource R2 and it is not demanding anything. If it is not demanding, simply terminate. It will get terminated because it will get successfully executed. So when it gets terminated, I'm just writing here, if it gets terminated means whatever the resources it is having they will be taken back. so when it is taken back then we can simply say, what is the new availability? 1, 1. Because whatever the resources which are taken by P2 they will be taken back and it will be added to the current availability. So what was the availability? 1, 0. Added to that I am having 1, 0. What is the P2 allocation? 0, 1. So when I add this, so it becomes 1, 1. 

So what is the current availability now? 1, 1. And how many resources I have? Both are having 1, 1 resources. And how many processes? P1 already terminated, P2 already terminated. 

Now check for P3. P3 need is 1, 1. Can I fulfil? Yes. I am having 1, 1. I can easily fulfil the request of P3 because P3 needs 1 instance of R1 and 1 instance of R2 and I am having 1 instance of R1 and R2 both. So my availability is there, I will give that to P3. So P3 will get successfully terminated and it will move out from the system. So this is how we can execute all the processes. 

Now in this case, in this diagram, can we say there is a deadlock? No. There is no deadlock. Deadlock means all are waiting for happening of some event but event is not happening. But here the event is happening. P1 is not waiting permanently, like infinitely. 

There is a difference between the 2 terms, like we say waiting, we are simply saying that a process is waiting. A process is waiting for resource. If the waiting is finite, if the waiting is finite means I know that a process will wait for that particular amount of time. The time can be 1 minute, 1 hour, 1 day, 1 year, 10 years, 100 years, even if it is 1000 years, still it is finite. So that is called starvation. 

And if the waiting is infinite, means I don't know, I don't know what is the amount of time a process will wait to get executed. That is infinite. So that is called the deadlock. this is a very important part.

If it is finite, we call it starvation. In finite, we can simply say if anybody says a process is waiting for this many years, no problem, this is still a finite value because we know that after this amount of time the process will definitely terminate. But infinite means totally... 

So this is the case of starvation, and this is the case of deadlock. In this case, there is no starvation; there is a deadlock because P1 and P2 will be waiting infinitely. But here in this case, P1 is not waiting. P1 will simply get executed because it is demanding nothing. P2 will simply get executed because it is demanding nothing. But P3 will wait for some time because P3 needs 1, 1. When will it get 1, 1? After P1 and P2 get executed. P1 and P2 will get executed after some time, and that amount of time is finite. So P3 knows that after some amount of time, I will definitely get executed. Therefore, this is finite waiting, and finite waiting is always starvation. 

Here the question will be asked simply whether it contains the deadlock or not. There is no deadlock. So this is how we can examine the questions on RAG. 

One last point here: we have seen circular wait, meaning there is a cycle. Is there a cycle? If I start my journey from P1, can I come back to P1? No, I can't even move anywhere. Let's start from P3, can I come back to P3? Let's say P3 to R1, R1 to P1; there is no way I can come back to P3. So I can say it is acyclic, meaning there is no cycle in this. If there is no circular wait, then there is no deadlock. But if there is a circular wait, then there is a deadlock. 

So if there is a circular wait in RAG, if RAG has a circular wait, meaning a cycle, then always there will be a deadlock. If RAG has a circular wait, that is a cycle, there will always be a deadlock. This is true, but only in the case if there is a Single Instance Resource. That is a very important point: if there is a Single Instance Resource, meaning every resource is of single instance, and there is a circular wait, meaning there is a cycle, then there will always be a deadlock. Yes. 

And if there is a single instance and there is no circular wait, there will be no deadlock. Yes, meaning its vice versa is also true. But only for a single instance, meaning just for a single instance—I'm talking about single instance only—in the single instance, if RAG has a cycle, then there will always be a deadlock. True. And if RAG has no cycle, then no deadlock. Yes, this is also true. 

So both statements are true, but only in the case of a single instance, because till now I haven't discussed multiple instances. Multiple instances we will be talking about in the next video. So in the single instance, if there is RAG and it contains a cycle, like this, there will always be a deadlock. And if the RAG is not containing any cycle, but in a single instance, then there will be no deadlock. That is also true. So these two points are very important in this. 

This is all about the RAG with the single instance. If you like the video, please share it with your friends and colleagues, and please subscribe to my channel. Thank you.