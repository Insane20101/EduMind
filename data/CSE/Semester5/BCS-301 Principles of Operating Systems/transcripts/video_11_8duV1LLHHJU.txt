Hello friends. Welcome to Gate Smashers. In this video, we are going to discuss user mode vs kernel mode in operating systems. 

So what does user mode mean? As the name says, user mode. Here we are talking about users. We turn on our laptops and PCs, and we use some applications there. Whenever we are using some application, then by default, we are in the user mode. But all core functionalities of the operating system, all major drivers, all work and execute in the kernel mode. And the processor keeps switching between user mode and kernel mode. 

Now let me tell you with a very simple example. Let's say if I'm using a Linux operating system. And in Linux operating system, I opened a text editor. A text editor is an application where you can write programs and codes. So I opened a text editor, meaning I'm using an API and I'm writing a C program through it. 

Now when I wrote that program, let's say it is written in that program that you have to read a file and write some data into a file. Meaning I have to read some file and update some value in it. Here you have to focus that this file is in the hard disk, and hard disk is a hardware component of our system. And all the hardware are in control of the kernel, the operating system. So user can't access the hardware directly; it needs the kernel. 

When I started executing the program, I wrote user process executing. User process means that C program is executing, and now I have to read some file in that program. Reading that file means now I have to use a system call here. A system call is a way to access the kernel; meaning we have to access hardware through the kernel. A system call is a way to access it. So which system call will get used here by default? Read system call. 

Read system call means that I have to read data from some particular file. When that system call gets invoked—which system call? Read()—there, my system will go into which mode? It will go into kernel mode. And kernel mode's bit is zero. Meaning first we were in Mode 1. The moment we came into the read system call from Mode 1, there generates an interrupt or a trap. A trap will generate. Trap means you got shifted from another mode to kernel mode. The moment you got shifted into this mode, your mode bit changed from 1 to 0. 

Now what you should do is execute the system call. Meaning you are reading the data. Whatever file you wanted to read in the hard disk, you'll read the data from the hard disk. And after reading the data, you'll use a write system call there. You read the data and wrote it also. Now you'll again go back to your user mode. Because we as users, we are just working in the user mode, so I have to come back from the system call to our user mode. So here, again, the mode bit will change from 0 to 1. 

If I would relate it with a real-life example, let's say if I go to some bank. Let's take a bank; a bank is acting as a computer. If I want to withdraw money from the bank, then I'm in user mode, so I went to the bank and worked in user mode. But those clerks and POs sitting behind the window, they can work in kernel mode. Can you do this, like you went to the bank and directly went near their window and took the money from the back side? No, this is never going to happen. Because you are in user mode and it's kernel mode; you can't access it directly. 

If you'll go to the bank and will just take out the money from the drawer, then there'll be no function of kernel mode; you are accessing the hardware directly. So this can create a very big problem. You are overwriting directly and taking all hardware into your control. And this is the major responsibility of the operating system: to manage the resources and to govern the resources. So you would fill the form and give it to them through the little window, and they'll give you the money. So that mode where you are going from 1 to 0, and when you got the money then you are going from 0 to 1. So you got the money back, meaning your work is done and the process is executed, then you can leave from there. 

We are using the same logic here in the system also. Whatever application we are using by default—whether we are listening to songs or watching a video, whether you are working in Microsoft Office, no matter what application you are using—that application works in user mode by default, and your processor keeps switching between both of these modes. 

When in some process, if you want to write a C program where you have to do 2+2 = 4, you don't need the kernel to do 2+2 = 4. But if you want that output 4 to get printed on the monitor, then you write `printf`. Then what is `printf`? `printf` means you want to print some output on the monitor. So monitor is in whose hands? In the operating system's hands. Then a system call will get generated there, and that system call will print the value on the monitor. 

This way, our system keeps changing between user mode and kernel mode. It's called dual mode, so it's very important, otherwise, there'll be so much problem. You are accessing the hardware and system, so we are working in user mode. The operating system works in kernel mode, and switching keeps happening here. 

Although this is a theoretical topic, many times theory questions come from it in competitive exams. And after this, we'll discuss about threads, so in threads also the concept that we use. This is all about the difference between user mode and kernel mode.