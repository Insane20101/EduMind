Dear students, welcome to Gate Smashers. I am going to solve in this video a question on TLB. I have already made a video on TLB: what is TLB, and how does it work? Even inside of that, I explained 1 or 2 examples, but this is a question from GATE. Many student requests were coming to explain this question. The question seems to be of a bit tough level, but in actual it is not, so I will explain this question to you in a very easy way, and all the TLB questions related to this—you can understand how they will be solved. 

Please like the video immediately and subscribe to the channel if you haven't yet. And even if you do, you can subscribe from other devices; subscribers are very important! So let's start. 

First of all, whenever such a question comes, read the statement carefully. And after reading that statement, make a diagram first. Even if you make a rough sketch, it is very important to make a diagram. 

Consider a paging system uses a 1-level page table. So, 1st line: it turns out that the page table is a single-level page table. If a two-level can also be given, a 3-level can also be given; here it is 1 level. And my page table inside it is of main memory and TLB. Then, the main memory access time is given. TLB lookup takes this. Each page transfer takes this time to/from disk. Meaning, if a page fault collision occurs, then what do we do? Get the page from disk, and then we access the page. TLB hit ratio—ok. The page fault rate is indicated. Assume that for 20% of total page faults, a dirty page has to be written back to the disk before the required page is read. 

This is an extra point being mentioned: that if I have to bring the page from disk to main memory, 20% of the cases are like this! Inside which the dirty page—meaning I have to send a page already in main memory to disk, and then I have to load the page as it is. Meaning space will have to be freed inside main memory! And how do you empty it? The existing page, you will make it run away from there: "You go to the disc so that I can bring any other page from disk." So that is your simple concept of virtual memory which we are using here. It is very important to draw the diagram first.

So I have a CPU. What will the CPU do first? It will do the lookup in TLB. So it will look into TLB. If inside TLB I got a reference—meaning I got a hit—what shall we do? We will pick up that page from main memory; TLB will give just the reference. If I got a hit, I will take that page out of main memory. Ok, the story ends here. When is it? While TLB hits.

If let's say TLB missed—what if TLB misses? Meaning I didn't get a reference from TLB. Then what shall we do? We have been given a page table (single level); there is a single-level page table, so what will I do? Go to the page table, single level. If multi-level had two levels, we would have made two page tables. Simple! There is one level here, so we will go to that page table. If I got a physical address from that table, that it is lying here in the main memory, then what shall we do? We will call it a hit: I got it in the page table, not found in TLB, but got a reference in the page table. So I go to that memory location, and I'll pick up the page.

But here comes a worst case: miss one. Not even in the page table—meaning if its reference is not found in the page table, so that means it is not in main memory at all! We call it a page fault! If a page fault occurs, so we use page-fault service time. And page fault service has been told many times: what happens? We from disk—from where? From disk—will bring the page, which is secondary storage memory. From secondary memory, we from inside the disk will bring the page. This is called a page fault!

But even here are two cases of page faults. Which are those 2 cases? One case is telling that's ok: you did not find the page, page fault collision done. So you go to disk and pick up that page from disk and put it in main memory. You will get it. 

But there is another 20% of the cases. This is fine in 80% of the cases: you'll pick up the page from the disk, get it. But what is in 20% of the cases? Like that, you will even pick up the page from the disk, so will not be able to put it in main memory because main memory is full. Some dirty page from here—meaning a page that has been updated—after getting it out of here, we will send it to the disk first. Then, by removing it from the disk, we'll bring the page here. This case, which is 20%, that's the whole story. You get clear from this diagram.

Now just enter the values. Watch: first, TLB hit one! TLB hit ratio is also given as 95%. So 0.95 I wrote here. What does 95% mean? Obviously 0.95. What shall we do in this case? We will see in TLB first. Watching time in TLB is 20, and then from there we go straight to main memory, will pick up the page. It's over! This is your TLB hit!

If not found in TLB—meaning this one, missing case—so on the miss case side, that's 0.05. Ok, isn't it? 0.05. Because 95 is the TLB hit, so 0.95. Take note of this: sometimes students write 0.5 by mistake, but 0.05 will be your miss ratio, miss of TLB! 

And if we miss, what will we do? We will go to the page table. But before going to the page table, we looked at TLB. That's when I found out that man, we missed in TLB! In TLB we saw earlier, we have spent 20 nanoseconds. Then it turned out that it was not in TLB. Then we will go to the page table. Where is the page table existing? In main memory. Already given. Neither has been given, so let's assume that the page table is inside your main memory. That is, how much time does it take to access the main memory? Same as the page table. So it will take a hundred times to access my page table. 

And here I got it—meaning I got a hit here. So if it got hit here, how much time will it take? Mine will take a hundred more times. So this is the time. What time it is! How many times is this happening? It has been given to us: what is the page fault rate? 10%. What is the page fault rate? 10%. So that means a page fault will happen when the page is not found in the table. But what is the case here? That I saw in TLB, not found in TLB, so I went to the page table. There I found that reference! So I took it from main memory. That is, the page fault collision did not happen. So if the page fault rate is 10%, so reverse the page fault rate! Means a page fault is not happening! So how much will it be? 90%. So that means you multiply it by 90%. 

So this is yours: it became an equation. You can multiply first; it's the same thing. So what is the 0.90% case? It becomes this one. Comes back to us in the last: case with 0.10, case with 0.10. 

Now which one is the case with 0.10? If a page fault collision occurs. Now page fault collision occurred, so how many? 10%, i.e., 0.10. Or you can write 0.1 as well. In 0.10 we have two cases made: one with a dirty page, and the one without a dirty page. Ok? Meaning even the story would have been understood. What does the case with 0.10 say? 

If I talk about without a dirty page, then what will happen? The first thing you'll look for in TLB? Not found. Then what will happen to you after that case? Not found in TLB, then you will go to the page table. You didn't find it there either. Then I came to know that this dirty page is here. What will you do after that? You will bring that page from disk, will put in main memory. So from the disk, the time taken to fetch the page and put it in the main memory—that has been given to you here, that is 5000 ns. Each page transfer takes 5000 ns to/from disk. So what is the time taken to fetch it from disk? 5000. So 5000 times your this will be added. But remember, it did 0.10 page fault! 

Now which case is this page fault? There is a case without one, without a dirty page. Ok, isn't it? Without a dirty page. And how much is that of yours? That is 80%. How much is that? 80%. So the rest are the remaining 20%. 

Which one? What do you have to do in the rest 20 case? The dirty page must first be removed from the main memory and sent to disk. Send it back before here! Then bring the second page from there. So what's the difference? First you searched in TLB: not found! Then you find it in the page table: not found! Because it is a dirty page, then you will also bring it from disk. So there, that is saying that there are 20% such cases. There is no place, so that means first you have to send it out from the main memory to the disk, in which 5000 extra will be taken separately. And then after that, the new page from disk will have to be brought to the main memory, so it will take 5000 separately. So this is the only difference between these two cases. 

Ok, so that's the whole story of what it is. This bracket closes here, and the big bracket closes here. So this bracket is that, remembering that in the proper way it has over here. And this is to multiply 0.05 with the whole. So that's that 0.10: it will multiply with this whole. Meaning write it in a proper way. This turned out to be your case with a dirty page. 

So all the cases which are there have been explained to you in a proper way. When will you solve this whole? So it will come to 154-something, which you can round off and write as 155 ns. So the whole story of this whole thing works in this way of your TLB. Thank you!