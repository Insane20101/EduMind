Hello friends, welcome to Gate Smashers. In this video, I am going to explain about TLB, that is, translation lookaside buffer. In this video, we are going to discuss all the points related to TLB, which will be beneficial for your competitive exams as well as in college and university-level exams.

First of all, I want to explain to you what TLB is and why do we use it. The story starts from paging. We have already discussed the concept of paging. Let me give you a small overview. What do we do? We divide a process into pages. Inside the main memory, which is already divided into frames, we bring different pages of different processes into the main memory. What is there in the main memory? Frames. Now, the page table, what is a page table exactly? It is like an index, just as there is an index in front of a book. The page table tells me which page number is lying at which frame address, that is called a physical address. I have page table entries numbered 1, 2, 3, 4, in this way, and the page table tells me that page number 1 is lying at which frame—it is at frame 1, frame 2, or at frame 3. This we have already discussed under paging.

Now we talk about the CPU. The CPU generates a logical address, and what happens in a logical address? Page number and page offset. As I already mentioned, what do we do? We divide the process into pages. So, see what happens: page number and page offset, which we are calling the logical address. Now, with whom have we mapped it? We have done its mapping with the main memory, meaning we have brought multiple pages inside RAM. Now, how do I know which page is lying in which frame? Who will tell this? The physical address. From the physical address, we will know which page number is lying in which particular frame. We do mapping. This is all about paging.

But what is the problem here? Where is my page lying? In the main memory. And the page table from where I am checking the index, where is it lying? That is also lying in the main memory. Now, let's say if the time taken to access the main memory is $x$. If the time taken to access the main memory is $x$, then how many times am I accessing it if no page fault is occurring? Let's say the page which I am searching is lying in the main memory. So how will I search it? First, I will go to the page table. From the page table, I will know the frame number. Corresponding to the frame number, I will go to the main memory, and then I will pick that particular page number. So this is the story. The CPU here generates a logical address. Corresponding to that, how shall I go? I would go into the page table first. From the page table, I will know the frame number, and going into that frame in the main memory, I'll pick up that page. 

Now, looking at the whole story, how often am I accessing the main memory? One thing happened: when you go to the main memory, you will take time to pick that page. Let's say the time taken is $x$. And what are we doing for the second time? First of all, we are also looking at the page table. Where is the page table located? Where would the page table lie? Obviously, the page table is also lying inside the memory. So that means I am putting an $x$ amount of time to find it in the first page table, meaning the index next to the book will find it in the first index. What is the seek time in the index? $x$, because the index and the page table—that too—is lying in the main memory. And what is the time taken to access the main memory? $x$. And if the time to access the main memory is $x$, then first I am accessing the page table. To access the page table, an $x$ amount is used. What did I get from there? I got the frame number. And then I went inside the main memory again, and from that frame, I picked up that page. That is, how much did my total time take? $x + x = 2x$. Meaning, I have to access the memory twice, and then I will get the page. This story is simple.

Now, what is the problem with this story? When we are accessing the main memory twice, it means if the page table is simple, then we are accessing it twice. If the page table is too big, and we have also divided the page table into multiple levels—we divided a page table into multiple pages, divided into multiple levels—then where would all those multilevel pages be lying? That too must be lying inside the main memory. That is, in the ideal condition, you can say that it will take $2x$. But if you are using a page table on multi-level, if your page table is also in multiple levels, then my time will increase as much. Because where will all the levels lie? They must be lying inside the main memory. So what is the time to access from the main memory, although I am taking some time? We want to reduce this time. 

Now, to reduce this time, what do we want? We want faster memory. And which is that faster memory? Cache memory. Meaning, now we are using the concept of cache memory, or whatever you can call TLB, that is, translation lookaside buffer. Sometimes we also call cache as buffer. That means the buffer we are using, this is TLB. 

Now, what is the concept of TLB? The TLB is faster than RAM. This means it is faster than RAM. So what shall we do now? We will put the page table entries in the TLB. What we did in TLB was put the entries of the page table, meaning the frame we are looking for—where did we put the frame numbers? We put the frame numbers in TLB, meaning kept inside the cache. 

Now, what is the benefit of that? See the advantage. The CPU generated the logical address, meaning the CPU is looking for a page. Where is that page now in the main memory? I don't know, I want its frame number. I'll find it in TLB first. Now, what happens in TLB? I have all these tags in TLB. What are we doing now? We are searching in which particular tag of, in which particular location, the corresponding frame number is lying. The time to access TLB now is comparatively less than RAM, because TLB is cache memory, which is much faster than the RAM. 

What did we do? First, find the TLB. In TLB, if I got it—the frame we're looking for, the page we're looking for, corresponding to that page, which is the frame number—if I got it here, meaning it is called a TLB hit! So, if TLB gets a hit, then I'll go to the frame number and pick up that page. 

But if TLB didn't hit, then what will happen in that case? In that case, we have to apply the normal procedure. What is the normal procedure? That you look in the first page table. From the page table, you will get the frame number. Then you can access the main memory. 

So what will be my effective memory access time? What is effective memory access time? First, I will find it in TLB. First, I searched inside TLB. It took me some time to search inside TLB. First, I spent that time. What did I get after spending that time? I got the frame number. Then I found the particular page in the main memory. The time for finding it in the main memory is let's say $x$. This time will be taken if TLB is a hit, meaning the corresponding entry of the page you are looking for—its corresponding frame number—is lying in TLB. 

Now, what comes of the story is: to keep all of them, to keep the complete page table of all the frame numbers here, can't keep up. What's the reason? That TLB has a limited size. Meaning, if you increase the TLB, you increase the cache, so your cost will increase. What do we do with our normal laptops? What happens in the normal PC we have? The cache memory that is there is limited. Like, if I talk about 12 bits—let's suppose my TLB is of 12 bits. So, that means how many entries can I keep? 2 raised to the 12th power entries I can keep here. Not that I would increase this too much. If I increase it too much, my cost will increase a lot. So the cost cannot increase very much. 

So what are we doing? We have a limited amount of data. Put a limited amount of frame numbers here. So if a hit occurs—what does a hit mean? The frame number of the page you are looking for is lying here. So you go straight, pick up the frame number from here. So the total time taken in case of a hit is: time to access TLB plus the $x$ amount you're putting in memory. 

Plus... if it happens here, a miss! What's the point of a miss? If you searched here, searched inside TLB, looked inside the cache, but you didn't get the frame number, so obviously what will you say to it? TLB miss! That is, the page you are looking for—its corresponding frame number entry is not found here. That is called a miss! So you entered the access time of TLB. Then, after applying, you found out: hey! The page you're looking for doesn't have an entry here. Then what will you do? Then you go to the page table, because obviously in the page table, corresponding frame numbers of all pages must be there. That is, you accessed the page table. And we already know what is the time to access the page table? $x$. The reason I have already told that: where is the page table located? Inside the main memory. So the time to access the main memory we have kept as $x$ value. So, obviously, how long will it take to access the page table as well? $x$. What did I get from the $x$ amount? The frame number. Now I have got the physical address. By going to that physical address—meaning the $x$ amount of time which I have invested more—then I found the particular page. 

So that is: if I get a hit, it looks at the time. If I miss, it will take time. So this is the formula for this effective memory access time. 

But remember, if no page fault occurs—what does a page fault mean? I'm assuming that's the page you're looking for, that page is inside the main memory. If that page is not inside the main memory, so what is the concept of page fault service time? The concept of page fault service time is different, meaning now we have to fetch that page from secondary memory. Where? Inside physical memory. So here, if I have written effective memory access time, if there is a page fault not occurring, so simple: find it in TLB first. You will get the frame number from TLB in the main memory. So it took this time. That is called a hit. And what do we mean by a miss? You saw in TLB before; you could not find the frame number. Then you looked in the page table. You got the frame number. Then you accessed that page by going to that frame number. 

So, obviously, we want that my hit time, the hit ratio, should be high, and the miss ratio should be less. If your hit ratio will be high, so the effective memory access time will be benefited only then, because you set up TLB. If your misses are too many, if the miss is more, then there is no benefit of applying TLB, because you're spending extra TLB time here. If your hits are too many—meaning the page you are looking for, particularly the entry of that frame number is here—so you can say that you have benefited from installing TLB. 

The numericals which are based on this come a lot in the competitive exam. Even in college and university, they ask what is the concept of TLB, or simple numericals can come related to that. So, numericals we will discuss in the next video. Thank you!