Hello friends, welcome to Gate Smashers. The topic is multi-level feedback queue. In a multi-level queue, we've seen a problem that is starvation. The problem is occurring there because when high-priority processes like system processes keep coming, then because of that, lowest-priority processes will keep waiting for their turn as to when their turn will come and they will get executed in the CPU. When their turn is about to come, let's say another system process comes and again they are waiting. So because of it, there's a problem of starvation. 

The method to remove the starvation problem is feedback. Feedback means the lowest priority one would give its feedback and we would keep upgrading it gradually. I've taken a simple example here; it doesn't mean that this is the only way out. You can use different algorithms also. It's possible that you'll get different algorithms and values in the numericals, but this is one of the methods to understand how multi-level feedback queues actually work. 

So here, if I have lowest priority processes in RQ1, RQ2, RQ3, RQ4... I mean the concept is the same. I mean this is not one queue; there are multi-level different queues for different types of processes. Now, if it has the lowest priority queue in it, then when a process comes in the lowest priority queue, let's say P1 came, then P1 will get its turn when high priority processes will end. When all this will end, then finally P1 will get its turn. But it is also possible that the moment P1 is about to get its turn, some new process comes in it, then we'll give it to the CPU. So this will keep waiting. 

So what we are doing in this case, the first biggest advantage here is that we'll feedback. Feedback means the lowest priority process entered from here, P1. We gave P1 a time quantum of 2 for some time, let's say. So we executed it for some time, then we upgraded it. Upgrade means we placed it in a different queue so it can get the CPU much faster. Then we upgraded it again, and this way we are moving it again and again. And when old ones keep moving, then space for new processes will get created and new processes will come there. 

The second point here is that if there's some high priority process, we don't have to give feedback to the high priority process. Because a high priority process's turn will come first, so if a system process arrives, then it'll directly enter here and will execute in the ready queue. And whatever algorithm we use, let's say if we are using FCFS, then it'll get executed through it simply. So we are not using multi-level feedback queues for higher priority; we are using them for lower priority so they'll get their turn faster and there'll be no starvation. 

So if I have a P1 process, a batch process of lowest priority, let's say if its execution time is 19. Meaning it'll take 19 units of time to get executed. So how will it proceed? First of all, if it's a batch process, then it'll go in this queue. It can't go in any other queue. So when it'll come in this queue, then it needs 19 times, but it'll get 19 times executed when it'll get its turn first. So what we'll do here is we'll just run it up to 2 time quantum. Meaning out of 19, we'll run it up to two time quantum, then 17 will be left. And we are giving its feedback in a way and converting it into queue 2. Meaning we sent it here so some other processes would be able to come here. 

Now P1 doesn't have to wait for much long. Now its turn will come in this queue, and it's running up to 4 time quantum in this queue. So if it ran up to 4, then how much is left? It's 13. After 13, because other processes will also come in this queue, we won't keep it in the same queue for much long. We're shifting it, meaning keep giving its feedback, so we'll send it to the next queue. Now, if it ran up to 8 in this queue, then how much is left? It's 5, meaning it needs 5 units of time more to get fully executed. 

So we'll send it again in the queue below. In this queue, it's first-come, first-served. Any algorithm can be used; it's not fixed, but this is one of the examples to understand multi-level. So the leftover 5 time went as first-come, first-served. 

So we are taking care of both things. First, the lowest priority process should not get into starvation; we keep on changing its queue. Meaning if its queue will keep changing, then its turn will keep coming faster. And the second one we are using here is that if there's some high priority process, then it doesn't have any problem. It'll keep running as it always runs. So that's why we use multi-level feedback queues, just to remove the starvation which occurs in multi-level queues. Thank you!