Hello friends, welcome to Gate Smashers. In this video, I am going to explain indexed allocation. In this video, we will discuss all the important points related to indexed allocation that will be very beneficial for your competitive exams and even your college and university exams. 

Before indexed allocation, I have already explained contiguous allocation and linked list allocation, and I have already made videos on that; their link is in the description box. If you haven't checked those videos, then please check them so that you know what contiguous allocation is, what its advantages and disadvantages are, what linked list allocation is, and what its advantages and disadvantages are. The third technique is indexed allocation.

Look, as the name suggests, "indexed"—the meaning of index is that, like we have in books, in books obviously we have content, many topics are there, but to search that topic, an index is given in the starting of the book. Generally, in that index, we have mainly page numbers, meaning there are page numbers given along with topics so that you can get to that page to search data, meaning you searched and you can fetch your data after getting on that page. So this is the basic concept of an index.

Now, look at how we will store the data here in index. For that, we have a disk here, a hard disk; it's a permanent storage. Now, in this permanent storage, we have to enter the data. Let's say we have file A. Now, what we did to this file: we divided file A, like I have already mentioned many times, that we divide the data before storing it, and that division is based on the physical blocks you have, meaning the physical blocks inside the disk. It depends on its size, that you have to divide the file in what size and how. So let's say I divided file A into 4 blocks, meaning block 1, block 2, block 3, and block 4. So I divided this file into 4 blocks. 

Now, what we did: we stored this file in the disk, meaning we stored it in the physical blocks in the disk. And after storing it, let's say I stored block B1 in 9, B2 in, let's say, 1, stored B3 in, let's say, 14, and stored B4 in, let's say, 4. You can store it in any block depending on the availability. Look, if you're thinking why didn't I do it contiguously or why did I choose this myself, then let's assume that the others are already filled; only this was left empty, and some file already exists in all the other ones. Based on the availability, I adjusted all the four blocks.

Now, if I adjusted these blocks, then obviously we will fetch it too; our main purpose is not just to store the data—fetching the data after storing it is the main purpose. So how we will fetch that data? What we will do now is that we will make an index of this. The meaning of index is that the index block will tell me where my data is placed actually. So what I did: I made an index block of file A, and that index block is 6. Meaning, in this 6th block, there is the index of this file. When you'll get to this sixth block, meaning when you will fetch this sixth block, what that sixth block will tell you? It will tell you where B1 is (B1 is on 9), where B2 is (B2 is on 1), where B3 is (B3 is on 14), and where B4 is (B4 is on 4). 

Meaning, here what you'll get in the directory—in the directory, you'll get the name of the file, other than that its other attributes, but the main important thing is that you'll find the index block here. Then, after getting to that index block, meaning every file will make its index block, that means what we're doing, like in books we reserve some pages in the starting, not like that the whole book contains data—the whole book doesn't contain data; in the starting pages of the book, 5 to 10 pages, we have made an index. We are doing the same thing here. We aren't placing data in all these blocks; along with data, we are utilizing some blocks as index blocks.

So let's say the index block of file B is in 15. So when you'll get to 15, in 15 you will get to know about that file, whatever the file is. Let's say the index block of file A is placed here. Every file will have its own index block. Whatever the data you want to store, where is block 1, block 2, block 3, block 4 of file B—so first you have to search the index block; that index block will tell you where block 1, 2, 3, 4, 5, or as many blocks as there are of the file, where they actually are. So this is the concept of indexed allocation. 

So if you're asked if there is only one index block for all files: no, every file will have its own index block, just like every book has its own index. It does not happen like that if there are 10 books of a publication, all 10 books will have only one index. No, every book has its own index. Similarly, every file will have its own index. So this is the basic concept of indexed allocation. In the directory, you'll only find the index block. After getting there, you'll get to know where your actual data is placed, which will tell you about your actual data, meaning where the B1 block, B2 block, B3 block, all of this is.

Now look, what are the advantages? The advantage is that it supports direct access. If I have to do direct access—the meaning of direct access or random access is that rather than having to search B1, B2, let's say I'm searching B3, meaning I don't want the whole file, I want the B3 block of a file. So obviously, the problem in the previous one was—in the previous one, in linked list, what you have to do is first go to the first block, that will give you the address of the second; the second will give the address of the third; the third will give the address of the fourth. So you search for the block one by one like this, then you get the desired data. But it is not like that here; it supports direct access, it supports random access. 

Why? Because first you'll get to 6. At 6, you'll get to know that B1 is placed at 9, B2 is placed at 1, B3 is placed at 14, and B4 is placed at 4. So which one do you want? You want B3. So directly get to 14 and fetch data from there. So this is the actual story of direct access or random access.

Another advantage is no external fragmentation. Obviously, any block inside the hard disk won't remain unutilized. That was the problem in contiguous allocation; this wasn't the problem in linked list, and here also it is not a problem because we can place data anywhere, in any block. So obviously, there is no block that remains unutilized, where you have a file and you have blocks for that file, but you are not able to allocate them. That is the actual problem in contiguous allocation. I have already explained this many times; if you haven't checked the video, then you must check it once. So there's no concept of external fragmentation here; there's no problem. You can utilize all the blocks so far.

Take a look at the disadvantages. The first disadvantage here is pointer overhead. Obviously, you can see how many pointers we have to use here. First, you are going to the index, it is pointing you, and after getting to that point, you'll get to know where your data is actually placed. Obviously, a huge number of pointers are actually used here. So somewhere, if I have to use memory, I have to use pointers. If we assume every pointer to be of 4 bytes, 6 bytes, or 8 bytes, based on the hardware or based on whatever infrastructure we are having, the pointer creates overhead because the 6th block that was here—that could have been used to store data, but every file will have its own reserved index block. Like in a book, we don't keep data on all the pages; we've reserved some pages in the starting of the book, we have to reserve that otherwise we won't be able to search.

The next disadvantage is the multi-level index problem. Look at this problem here: if your file is small, if your file has only 2 to 3 blocks, then also you have to keep an index block. And if your file is very large, then also you have to keep an index block. So if it is a small file, then unnecessarily we reserved one block in which we are only placing the pointers; no data is placed there. Obviously, somewhere we are over-utilizing the storage—rather than storing the data, we are storing the pointers. 

And if your file is very large, then obviously all its values—meaning I have to make an index for all those blocks, and that index will further point to the data. So in this case, what can happen is that your index file becomes so large that it is not able to fit in one block, meaning the index is not fitting in one block. There are that many entries in the index. And why will there be so much entries in the index? Because your file was too large. You have put so much effort in dividing the file, so the index will also be that much larger. 

And if the index becomes larger and it's not fitting in one block, then what will we do? You'll have to reserve multiple blocks, meaning you have to reserve more than one block. That is also the overhead of a multi-level index, meaning you have to make one more index after this index, like we discuss in indexing, I've also discussed in B-Trees. So basically, you have to make a multi-level index. That is also a type of overhead because searching will create even more problems in it.

So this is the whole story of indexed allocation. And if we talk about its real-life example, the Unix file system uses inode. It uses the structure inode, meaning how it stores the data—it supports indexed allocation. So here, the 'i' in inode is actually representing the index. So basically, Unix supports inode. So later on, we will discuss in the next video whatever problems occur in this index, the structure of inode, and if a multi-level index occurs, how we will handle that thing, how we will resolve that problem.

So this is all about indexed allocation. Thank you.