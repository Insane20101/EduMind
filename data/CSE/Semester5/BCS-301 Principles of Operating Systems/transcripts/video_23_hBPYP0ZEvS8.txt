Hello friends, welcome to Gate Smashers. The topic is multi-level queue scheduling. All the scheduling algorithms we saw until now, we just take only one ready queue there, that all the processes that we have to execute. What we do is, we try to put all the processes in the ready queue. And in the ready queue, out of all the processes, we'll try to extract some processes to the CPU and will give them to the CPU. Means out of all these processes, we pick some process on the basis of a scheduling algorithm and execute it on the CPU. 

But what the multi-level queue scheduling concept is, when we don't have one type of processes in real life, processes can be of different types. So when processes are of different types, then why is there only one ready queue for that? So what multi-level queue says is that it has three types of processes: system processes, which have the highest priority. Like interrupts... If an interrupt generates in the system, then all the working will stop. First of all, the operating system will handle the interrupt. Same with interactive processes. Interactive processes mean like if you are watching a movie on VLC, or interacting with an application directly, that's called an interactive process. Batch processes are those with the lowest priority. They are called background processes also, where we give multiple processes to the system and the system keeps executing them in the background. 

So if I have this much different types of processes which have different priorities also, then why is there only one queue for that where we place all of them in one ready queue without checking what type of that process is and forward it to the CPU from there? NO. What multi-level queue says is that there must be a different queue for every process. Means each one will have its own ready queue. When a system process will come, we'll place it in this queue. When there'll be an interactive process, then in this one. If batch processes, then we'll place them in this one. And we've already decided the priority also in it. 

Second point that's important here is that every process can have its own algorithm also. Means if a process is a system process, then it'll be with round robin scheduling algorithm. Means if I have 5 to 10 system processes, then how to execute those 5 to 10 system processes? Through round robin. If I have 5 to 10 interactive processes, then how to execute them? Like shortest job first. Means I can use any scheduling algorithm. These scheduling algorithms can be the same and different also. And we'll give them to the CPU one by one. So the biggest advantage here is we're categorizing the processes according to their different real-life scenarios. 

But the problem is, obviously if we keep getting too much system processes, then there'll be no turn of interactive and batch processes; they'll run so late on the CPU. Whenever a system process will come, it'll get executed first. Batch and interactive processes will keep waiting. So there'll be a problem of starvation because the processes on lower priority will keep waiting for their turn. So to remove this, we can use multi-level feedback queue, that's its next version. 

So that's it in the multi-level queue scheduling. If they'll ask any question, then maximum it could be based on theory. There's no such practical or numerical question existing on this. Thank you!