Overlay is a method by which a large-size process can be put into the main memory. That means if the size of my process is more than the size of memory, I can make that process accommodate in the main memory by using the concept of overlay. How? Let's say I have four partitions in the main memory: 2MB, 4MB, 8MB, and 16MB. Now, what is the size of the maximum partition? 16MB. Here, what constraint comes in fixed partitioning? That I cannot make a process bigger than 16MB enter. But if we use overlay, then I can accommodate the process even more than 16MB. 

So where do we use overlay? Where the size of our main memory is less than the size of the process. In that case, we bring that process into the main memory. And how do we allocate memory to it? This is the point. Now, how does overlay use this? When the process whose size is more than the main memory cannot be brought in all at once because obviously the main memory size is smaller and the process size is bigger, what will we have to do? We will have to divide the process, meaning we will divide the process, program, or object code. The code and functionality that is needed first, we will load that functionality into the main memory. When the work of that functionality is done, we will bring it out and bring in another partition, and a third after the second. We have to bring in the partitions like this. 

Overlay creates a problem here because there is no driver in the operating system that supports overlay. That means the user has to decide on their own how to divide that process or program so that whenever I bring in the partition, I could execute its functionality properly, then bring it out, and then bring in another program. So, like this, there can be a very big problem. It may happen that the work I have to do first, I didn't bring in that partition first.

Actually, this concept of overlay is used in embedded systems because the user is dividing here. Now, the user divides manually on their end; they can divide however they want. Embedded systems are those where my functionality is fixed, meaning those circuits perform only a fixed type of functionality. Take my washing machine, for example—in that, I know it only works on a fixed functionality. So what can I do there? I can divide that process and create two to three different functions. And in the main memory—obviously, my main memory is limited in an embedded system—I'll bring in one functionality. When its work is completed, I'll bring in the second functionality. When its work is completed, I'll bring it out and bring in the third. 

However, the problem here is that when the user divides those functionalities or processes, that division should be independent. Independent means if I bring in this partition, it should not be like some code in this partition is depending upon that partition. Otherwise, a problem can be created where the data the CPU is searching for cannot be found. But this doesn't create a problem in embedded systems because the functions and processes in embedded systems are fixed. 

In computers and desktops, however, our processes are of different sizes and there are different numbers of processes. So there, I cannot leave the functionality to the user to divide themselves—how we should divide it, how we should create it. That is why the concept of overlay is used in embedded systems. It is still used in many embedded systems, but we don't use this in our computers. Here, we use the concept of virtual memory. We will discuss the concept of virtual memory and demand paging in further videos. 

Let's discuss a question about how we actually use overlay. Suppose we have a two-pass assembler: pass 1 is 80 KB, pass 2 is 90 KB, the symbol table is 30 KB, and the common routine is 20 KB. At a time, only one pass is in use. What is the minimum partition size required if the overlay driver is 10 KB in size? 

Here, they are talking about having an assembler. Why do we use an assembler? An assembler is used to convert source code into object code. For that, here I am using a two-pass assembler, meaning it is working in two passes. When we use pass 1, there is no need to use pass 2, and when we use pass 2, there is no need to use pass 1. 

If I look at the total size here, the size of pass 1 is 80 KB, the size of pass 2 is 90 KB, the symbol table (which is mandatory) is 30 KB, and the common routine is 20 KB. We have to bring in pass 1 into the main memory, then bring it out, and then again bring in pass 2 and bring it out from the main memory. For that, there is an overlay driver, and the size of that driver is 10 KB. 

So what is the total here? It's 230 KB. That means the size of the whole program becomes 230 KB. If the size of my memory is 230 KB, there's no problem; you can directly work with this full function or process because your memory size is 230 KB. But that's where we are using the concept of overlay. Let's say the size of my memory is not 230 KB; it is less than 230 KB. When the size is less than 230 KB, I cannot keep the whole process or assembler in the main memory at the same time because we are assuming the main memory size is less than 230 KB. 

So here it is asked in the question: what should be the size of the partition, or how much should be the size of the main memory so that I can use overlay there and use the passes properly? It's already mentioned in the question that at a time only one pass is in use. That means if we use pass 1, its size is 80 KB. If we load pass 1 first, that means we have a requirement of 80 KB plus 30 KB for the symbol table, 20 KB for the common routine, and 10 KB for the overlay driver. That means what is the total? 140 KB. 

If I load pass 1 first along with the symbol table and common routine, then I need 140 KB of memory here. Can I take 140 KB of memory? Will this 140 KB memory be enough for my work? Wait, we haven't calculated pass 2 yet. If we bring out pass 1 from the main memory after loading it and then load pass 2, the size of pass 2 is 90 KB. When we load pass 2, the symbol table is also needed, the common routine is also needed, and the driver is also needed. So here my total is 150 KB. 

If I load pass 1, I need 140 KB, and if I load pass 2, I need 150 KB. That means if I keep the size of one partition in the memory at 150 KB, then there's an empty space of 150 KB. When I load pass 2, my full 150 KB will be filled. Pass 1 isn't needed at that time because when we load pass 2, only one pass is in use. Then I'll load pass 2 at that time. And when pass 2 gets out of here, I'll load pass 1. When pass 1 comes in, it's occupying 140 KB of memory, so 140 KB can easily be accommodated in 150 KB. 

So that's the point here: you have to choose the one which is the maximum out of these two. If we're using pass 1, our total requirement is 140 KB, and if we load pass 2, our total requirement is 150 KB. If I keep a partition of 140 KB, the problem still exists; when the pass 2 of 150 KB comes, how will you fit that? 150 KB won't fit in 140 KB. So the best thing is to choose the partition of the size that is larger or has the maximum number. That means the answer to this question is 150 KB. 

This was about partitions. Even if you are asked normally what the size of the main memory should be: if the size of my main memory is 230 KB, then there is no need to use overlay. We use overlay where the main memory is limited and processes are quite larger. We use overlay in that case. So that's why, if you're asked this type of question, simply calculate it first, and out of those values, the one with the maximum value is the size of the partition, or we can say that will be the size of the main memory. It can be asked in any of these terms—it may be possible they ask in terms of partitions or they may ask in terms of main memory requirements—so you can calculate that. 

That is why we use the concept of overlay where my main memory is limited, and most of the time this happens in embedded systems. In real-time computing, we don't use this in our PCs. In real-time systems, we use the concept of virtual memory, where pages are swapped in and swapped out. How we do demand paging or how we use that, we will see in further videos. 

This is all about the concept of overlay. Thank you.