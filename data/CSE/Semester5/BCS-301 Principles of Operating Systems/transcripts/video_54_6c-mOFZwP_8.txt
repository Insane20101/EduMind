So in paging, we split a process into equally sized pages and insert it into the frames of main memory. Let's assume we have a process P1 whose process size is 4 bytes and page size is 2 bytes. So the total size of the process, as we have assumed, is 4 bytes, and the size of a single page is 2 bytes. The number of pages per process will be 4/2 bytes, whose answer is 2 pages. 

Now, I have shown here that the process P1 is split into two pages, 0 and 1, in which the numbers 0, 1, 2, and 3 are present, as we have seen before. So there are 4 bytes in the process. If we start numbering the bits as per the binary system—because binary numbering starts from 0—I have represented these bytes here as 2 bytes per page, thus dividing the process. 

In main memory too, we have a 16-byte main memory, and we have discussed earlier why page size and frame size should be the same. This is so that if we divide a process into pages, one of the pages can be taken and fitted in the frame compactly—meaning the page should fit in the frame as it is. That's why we have always kept the page size and frame size the same. 

The number of frames can be calculated by dividing the memory size (16 bytes) by the frame size (2 bytes), which I have already represented here. But always remember that the number of frames here is 8, and whenever we start binary numbering, or even in arrays, we have numbered the pages 0 to 7. 

Now, what is it representing internally? We have 16 bytes of main memory, which means I can store data up to 16 bytes. Now I have just named these bytes. We know that main memory is byte-addressable, which means we represent the memory through bytes only—that is, whenever the CPU demands, we easily take the bytes out and give them to the CPU, through which it gets proven that this is just a representation of dividing a process into pages and memory into frames. 

Here, remember an important point: the CPU has started executing P1. When the CPU is executing P1, there arises a problem—the CPU doesn't know that you have created process pages and fitted them into memory frames. Whenever the CPU executes a process, it just calls the data or instructions of the process. Meaning, among the bytes 0, 1, 2, and 3 of P1, if the CPU says, "This byte," but where is it actually? It is in the main memory in page form, as we have seen. 

So here, let's say what I am doing is: these blocks or frames 1 and 2 are already filled. Page 0 of P1 has come here, and Page 1 of P1 has come here. Let's say this one is also filled. Let us fill these frames too so that you will get clear on the concept of inserting data into frames. 

We have assumed that Page 0 and Page 1 of P1 have been inserted into Frame 2 and Frame 4 of the main memory respectively. Which implies the bytes 0 and 1 of P1 have occupied the places of bytes 4 and 5 of Frame 2, and bytes 2 and 3 of P1 have occupied the places of bytes 8 and 9 of Frame 4. Thus, the bytes in P1 have been stored in their respective places in main memory. 

Now, irrespective of the knowledge about our paging strategy, the CPU is simply asking for byte 3 of P1. So if we want to give byte 3 to the CPU from main memory, it may be allocated anywhere at random, which means it can be stored anywhere in the main memory. 

Now, here we need mapping. The address generated by the CPU is not the absolute address; the CPU needs data from here, but it has generated the address 3. So I need a technique which converts the address of 3 to the address of 9 and gives me that data. This is what we call mapping. 

This mapping is done by the Memory Management Unit (MMU). The MMU converts the address generated by the CPU into an absolute address. For this conversion, the MMU uses page tables. You can check how—if every process has pages, then I need more than one page table. 

We use a simple concept here: we are creating the page table of P1. How many entries will it have? How many entries will a page table have? It is equal to the number of pages in the process. In the sense, how many pages are in P1? Then how many entries will be here? This entry is Page 0. Now where is Page 0? Look at it carefully, where is it? In Frame 2 of main memory. So what does it contain? The frame number. 

Whenever I want to access a page of a process, I will get to know the frame number from which I will fetch its data. Likewise, I will access Page 1 of P1 from Frame 4. 

Now, if the CPU asks for any byte—for example, byte 3 here—we don't know where it is located. First of all, we have to locate the page number of byte 3 so that I can go to that page. Let's say it is located either at Page 0 or at Page 1. If it is at Page 1, then I should check Frame 4 and fetch the data. 

Now, how does it work? The CPU generates the address called the logical address, which is a very important point. It is made of two factors: how much is the page size, and how many bits do we need to represent two bytes? If we have two numbers, what may be the numbers? How many bits do I require to represent 0 and 1? Only 1. One is 0, and the other is 1. 

Similarly, let's say we have a total of 8 numbers. 8 numbers mean the numbers that I have are 0 to 7. If I have 8 numbers, the numeric range I have now is from 0 to 7. So zero is represented as 000, and seven is represented as 111, which shows that I need at most 3 bits to represent numbers in this range. But if I want to represent numbers beyond 8, I need 4 bits. 

The numbers in 0 to 7 are 8, and to represent this numeric range—a very important point on the numbers we are discussing—say we have 16 numbers. My numeric range is between 0 and 15. If 15 is the largest number of the range, then it is represented with 4 bits (i.e., 1111). We are not talking about values, but the numbers are 16 in the range. If 15 is the largest number of the range, then it is represented with 4 bits. 

If my page size is 2 bytes, then I need only 1 bit to represent it. Here I have the page number, and the given number of pages per process is also 2. So I only need 1 bit to represent it (i.e., 0 and 1). If I have 2 bits, 0 and 1, I need only 1 bit space to represent it, and we are given space here. 

Now, the point is the CPU-generated address is 3. To represent 3 in binary, I have written the page number here as 1. Here the main point is I have to access Page 1 of the page table in P1. We have 2 bytes in each page. I have to select the zeroth byte or the first byte. I have reached the page number, but I have to choose among the zeroth byte or the first byte to fetch. That is represented in this byte where I have written 1, which means I have to select the second byte. 

This might be a little confusing, but one point to clear it is that we should start numbering from 0. For example, if I have 2 rows of students where there are 2 students at row 0: student 0 and student 1. Start numbering from 0. Now here, for byte 3, we have to go to Page 1 and access the second byte. To represent the second byte, we use 1 in binary numbering—in the sense, if we go to Page 1, we select the second byte in it (i.e., byte 1), and yes, we have fetched this byte. 

To fetch this byte, we have to convert the logical address to an absolute address. That means I have to convert the logical address 3 to the absolute address 9. To convert it to 9, we have to use—as we discussed earlier—the logical address generated by the CPU, which should be converted into a physical address. 

Now that I know about the logical address, which consists of the page number and page offset, let's see about the physical address. The physical address represents the location of the byte to be searched. The number of bits to represent the physical address depends on the size of the main memory. The memory address is byte-addressable, so nothing special needs to be done as a byte is the minimum. 

Let's take 16. To represent 16 in binary, we need 4 bits, as discussed before. So the total bits of the physical address are 4 bits. In this, the first part represents the frame number, and the next part represents the frame offset. Frame offset means frame size. 

To represent 2 bytes, we need 1 bit in this sense. And if you have an iota of doubt, you can check 8 numbers from 0 to 7, where for 0 it is 000 and for 7 it is 111, so in total we need 3 bits, which is used here. The total bits to represent the physical address are 4 in number, in which 3 of them are for the frame number and 1 is for the frame offset or frame size. 

So pay attention here: the address 3 generated by the CPU has to be converted into the physical address 9. To convert, we know we have to go to Frame 4 in Page 1 of P1. It is given Page 1, and in there we have Frame 4, which we accessed too. Here we accessed Page 1 successfully. 

How do we represent 4 in binary? Frame 4 is theoretical, but the CPU takes only binary values, as per which 4 is represented as 100, along with the frame offset, which means frame size. Moreover, the bits of the page offset and frame offset should always be the same. Because if we want the second byte of Page 1 of P1, then it will also be located in the second position in the respective frame. 

This is a simple concept. In this page, this is the first roll number and this is the second roll number. So if we shift the places of these two to the right here, their places will be retained as they are here too. Here, without changing their sequence, we shifted their places to somewhere which is an unknown distance away. 

In normal terms, I need the second byte, and in binary terms, I need byte 1 from Frame 4. This represents Frame 4 and not Frame 0. Therefore, I will fetch data from byte 9 and give it to the CPU. 

So it is summarized that the CPU generates the logical address. The CPU just tells the byte number it needs, which we should convert into a logical address to access the page number, which again should be converted into a physical address to access the frame number. 

Now let's review this concept even faster with a simplest example. The CPU says, "I need byte 1." It needs byte 1. Now that the CPU has generated the address of byte 1, how can we represent it? If we have a two-bit space for representation, we represent it as 01. Which means we need byte 1 in Page 0—i.e., this one which we wanted. 

After we went to the page table to check Page 0, it says that it is located at Frame 2 in main memory, to which we bounce again. In that, I need to pick byte 5. We got to know that it is located at Frame 2, in which 2 can be represented as 010 in binary. 2 can be represented as 010 when represented in 3 bits, and this bit will remain as it is for the page offset, as we are not changing the sequence of the bits. 

So can you guess what value comes in decimal? Now I will get the data from the same location as that of byte 5. So we need page tables to map from the logical address to the physical address of a byte. 

Now, after the CPU generates a byte address, we find the corresponding page numbers, in which we see the corresponding frame number, to which we jump and fetch the required byte and give it back to the CPU. The mapping from 1 to 5 is done by the Memory Management Unit, and how? Just remember this always: which byte in it? There are two factors in the physical address. These two bits will not change because bytes are placed as they are without any change.