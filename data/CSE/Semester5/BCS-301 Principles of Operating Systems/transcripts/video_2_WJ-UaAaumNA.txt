Hello friends, welcome to Gate Smashers. The topic is Introduction to Operating System and its functionalities. Before starting this video, I would like to request my viewers to please subscribe to my channel if you haven't yet, and please press the bell button so you can keep getting notifications of all the latest videos. 

In this video, first we'll see the introduction to the operating system, and then we'll discuss what the functionalities of the operating system are. So, what is an operating system basically? An operating system is a system software. Meaning, it's a software which is in your system, and its basic functionality or definition is that it works as an interface between the user and the hardware. In very simple language, the operating system is providing interaction between the user and the hardware. 

Now, if we look at this diagram first: which of these is hardware? If we take the scenario of our laptop, in our laptop or desktop, what hardware basically is? CPU, which we call the brain of the system. CPU... then input/output devices, which include keyboard, mouse, printer, scanner. Then RAM, that we call the main memory. Then secondary memory, i.e., hard disk. 

So, when we access a lot of this hardware—when I'm accessing my system, then I'm just accessing these devices. But am I directly accessing those devices? Is the user directly accessing the hardware and devices? No. He's accessing through what? Through the operating system. The operating system is working as an interface here. 

Then the second point is: why? Why is there a need for an operating system? If there'll be no operating system, then what will be the scenario? The scenario will be that if a user wants to access a particular hardware, he has to write a program for it, and through that program, he'll always access that device. Meaning, he always has to write a program to access those devices. He has to write different programs for different devices again and again. 

So when he'll write this much program in repetition—for example, I have to give a print command simply, I have a Word file that needs to be printed, then I can't print it directly. First, I have to write a program for it so that the printer will know that it has to print. Similarly, if we'll talk about the CPU, if I want to execute a process or a job, then that job execution is a later work. First, we have to give the CPU an instruction, and how will we give that instruction? For that, we have to write individual programs, and because of that, the interaction between the user and the hardware will be very complex. 

There could be another way also. It could be possible that a user is accessing one hardware, and if another user wants to access that hardware, then I'm not releasing it. Why? Because I'm the user accessing the hardware directly, meaning there's no authority in between who can take back the hardware from me. So there are a lot of reasons because of which we need to use an operating system that is used as an interface between the user and the hardware. We'll see that in functions. All the functions of the operating system, we can call them as these are the reasons why we are using an operating system in our system. 

So, if I'll give a basic example of an operating system, the first operating system that is used in the majority is Windows. Now, the primary functionality of the operating system: if we'll say why we are using an operating system, the major primary goal of the operating system is to provide convenience. To provide convenience to the user. Yes, it provides convenience, meaning I can access my hardware in the easiest way. That is the functionality of the operating system, and this is the reason why Windows acquired the whole market for many years. 

There was a time when Windows acquired 95% of the market, meaning the whole market was acquired by Microsoft. They had this much popularity because the major goal is convenience, and Windows is providing convenience the most. If you'll see in your or your friend's laptops, the operating system you'll get is Windows. There could be different versions in Windows, like 2007, 2008, etc. That depends. 

But why are we providing Windows basically? Because of convenience. But gradually with time, the problems that we were solving in our systems started to get complex, and the value of time is increasing so much for the user. Meaning, the user wants that his problem should get executed very fast, and for that, we need throughput. 

What throughput means in a simple definition is the number of tasks executed per unit time. That is a simple definition of throughput. So the more number of tasks are getting executed in a particular amount of time, the more will be my throughput. It means we need such operating systems now whose throughput is high as well as convenience is high. 

So the maximum use in throughput is Linux. Although the Linux operating system doesn't mean that it's not providing convenience, I'm just comparing it because why Windows acquired 95% of the market? Because of the only reason, i.e., convenience. But now, if we'll see at present in 2018, it is approximately 82%. Meaning, now their acquisition in the market has become low as compared to past years. Now Linux, that was in 1-2% before, has now increased because of throughput. And other than this, we are having Apple's own operating system, that is Mac or Macintosh, that also runs well in the market. But that is a different thing. These are real-life examples like Windows, Linux, and Macintosh. But the major goal is convenience and throughput.

Next part that comes here is: what are the functionalities of the operating system? Meaning, if I've put an operating system in my system, then what are its functionalities? The major functionality of the operating system: the first one is resource governor or resource manager. It manages the resources. 

How do we manage resources? If I'm a single user and accessing my system by my own, then at that point, I don't need a resource governor that much. Meaning, wherever there is parallel access, meaning when multiple users are accessing a system—that work happens in majority on a server level. If we would talk about a server where a lot of users are sending requests to access some data, at that point, the operating system is useful and works as a resource manager. How much hardware to be provided to which user or for how much amount? That is done by the operating system. After that time, it will take back that device from the user. This is how we manage resources so that there'll be no load on the system. 

The moment we open the normal Task Manager in our system, then you'll easily get a lot of values like that: how much percent we are using the CPU, how much the RAM. All those values are managed by the operating system. So there'll be not that much load on the system, and not much less either. Meaning, it tries to manage resources properly. But specially where it is used is parallel processing, where multiple users are trying to access one device. 

The second point is storage management, or we can say it as process management. Process management means we are managing processes in majority in our system. Meaning, we are executing processes. I opened the laptop and I'm working on Microsoft Word on it. Along with that, I've opened my Windows Media Player also. Along with that, I've opened a gaming app also. Meaning, I'm sitting with multiple processes opened. How to execute all these processes? That is also done by the operating system. 

How does it work? CPU scheduling. Here we use CPU scheduling algorithms. CPU scheduling means if I wrote a simple program and saved it with `.c`, now I want to execute that program, then that program becomes a process. Now, if we want to execute the process, then where will we do it? At the CPU. Now, how to execute that process at the CPU? That is done by CPU scheduling, and there comes a variety of algorithms that are useful to access and execute in an efficient way. And who is handling all that management? The operating system. 

Other than this, we have storage management. Storage management means basically storage includes secondary devices, i.e., hard disk. Meaning, the data that we want to store in our system permanently, the operating system is managing how to store that data. And how do we do storage management? Through file systems like NTFS, CIFS, etc. CIFS and NFS—Common Internet File System and Network File System. All these get used in Linux and Windows. Their major goal is that it manages how to store all my data in the hard disk permanently. How to store it in tracks or sectors in the disk's architecture, that is also managed by the operating system. 

Then the fourth one is memory management. In memory management, what I have here is RAM. Because if we would say why we have separated storage management and memory management: because storage management consists of the hard disk. It's the operating system's functionality to manage the hard disk. But there's no constraint on the hard disk; we have a constraint in RAM, i.e., limitation of size. Meaning, RAM in our system is limited, and all the processes get executed first in the RAM. This topic is called the concept of multitasking or multiprogramming. 

Meaning, all the processes that we have to execute, we bring all that first of all into the RAM. And when it comes into the RAM, then we transfer it to the CPU one by one. So how much memory of the RAM we have to give and take that memory back when the process is done? Allocation and deallocation. Because the RAM is limited, it shouldn't be like the process has been executed, but it's still in the RAM, and the other process that wants to get executed is not able to come in the RAM because it's full already. So what the operating system does is, when a process is done, then it takes it out of it and brings the other one. This is called swapping. So these are whole individual topics and units; it's just I'm discussing here what the functionalities are. I've already discussed about them. I have created whole videos on it where we've talked about memory management, storage management, and disk or file systems. We have already discussed CPU scheduling and resource management. 

Other than this, we have the 5th—I'm writing the functionality here—i.e., security. Security and privacy. It's also provided by the operating system. Whenever we turn on our system, what we first get is the password. So why do we put that password? So that an authenticated person who has its proper password can access it. And Windows uses the Kerberos security protocol to keep that password properly in secured files so that no one can access it. 

Other than this, in the RAM, as I told before, when we execute a process, then it gets a portion in the RAM, like it got a memory allocation `0-100`. One process is `P2`, it got a memory allocation of `101-200`. If any process calls or executes any instruction outside the given segment, then that process gets blocked instantly. Meaning, if any instruction of `P2` is accessing the data or interfering in the data, then that process gets blocked. Meaning, it's providing security in between the processes so that no process can interfere or change data with each other. So security and privacy are also provided by the operating system. I have many other functionalities here, but these are the major functionalities of the operating system. 

Here I would like to tell a last point here, that as I wrote in the diagram, user applications. What's the application here? How do we access the operating system as a normal user? If you'll notice when you turn on your laptop, then the desktop appears. That's an interface. We can go to "My Computer", can access data, can access any movie or song in C, D, or E drive. 

Now, in actual, user can either access through an application or can access direct also. We use applications in majority. Like, if I want to print a particular file, then what I do is, first I'll create that file in Microsoft Office. That is a kind of application. When we enter `Ctrl + P`, then it asks "Do you want to print?" Yes? Then you give the print command. What is that print command actually? We are accessing the hardware with it. But you are accessing the hardware through the application directly, and the operating system is working as an interface here. 

Do you feel anything like that? No. We as a user don't feel this. So it's providing such a nice interface that the user doesn't even feel when they're accessing the hardware and when not. 

Or the other way is the shell. Like if we would talk about Windows, then we can access the operating system or kernel through the command prompt. And in Linux, we open the terminal and can write the command directly in it. That's through the command prompt. We have to write particular commands in it. That's called a system call. 

The operating system basically works on system calls. We'll see this in the next video; here I'm just giving you an introduction that whenever the operating system will work, it'll work through system calls. So whenever we are giving a command through an application like print, then print will invoke a system call, and that system call is run. Same way, if you are opening a file in C or D drive with a double-click, we just opened normally with a double-click and the file got opened. But how did the operating system open that file? Through an open system call. 

Meaning, there are a lot of system calls like read, open, write, through which the operating system works between the application, user, and hardware. So these are basic functionalities or goals of the operating system that you have to keep in mind. It can be asked in normal theory, or GATE, or UGC NET. In majority, it will come in your college or university level exams. Thank you!