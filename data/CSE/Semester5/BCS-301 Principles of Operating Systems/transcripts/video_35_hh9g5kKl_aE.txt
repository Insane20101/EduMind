Let's see the solution of the Producer Consumer Problem. What is the Producer Consumer Problem? What is the cause of the Producer Consumer Problem? And what are the flaws in the Producer Consumer Problem? For this, you can check my video where I have fully explained the Producer Consumer Problem, how the Producer Consumer Problem occurs, and what problems are created—meaning inconsistency or loss of data—these are the problems that occur in the producer consumer. 

Here, we are solving that Producer Consumer Problem by using the binary semaphore, so let's see the solution. Here I have not mentioned the full code; the full code is discussed in the Producer Consumer Problem. Here we are talking about solutions: how we can solve this through a semaphore so that we can achieve synchronization. Yes, the problem of synchronization in the Producer Consumer Problem we are removing it here by using the binary semaphore as well as the counting semaphore. 

Here I have taken 2 counting semaphores: full and empty. Empty here tells me the number of empty slots. The number of empty slots in the buffer—that buffer where we produce data and where the consumer consumes data or consumes an item. So my number of empty slots is showing empty, and the number of filled slots—how many slots are already filled—that here the full counting semaphore tells me. And here one binary semaphore is taken, that is $S$, and we initialized it by $1$. 

This is the code of the producer; there are other values also, but we are just concentrating on the solution, how we can solve it:
Down(empty)
Down(S)
Buffer[IN] = item (we will put item in buffer)
IN = (IN + 1) % n
Up(S)
Up(full)

So let's solve it one by one. First let's see this case. Let's say that here, at a point of time, because already I have put 3 values here—meaning in 0, 1, 2 slots there are 3 items a, b, c already put. So here, if I talk about empty first, the value of empty here is 5, because what is empty? Number of empty slots. So how many total slots do I have in the buffer? 8. So out of those 8, 3 slots are filled, so how many are empty? 5. And full—full means the number of filled slots, meaning how many slots are already filled? I have 3. 

So if we see at this point of time that my empty are 5 and full are 3 values, so now here if the producer comes—let's say the producer comes, the producer wants to produce 1 item—so what will he do first to produce the item? Down(empty). Down(empty) means it wants to use 1 empty slot because he will put 1 item in it. So to use 1 empty slot, he will first do Down(empty) here. He will do Down(empty), so the value will become what? 4. 

Then the 2nd instruction—just give the numbering like this—2nd is what? Down(S). Down(S) means the value of the binary semaphore was made from 1 to 0. So if I write the value of $S$ here, the value of $S$ is 1. So the value of $S$ was initialized to 1, so made from 1 to 0. Successfully made the value 0, so it enters what is called a kind of critical section where we are actually executing code. 

Item P to Buffer[IN]. IN is pointing at the third location, because what does IN show here? Address of that empty slot where we can put the data or item. So here what is the value of the slot? 3. So in 3 we put item P—let's say the item name is some d. So we put d in the third value, third address, and IN = (IN + 1) % n, meaning the value of IN was 3, so (3 + 1) % 8, because n is what? The number of total slots. My number of total slots is 8, so what comes out? 4 % 8, that is 4. Means here we will update the IN value from 3 to 4. 

And after that, when this code is executed, then this is exit code. Like this is entry section code and this is my exit section code, meaning when it exits the critical section, it will execute this code. What did it execute? Up(S), meaning the semaphore value was made again from 0 to 1. Why? Because we are giving a chance or scope to other processes to execute their code, so that it should not happen that I did my work and stopped others' progress. No, here we want to achieve progress. So how will we achieve it? So that I executed my code, then I will make the semaphore value 1 so that others can also execute. Up(full)—did Up(full) means the value of full we made 4. 4 means here I have how many numbers of filled slots? 4. 

So now here, to check consistency, there is a simple example: how many empty numbers of slots? 4. And how many full? 4. So 4 plus 4 is 8, and how many total slots do I have? 8. 

Now the consumer comes. The consumer comes, the consumer wants to consume 1 item from this, so where will the consumer consume an item from? From this buffer. So first what will it execute? Entry section code. We have written 2 lines in entry section code: Down(full), Down(semaphore). Means first full—how many values are full? 4. We first made 4 down, means we made the value of full from 4 to 3. And Down(S) means the value of the semaphore we again converted from 1 to 0. 

Then it enters the critical section. Buffer[out]—what is out value? Out value is 0, means the pointer of out is pointing here. So the item here—which item is there? a. We will consume a, we will put it in item c, the variable, and Out = (Out + 1) % n, that is the value of out which was initialized from 0, we incremented it with 1. Means how did we do it? Here the value of out is 0, so (0 + 1) % 8, so 1 % 8 is what? 1. So we simply made the value of out 1 because an item is already consumed. 1 item is removed from here, so here next where will the consumer consume from? From 1. So to run it properly, we incremented Out with 1. 

Now Up of semaphore. Up(S) means we made the semaphore value 1 again, because after this, if the producer wants to come again, then we have created a scope for him that yes, he can also come again. Now, the last point is—this is the third value, this is the fourth instruction, this is the exit code—Up of semaphore we made from 0 to 1, Up(empty). Empty means 1 empty slot is increased. If 1 empty slot has increased, then we incremented the value from 4 to 5. So the consumer consumed its thing and left. 

So we saw a simple example here that if pre-emption is not occurring here—meaning if the producer comes first, then the consumer comes, or you can run it in reverse: first the consumer comes, then the producer comes without pre-emption, without context switching—if we do it, then it works properly. You can see the value of empty is how much? 5. The value of full is how much? 3. So 5 plus 3, that is 8, so the total number of slots I have is 8. And if you look carefully, then how many full slots are there? 3 full slots. And how many empty? 1, 2, 3, 4, 5. So this producer-consumer code is properly working here if we have only worked sequentially till now—meaning first the producer comes, then the consumer, or you can also try first the consumer or producer. 

But now we are checking one more example, in which we will try to pre-empt this, because where does the major problem come? When processes are executed concurrently. Means when they are executed at the same time, then it is very important to synchronize them. That's the point we will see again here. 

Let's just start again. The value is again initialized, the value is again 3. Now let's say the empty value is 5 and full is 3. Empty value is 5 and full value is 3. The producer came; he first executed Down(empty)—means executed the first line. To enter the critical section, it is necessary to execute these two lines. So it did Down(empty), meaning the value of empty went from 5 to 4. It made the value of empty from 5 to 4. After that, it came to this line, but if it executes this line—meaning the value of the semaphore which is again initialized with 1, if it makes this from 1 to 0—then it will enter, then it will again be sequential. 

So let's try to make it a context-switching kind of thing, meaning we will pre-empt this. Meaning the producer comes first, he made this empty from 5 to 4, but when it was about to do Down(S), right here the producer becomes pre-empted. Meaning the producer got switched here, and who enters? The consumer. The consumer comes. The producer becomes pre-empted here, the consumer comes. Now when the consumer comes, what did he do? Down(full). Down(full) means he made the value of full from 3 to 2. The first line he executed was Down(full), so he made the value from 3 to 2. He did Down(S), meaning he made the value of the semaphore from 1 to 0, and enters the critical section, because here the main point is actually Down(S). Because out of the two, whichever first—out of the producer and consumer, the one who first does Down(S)—will down the value of this binary semaphore, he will first enter the critical section, and when he enters the critical section, the other one cannot come. How? Let's see. 

If the consumer comes here, the consumer did Down(full), meaning he made the value of full from 3 to 2, and did Down(S), meaning he made the value of S from 1 to 0, so the consumer enters the critical section. Now what does he have to do? Item c = Buffer[out]. Meaning what is the value of buffer initially? 0. So he picked the item from the 0 location and put it in the item here, and out = (out + 1), meaning he made the value of out from 0 to 1, updated here. 

Let's see: if the consumer is at this point, then can the producer come here? This was the problem in the producer-consumer problem, that when one process—either producer or consumer—has one process running, then in between if some other process, let's say the consumer comes at the same time and he executes full, and in between if the producer again comes, then will it create inconsistency here? So here we have to stop this thing. 

If, let's say, the consumer is now running and at this time the system again gets pre-empted, if it again gets pre-empted, then you yourself check that here the producer has already executed the first line—this line it has already executed, it had made empty from 5 to 4—but can it do Down(S)? No, because the $S$ value is already 0. If the $S$ value—meaning the value of the semaphore—is already 0, then when this tries to do Down(S), the producer will be blocked here. And that's what we are doing here: that if one process is running and in between another comes, no problem, but from here again if control goes to the first one, then we will stop it here. 

So that's what we are doing here: we stopped the producer here so that he can't execute or run further, meaning if he also runs, then there will be again the same Producer-Consumer inconsistency problem created. So here, in case if the producer comes, then he will simply be blocked. So that's why, if the producer doesn't come, then the consumer will execute again. The consumer is executed up to here; after that, Up(S)—Up(S) means again he made the value of S from 0 to 1. Yes, now there is scope that now the producer can execute here. Up(empty)—the value of empty from here again updated from 4 to 5. Now the consumer is fully executed here. 

Now the consumer is executed, then control comes again to whom? The producer. And in the producer's PCB, it is already saved that he had executed instruction 1, so my control will start from where? Execution will start from where? From the 2nd number instruction. This we have already discussed many times before. 

Did Down(S), meaning it made the value of $S$ from 1 to 0. Yes, now it can successfully execute. How will it do it? Buffer[IN] means the item that it had to produce—let's say the name of that item is d—so he in the 3rd location... where is IN? What is the value of IN? 3. So he put d in buffer[3]. IN = (IN + 1)—what is the IN value? 4. The IN value is made 4 here, meaning the producer comes up to here. 

Now if you want to check, to check you can again check here: let's say that the producer gets pre-empted here, the consumer comes. If now the consumer comes here, it can do Down(full), but cannot do Down(S) because when it does Down(S), the $S$ value is already 0, so the consumer will be blocked here, so he cannot execute the critical section code. So this is the little point and this is the logic, that when one is running, then we try to stop the other one—meaning we are trying to convert concurrency into serialization in a way by using the semaphores. 

So here now the producer has already run this much. Did Up(S)—meaning when it exits, during exiting it will again make the value of $S$ from 0 to 1—and Up(full), it made the value of full up, meaning from 2 to 3. 

So if you look carefully here, what is the empty value? This has been executed fully, the consumer is also executed. If you look here, what is the empty value? 5. What is the value of full here? 3. So means the consumer consumed one item—which item? Which was at the 0th location. At the 0th location was a; we forgot to remove a from here, so we remove a, and the value of out is updated here at 1. 

So if you look carefully, what is the value of empty? 5. And what is the value of full? 3. So what is my total value? 8. Meaning there are 8 numbers of slots. And if you see in the diagram also, how many slots are filled? 3 slots are filled, 5 slots are empty here. 

So this is how actually the Producer Consumer Problem works, and this is the solution of the Producer Consumer Problem by using the binary semaphore as well as the counting semaphore. You can try to solve this using the mutex method also, or you can try to solve this using the locking method. So this is all about the solution of the Producer Consumer Problem. Thank you.