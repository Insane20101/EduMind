Let us see a question. The question will help you to understand more about the concept of paging. In the question, they have given: consider a system in which LA, that is a logical address, is of 7 bits; physical address is 6 bits; page size is 8 words. Then they are asked to calculate the number of pages and the number of frames. 

Now, let's see one by one. First of all, logical address. We already know logical address is created by the CPU, meaning CPU gives some address which we will take as a logical address, and from there we try to calculate something. What is that something? Logical address is comprised of two things: one is the page number and the other part is page offset, or we can say the size of the page. The most significant bits—you cannot change it—the most significant bits, MSB, are page number, and LSBs are representing the page offset and page size. 

Now, the point is: the logical address is given as 7 bits, meaning the logical address space is 7 bits? No! The logical address is of 7 bits, meaning the total size is represented with the 7 bits. This is a very important point. I have already written some numbers. Let's say 2 raised to power 7 is 128, meaning here 128 is the space, is the total size, but 128 can be represented with 7 bits. How? As we know, 128 means 0 to 127. In 128, how many numbers will be there? What is the range? 0 to 127. And what is the maximum number? 127. So how do we represent 127? With the help of seven 1's: 1111111. So if you convert these 1's into decimal, then it will be 127, meaning 127—how many bits do I need to represent the maximum bits up to 127? 7. So from 7 bits, I represented 128 space. 

That means here the logical address is not 7; the space is not 7. Space is actually 128. But how many numbers of bits are used to represent? That is 7. Here I can write the total number of bits representing the logical address is 7. But from those 7 bits, some bits are of the page number and some bits are of page offset. So to calculate this, I should know either of the two things. And yes, the page size is given here. Page size and page offset are the same. Page size is given 8 words. 

Now here is another small point, and that is word. Now, what is word? It is a representation. Minimum unit to represent the memory is actually a word. We always talk in our system too, like my operating system is 32-bit or 64-bit. So there, what we are representing is that actually our system, i.e., CPU, always calls 1, 2, or 3 words. I mean to say that word and byte are actually the same things. If nothing is mentioned to you, then you can simply say 1 word = 1 byte. But 1 word can have 2 bytes, 1 word can have 4 bytes also. How many bytes can be made with a 32-bit system? 4 bytes are made with a 32-bit system. That means the size of 1 word is 4 bytes. So this depends on different systems. But here we are only solving numericals. Wherever the word is given in numericals, you can replace it with byte. But always keep in mind that if nothing is mentioned there, that means 1 word = 1 byte. But if something is mentioned there, then according to that, you have to solve it further. So we can assume word and byte the same thing. 

Now here is the page size. Now here 8 is not the number of bits to represent; it is the size which is given. Meaning full space is given. So here the total size is 8 bytes. To represent 8 bytes, we need 3 bits. Here 8 is not actually the bits; this is the size. It means this is given to us. So, to represent the eight numbers, we need 3 bits. So 2 raised to power 3 is 8. So here to represent page offset, 3 bits are required. So 7 bits were total, 3 bits are taken by offset, so the remaining bit is 4. 

So here I can say that 4 number of bits are used to represent the page number. But don't be confused: the number of pages is not 4. The number of pages will be calculated as 2 raised to power 4, i.e., 16. So here we are just writing the number of bits required to represent the bits. But when we actually need space, when we have to do the actual calculation of value, it will be calculated in 2 raised to the power. So we got the answer to one question: What is the total number of pages? 2 raised to power 4, i.e., 16. And if you have been asked the number of bits required to represent the total number of pages: 4. 

Now let's talk about the physical address. The physical address is directly related to the main memory, meaning depending on the size of the main memory, how much physical address will I have? The total number of bits that will represent the size of the main memory is called the physical address. Here the given physical address is 6 bits. This means a total of 6 bits is required to represent. How much is the total space? i.e., 2 raised to power 6. Always remember these are bits. And if you want to know the space, then 2 raised to power 6. What is 2 raised to power 6? i.e., 64. 

So here I can say that what is the main memory size according to this question? Although it is not asked, but it is simple: the size of the main memory is 64 bytes, or you can say 64 words also. The total size of memory is 64 words, and to represent that, 6 bits are used. So we know it is 6 bits. 

Now here we already know that the physical address is again comprised of two things. Like here it is page number and page offset; here are the frame number and frame size, or you can say frame offset also. So frame offset or frame size is always equal to the page size. We already know that we can properly fit each page within one frame. For that, we have to take the same page size and frame size. This is a simple concept. If you go to courier any goods, so whatever goods you are going to courier, you put it in the envelope. Do you take the envelope of the double or triple size of the goods? No! We try to fit that good in the envelope so that the goods inside it shall not move. So the same concept: the size of the page we are taking is the same as frame size. So the page size is given here, so don't get confused: the size of frame size is the same. What is the size? 8. 

But 8 is size. What number of bits are used? 3. Because we represent 8 with 3 bits. And don't get confused, always remember this and this value will always be the same. If we have a total size of 6, it took 3 bits. So how many bits are remaining? 3. How many numbers of frames? Frame numbers are not 3. The number of frames is 2 raised to power 3, which is 8. So again, I got the second answer also: How many numbers of frames? 8. 

If I do simple representation here, I can show you by representing a total number of 0, 1, 2 like that up to 7. So the total number of frames will be 8. So you can give them the numbering between 0 to 7. So how many frames? 8. And here if I talk about pages, what is the total number of pages I have? The total number of pages is 2 raised to power 4, that is 16. That is a total of 16, but the maximum number is 15. When you put numbering 0, 1, 2 up to 15, that is the maximum number. So this is how we represent the number of pages and number of frames. 

Although they have not asked here, in case if you have been asked, let's say in this structure there is a page table—there is a page table. Then how many numbers of entries are in the page table? How many numbers of entries are in the page table? We already know the number of entries in the page table is always equal to the number of pages. Always remember the number of entries in a page table of a particular process is always equal to the number of pages in the process. The number of pages into which you have divided that process—the same number will be the number of entries here. 

So, how many pages do I have? 16. Total entries are 16. 16 entries will be created in the page table. In this way, 0, 1, 2 up to 15, I can represent it here. So this is all page numbers. What will be inside the page table? Frame number. If I write frame 2 here on this page table, if corresponding to page number 1, I write frame number 2, you can say that this page—this page, this page is in frame 2. I shall write frame number 2. Page of this process—not the process, but we can write the page of this process. This is how we actually represent the structure. 

Whenever CPU will demand, whatever number CPU will create, we will convert that number according to this: how many page numbers are there, and which byte number is it? Byte number of that page. Because let's say this is page number 1. This is page 1 of this process. It will tell how many bytes are on this page. In this page, 2 raised to power 3, i.e., 8 number of bytes are there. On every page, 8 number of bytes. That's why we have the total number of pages 16, and inside each, there are 8 bytes. 16 multiplied by 8—what is it? 128. This is what we have done. 

We have got LA that is 7 bits. 7 bits meaning 128. So we will represent it like this. So whatever address CPU will generate, we convert that address to its corresponding, then we will check in the page table in which frame is that page number lying. Then going inside that particular frame, then byte number. How will we know the byte numbers? This bit will always represent actually which byte—the total number of bytes is 8. But which particular byte CPU has demanded? Whatever byte will be there between 0 to 7, whichever byte I need, I will give that to the CPU. 

So this is how the actual concept of paging works. Many questions like this you will find in previous year GATE questions, and maybe next year also such questions may come. So do this carefully. Thank you.