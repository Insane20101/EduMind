Let's see one question on logical address space and physical address space. It will help you to understand the concept in more depth. 

Logical address space is given as 4GB, i.e., logical address space is 4GB, physical address space is 64MB, and page size is 4KB. We have to find the number of pages, number of frames, number of entries in the page table, and what the size of the page table is. This means we are finding the maximum facts that we can have through this question. If all these things are given to you, then how can you find values for these?

Here, the first thing given is logical address space. Actually, what is a logical address space representing here? Logical address space always represents the size of the process. This means the total size of my process is 4GB. That is what it is representing. 

Here, whenever a logical address space is given, you can first convert logical address space to the logical address. It means I have a logical address space of 4GB. How many bits do I need to represent 4 GB? This is the question. A very important point is that if logical address space is 4 GB, then how many bits are used to represent 4 GB? 

How do we solve this? What is 4? 4 is 2 raised to the power 2. Just write all the things in the power of 2. We have discussed this earlier also, that if I have 16 numbers, the range I have is 0 to 15. The maximum number in the range is 15. How do we represent 15? 1111, meaning 4 bits are used. That is what I have already mentioned here. If you have 32, then you can represent it into 5 bits. This is what I was talking about. 

Here it is given as 4 GB. So how can we represent 4 GB in bits? First, write 4 as 2 raised to the power 2. What is G? G is actually 2 raised to the power 30. This means we can write it like this. And the last is byte. No need to convert the byte. We know that a byte is 8 bits, so don't write 2 raised to the power 3 there, because memory is byte-addressable. This is already given, that memory is byte-addressable. It means we are representing memory in bytes. In case even if this line is not written, you assume here by default that the memory is represented either in bytes or in words. Most of the time it will be written in words also, and by default, the size of the byte and word is the same. If it is different, then they will also mention it in the question. 

Let's assume here that a byte is the minimum unit to represent the memory, not bits. It's the byte. So here, don't convert bytes into bits, just convert the values. Convert all the values written in front of this byte here. So how many bits are there? The base is the same, so add all the powers. That is, my logical address is made of 32 bits. The logical address is 32 bits. 

Sometimes it may happen that you will be given the logical address as 2 raised to the power 32, and you will be asked what the logical address space is. If I know that my logical address space is 2 raised to the power 32, and if I have to convert it into space—meaning what the size of it is—then simply you can write 2 raised to the power 32 as 4GB. You can convert as you like, either from space to bits or from bits to space. 

From here, I got to know the logical address. My logical bits are 32 bits. 32 bits are used to represent the logical address. That is the meaning of the logical address. Now we know that in a logical address, some MSB bits are used to represent the page number, and LSB bits are used to represent the page offset, or we can say page size. 

Now, here I have two values, and I know that the total size of this box is 32 bits. But from it, how much bit is this and this, you can't find directly. For that, you have to go to page size. The other thing I have here is the page size. The size of the page is 4 KB. Again, this is size. I also have to convert this to bits. Meaning, how can I represent 4 KB in bits? Already we have seen that 4 is 2 raised to the power 2. What is K? Always remember K is 2 raised to the power 10. So it becomes 2 raised to the power 12. 12 bits are used to represent the page offset. 12 numbers of bits are used to represent it. If you have been asked what the size of the page is, then you write 2 raised to the power 12. 2 raised to the power 12 is equal to 4 KB. This is the simple funda. 

Now you know that 32 is the total size. From that, it took 12 bits out of it, then the remaining 20 bits were left. So the first question which is solved is the number of pages. How many pages? 20? No! These are the bits. In a logical address, we always write bits. 20 bits are used to represent the pages. But actually, the number of pages are 2 raised to the power 20. Here you can make a little mistake in case this is asked in the exam. Here you may see 20 is also in the option. No! This is representing 20 bits. The number of bits is 20, but actually what is the number of pages? That is always written as 2 raised to the power of the bits. So 2 raised to the power 20 pages are there.

Next is the number of frames. How do we calculate the number of frames? Through physical address, i.e., first I have to calculate the physical address. What is the physical address? What is given here? Physical address space. What is the physical address space? 64 MB. 

Now again, I have to represent 64 MB into bits. How will you represent 64 MB in bits? First, take 64. 64 is 2 raised to the power 6. Now M, what is M? M is mega, and mega is 2 raised to the power 20. And don't touch byte. Byte will be the minimum addressable unit. Since bases are the same, the powers will be added, so it will be 2 raised to the power 26. Total size will be 26 bits. 26 bits are used to represent the physical address. And physical address is actually the size of the main memory. Here the given main memory size is 64 MB. And if you are asked how many bits represent 64 MB, it is 26 bits. 

In these 26 bits, some bits are the number of the frame, and the rest of the bits are the frame offset. Now I don't know the number of frames, nor is the frame offset given directly. So how will we find it? We know that the frame offset or frame size is always equal to the size of the page, so that the page can fit properly inside a frame. If the size of the page is given, i.e., 4 KB, then the size of the frame will not be given separately. Frame size and page size are always the same. So assume that frame size is also 4 KB. And how do we represent 4 KB in bits? 12. We have seen that before. 

So you see by yourself that if it took 12 bits from 26, then 14 bits are remaining. 14 bits. That means my total number of frames is 14? No! Total numbers of frames are 2 raised to the power 14. So this is the concept of how we actually represent the number of frames and number of pages. These values are only representing the bits. So this is all about questions number 1 and 2. 

Now we are seeing the number of entries in the page table. How many numbers of entries will we put in the page table? We have already seen the page table before. Every process has its own page table. And how many entries are there in the page table? There will be as many entries inside the page table as you have pages. Meaning, the number of entries in the page table will be equal to the number of pages in a process. 

So how many pages? We have already seen 2 raised to the power 20. So the number of pages is 2 raised to the power 20, then entries will be 2 raised to the power 20. Meaning 1 million, 2 raised to the power 20 is 1 million. i.e., there are 1 million pages and 1 million entries, because on each page table corresponding... If I try to represent a small page table here, although 1 million cannot be represented in this small place, if I represent it, then it will look like this: if this is the page table, then this is page 0, page 1, page 2, page 3, and up to page number 2 raised to the power 20 minus 1, because we are starting the index with zero. So your pages will be like this from 0 to 2 raised to the power 20 minus 1. 

Now, the last question: what is the size of the page table? How will we calculate it? Very simple. I know that in this page table, how many boxes are there? The number of boxes is 2 raised to the power 20. But what's inside those boxes? How will you calculate the size? Number of boxes or values into size. This means, what is inside this box? And what do we put inside each page table? What will be represented inside each page table? 

Here we represent the frame number. Let's say page 0 is in frame 3. Let's say page number 1 is in frame number 10. Page number 2 is in frame number 20, because paging is non-contiguous. A page can be anywhere within a particular frame. But pay attention that I am representing the frame number here. This means in every box, we are representing the frame number. And to represent the frame, we need 14 bits. My values will be 14 bits, i.e., that page would be anywhere in the entire number of frames between 0 to 2 raised to the power 14 minus 1. 

So here, how would I represent frame 3 in 14 bits? In this way, the value will be written here. If I ask where is page 1? It is in frame 10. How would we represent it? We need 14 bits, because the number of frames I have is 2 raised to the power 14. So if I have to address 2 raised to the power 14, how much address is made? We need 14 bits. So how will we write 14 bits? This is what we are representing. 

That means in each box we are putting 14 bits. 14 bits in each box. I will put 14 bits as many times as there are pages in it. How many pages are there? 2 raised to the power 20 pages. And in each of the 2 raised to the power 20 pages, there are 14 bits. So this is the size of the page table: 2 raised to the power 20 number of pages are there. What are you writing in every box? The number of frames. And how many bits is it taking to represent each frame? 14. So 14 bits here, then 14 bits here also. So how many times are 14 bits used? 2 raised to the power 20. If you do 2 raised to the power 20 multiplied by 14, then you get the size of the page table. 

So here, the important point is: if we want to get the size of the page table directly, then we can take it out like this. But sometimes there are not only frame numbers inside the page table. Apart from frame numbers, extra bits are also there, which are representing some other important information, like whether the page is protected or not, or the data available on the page is modified or not. We will see all about this in the next video—what actually happens in every single page table entry. One thing we know is that the page number will be there definitely, then only we get to know in which frame that page number is present. Frame number will be there, but apart from frame number, what else bits will be there? That is what we have to check. Clear? 

So if nothing is mentioned, then directly take out the number of bits of the frame, then multiply it directly.