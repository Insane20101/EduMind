Dear students, welcome to Gate Smashers. In today's video, I'm going to explain the Test and Set instruction. 

Look, in process synchronization, to resolve the problem in the critical section, the next method we use is the Test and Set instruction. In the last video, I told you about the lock variable. The lock variable works fine, but what is the problem here? The problem I have told in the last video, once again I'll quickly tell that. Let's say there is a process P1 and process P2; two processes are there. P1 comes first, it runs line number 1. At the start, the value of lock is what? 0, which means the critical section is empty. P1 comes, it sees `while(lock == 1)` becomes false, and it will come out from here after becoming false. P1 executed line number 1, after executing it will execute line number 2, then it will come to the critical section. 

But the problem that occurred was that as soon as it executed line number 1, it checked the value of lock: `0 == 1`, false. On coming out from here, what did it become? Preempted. Let's say we made it preempted. This case is made intentionally so that you come to know what was the problem in the lock variable. So look, P1 executes the first line and gets preempted. P2 comes. P2 also executes line number 1. What is the value of lock? Still 0. Still there is no change in the lock value because line number 2 was not executed. So P2 executes line number 1: `0 == 1`, becomes false, came out. As soon as it comes out, `lock = 1` was done. When lock was made 1, from 0 to 1, who entered the critical section? P2 entered the critical section. P2 enters. 

Now look, P2 is now already in the critical section. Now P1 again woke up—let's say it went to the washroom after executing line number 1, and now it came back. When it comes back, it will not restart again; it has already executed line number 1 and it has to directly execute line number 2. As soon as it executes line number 2, what does line number 2 say? `lock = 1`. Lock is already 1; it was overwritten again by 1. No problem, the value of lock is to be made 1, then the value of lock whatever it is we made it 1 again. Whether it was 0 or 1, we again cut and made it 1, and who entered the critical section? P1 also entered. Getting the point? Now this way you can enter P2, P3, P4, P5, as many as you want, in the critical section. Meaning, mutual exclusion is not there in it. Getting the point? 

So what actually is the problem here? You just have to reach the root of the problem. What is the problem? The problem is that between instruction number 1 and number 2, preemption occurs. Preemption is occurring between numbers 1 and 2. So the next upcoming Test and Set instruction, what did it do? It combined these two, meaning it made these two instructions atomic, combined them, and made it this: `while (test_and_set(&lock))`. 

Now look, the critical section was at line number 3; the lines before it were combined and it made one instruction here, that is Test and Set. Here we used hardware and we made a special instruction which tests also—meaning it is being tested here whether the value of lock is 1 or 0—and also sets it—meaning the value of lock is to be set as 1. These two tasks are done by one instruction so that preemption does not happen in between and we achieve mutual exclusion here. This is the root of the story. 

Now look, now we come to Test and Set. Look at the structure: `while (test_and_set(&lock))`, critical section, `lock = false`. The same true or false—meaning the value of true is 1 and the value of false we take as 0. It is the same story, simple. 

Now look, what is this? It is a function, the Test and Set function. Who is doing this function? Because `while` works on true or false, if there is true in `while` it will perform according to that, if there is false in `while` it will perform according to that. But what did we give in `while`? Calling of a function. Now where is the definition of this function? Here is the definition of the function. So now we will discuss a little bit about this. It is very simple. 

First of all, let's say again the same, there is P1 and P2. First came P1. Who came first? P1. And initially, like we made the value of lock 0, what do we make the value of lock initially? 0, so that we can tell that the critical section is empty. Now here we made the value of lock initially as false (0 means false). 

Now look, `while(test_and_set(&lock))`. Let's say P1 came first, and when P1 comes, Test and Set means this function will be called. After this function is called, our control will be transferred there. So, `boolean test_and_set(boolean *target)`. What is this here? What did we do actually here? What is `*target`? It is a pointer variable with the name of `target`. I have written Galvin's code so that in your competitive exams, college, university, wherever it is asked, you can answer this easily. 

We took lock with an ampersand; we are using call by reference, not call by value. So in call by reference, what do we give? We pass the address. What was the initially taken value of lock? False. Its address—for example, I took 1000—means now we will pass 1000. So look, `boolean *target`. In this `target`, what will be the value? 1000. Means `target` is pointing at this false value, meaning `target` is pointing at the value of lock. Getting the point? 

Now look at the simple code of C, working on call by reference. We put the value of `target` in `r`. Now look, `target` is the 1000 address; we put a star ahead, meaning the value `target` is pointing at what? 1000 is pointing at what? False. So we put false, we take an `r` variable, so initially what do we put in `r`? False. Okay? What was it at the start? The initial value of lock was false, so what comes in `r`? False, because we have to return the value. We have to return it in the `while` loop. 

And what did we do to `target`? We made it true. Meaning what is `target`? 1000. Before that is a star, star means what is the target pointing to? Lock. So in that, we made true in place of false. The same: we did test also and set also. To set means to make true. So we made true also and checked it also, and finally, what did we do? Return `r`. Look, what was the value of `r`? False. So what will be returned here? False will be returned. 

Same as we do here: what was the value of lock at the start? `0 == 1`, false, meaning coming out in the critical section. The same task is done here: it became false, coming out, who entered the critical section? P1 entered the critical section. It's very easy, okay. 

Now what do we do? Let's say P2 comes. P2 says, "I also want to go." So as soon as P2 goes, it will again run this. As soon as P2 came, P2 again ran Test and Set. Now look, lock comes in here, we come to this code, we came on function calling, so from function calling we reached the definition. 

Now look, here what is the value of lock? It has become true because P1 is already in. Now look, what we have to do: `*target`. `target` is pointing at what? True. So we put the value of true in `r`. What comes in `r`? True. Okay. And what did we do? `*target = true`. Meaning what is `target` pointing at? Lock. We made it true. This was already true, so no problem. We again overwrote true and let it be true. So this is true, and we returned `r`, meaning the true value of `r` was what? True. We returned it. So look, now here what will be returned? True. 

So `while(true)` means that this will be trapped in this itself. Because there is a semicolon, so this will be trapped here in an infinite loop. So P2 will not be able to enter, so you have achieved mutual exclusion. Getting the point or not? 

Now it comes to your mind that, sir, now we cannot do any preemption because both these tasks—the preemption occurring here in between before—now we combined both these and converted them into one instruction, and through this we achieved mutual exclusion also, and progress is also achieved. What progress means is that if the critical section is empty, and whoever wants to come can easily come, nobody will stop it if the critical section is empty. So here mutual exclusion is there, and progress too is achieved here by default. 

So this is the basic funda of Test and Set. Generally, what happens when we often read from books is that we don't understand in one or two tries. So when you check this video and then after that read from the book, you will feel very easy, that this is a very simple concept. Thank you.