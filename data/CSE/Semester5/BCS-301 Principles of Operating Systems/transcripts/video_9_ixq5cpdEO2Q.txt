Hello friends, welcome to Gate Smashers. Today's topic is fork system call. We're going to discuss about fork system call in this video, and at the end of this video, you'll get two questions based on everything that we're going to discuss in this video, whose answer you can share with me in the comment box.

First, we are talking about fork, which is our system call. We use fork system call to create a child process. Meaning, let's say there's a process—let's say I wrote a program in C: `printf("Hello World")`. So `printf("Hello World")` is a main program which we can call a parent program. But in the parent program, if I would write `fork()` anywhere, then a child of that parent will get created. 

Let's say I want to place this marker from here to here. Let's say I'm the parent process. If the parent process is busy somewhere, then we have to create a child process. Child process means it'll be my exact clone. A child process will be the parent's clone, which will have its own ID—meaning child has its own ID and parent has its own ID. And that child will pick it up from here and will place it here. 

Or we have one more method: we can use the method of threads. What we do in threads is we don't have to make a full clone. Let's say both my hands are busy, then I can create a third hand here. Third hand means that the rest of the area is doing sharing, meaning maximum things are getting shared, just an extra thing is performing this task. But when we do fork, then everything is new. Although we are doing a little bit of sharing there also—we are also sharing code—still it has its own process ID, it has its own registers, there are a lot of values that it does separately. That we'll discuss in the video where we'll talk about differences between fork and threads. 

But here we're talking about what a fork system call is. A fork system call simply means to create a child process. So the moment we create a child process, what happens is, the fork system call generally returns a value: 0, +1, or -1. You can say +1 or a positive value also. Zero means child process; zero denotes child, it returns a child value. Positive means that process itself, which you can call the parent process. We denote -1 if the child process didn't get created because of some reason—let's say the operating system and kernel are busy, so the child process didn't get created. But we do not consider this case because we are assuming that the child process will surely be created. Meaning you just have to keep only two values in mind: zero denotes the child process, and a positive value denotes the parent process.

So let's say if I would write here, if we write a simple program: `main()`. If I write in `main`, `fork()`. And in the fork system call I'll write `printf("Hello")`, `printf("Hello")`. Now what is the meaning of this? The moment I wrote fork... let's say if I won't write fork here, then `main()`, `printf("Hello")`, so "Hello" will get printed once because this is a parent process. You can call the main process a parent process also. That process will get executed and it'll print "Hello" on the monitor. 

But the moment you wrote fork here, fork means... if you wrote fork inside the parent process, then a child process of the parent process will get created. Meaning it's already a parent, and a child process will generate and run in parallel and concurrently. So the child process will create, which I'm denoting as `C1`. This child process and parent process will execute in parallel. Meaning, how many times will "Hello" print here? Two times. Reason: the child process will print "Hello" and the parent will definitely do it. So "Hello" will print 2 times here. 

So that's the important point: whenever you write fork and we are talking about child processes in fork, you can denote the child with 0 and we generally denote the parent with a positive number. 

If I would write two times fork here... let's say if I would write the fork statement two times. Writing the fork statement two times means that one is the parent process—that's the complete main process, that's the general process or main process. The moment I wrote fork in that parent, then fork means the parent got a child. But the parent is still there, it didn't get killed, it's alive. A child process concurrent to it has created here. Now, the next fork—this was the output of writing the first fork. But the moment you wrote the second fork, the second fork means that this `C1` is like a parent for the second fork. Parent means `C1` will create a further child process `C2`, and `C1` is there already. And this parent `P` will also create its own child process—let's say `C3`—and `P` is already there. So it means `C2`, `C1`, `C3`, and `P`, all four will print "Hello". Meaning these four are running concurrently, out of which these three are child processes and one parent process is also here. So there are three children and one parent. 

This is a very, very important point. And talking from GATE and UGC NET points of view, or any other competitive exam in computer science, fork in operating systems is a frequently asked topic. Many times questions come on fork.

Let's check once more. Let's say if I take fork three times here: `fork()`, `fork()`, `fork()`. And below it, let's say I wrote `printf("Hello")`. So what it means is it's a very simple thing. Again, what we did here: `P` is a parent process. The moment I wrote fork in the parent the first time, then a child process of the parent, i.e., `C1`, got created, and the parent is already there. Now in the child, I wrote fork once again. The other fork will create a child of this child, i.e., `C2`, and `C1` will already get executed. Now this `P`, which was running parallel to it, this fork will create another child of that `P`, let's say `C3`, and `P` is already there as we saw. 

Now when you'll write fork a third time, what will happen is this `C2` will create its child, let's say `C4`, and `C2` is already there. This `C1` will create its own child process, let's say `C5`, and `C1` is already there. Now `C3` will create its own child process, let's say `C6`, and `C3` is already there. And this `P` will have its own child, let's say `C7`, and `P` is already there. 

So if we'll talk at the leaves, then 7 child processes are there and one parent process that was the main program. So there's one main program that you can call the parent, and 7 others are children. So how many times will "Hello" print? It'll print a total of 8 times. 

So how can you calculate the total times "Hello" will be printed? $2^n$, meaning the number of times fork is written, that's the number $n$. And many times they ask how many child processes will be generated. The total number of child processes are $2^n - 1$. 

You can see when we used 3, then these 7 are child processes from `C1` to `C7`. But `P` is the parent process. Here, if we'll say we wrote fork two times, so $2^2 - 1$, i.e., 3—`C1`, `C2`, `C3` which are children and `P` is the parent. But totally, how many times will "Hello" get printed? It is $2^n$, meaning total execution will be $2^n$ and total child processes will be $2^n - 1$. 

This is a very important point, guys. Here first you have to remember this very important point in the video. Second, it always returns the ID of the child process. The moment you'll write fork, it'll return the child process's identity as zero. We write the parent process's ID as positive. You can get it as positive 1 at many places, then it's fine. Here the exact number is not important; here it's positive, negative, or zero. So negative means the child didn't even get created. Don't consider this case because we are using fork, so the child will definitely get created. But in the worst case, it may be possible that the kernel is busy somewhere. If there is any problem or error, then maybe it won't create a child process. But if this comes in a question, then they'll mention it definitely. And in actual practice, we use fork to create a child, so in this type of question, you never have to use -1. 

Just remember these main two values and these two formulas. Over this you'll get many questions that you can try; just by putting direct values, you'll easily get the question solved. 

So guys, if you liked this video, then please hit the like button, share it as much as possible, and please subscribe to my channel. Thank you!