Hello friends, welcome to Gate Smashers. In this video, we're going to discuss differences between process and threads. 

We can consider a process as a heavy-weight task, and in its contrast, if we'll talk about threads, then it's a light-weight task. First, let me tell you what environment we are talking about. We're talking about a multi-processing, multi-tasking environment, meaning multiple processes or tasks are running parallel at the same time. Now to perform this type of environment, either we need multiple CPUs, but generally we have just one CPU in our system. So how should we perform a multi-tasking environment with just one CPU? Either we can create multiple processes and keep running them, or we can use a multi-threading environment. 

And where do we generally use a multi-threading environment? Let's say if I'm executing multiple processes and all those processes are running differently. If all those processes are working differently, then it'll be a different scenario. But if the same type of process is running many times, let's say if I'd talk about the print server. Print server means multiple commands are coming from different clients. I'm getting requests from different clients to print some pages, so here, the same code or process that I have is executing multiple times. So, to execute this type of environment, what we can do is, we can create more child processes in a process, or we can perform a multi-threading environment. 

Let me explain with a real-life example what we have to do. Let's say I want to perform a task. I have to shift this duster from this place to here. This is my process or my task. Now, to perform this task, what I can do is, let's say if I'm a CPU or processor, then I can create a new process, and that process, i.e., a clone process of mine—if my hands are busy, then my clone can pick the duster and place it from here to here. Otherwise, rather than making a whole clone, I can create an extra hand, and that hand will place it from here to here. 

What I mean to say is, when we generate a child process, generally what a process includes is: a process includes data and code. Data means it extracts multiple files from the data, so you can say data or files also. Other than this, we have a stack, registers; all this is included in a process. Now when we create a child process of this process, then it is an as-it-is copy of this process. That's called a clone, meaning it has its own data, and the data is the same as here, but it'll create it again. It'll again use that code independently, and it'll use its own stack and registers, whether it's program's or input-output registers. 

Here, whenever we generate processes, then we use a fork system call. In a fork system call, what we do is we create a child process of an existing process, and that child process is its as-it-is copy. But in a thread, if we are creating a thread of a process or dividing a process into multiple threads, then what'll happen is all threads will share the same data and the same code. Meaning, here there'll be no overhead. Here there is overhead, because multiple copies of that same data are getting created, multiple copies of that same code are getting created, and there is multiple usage of stack, registers, etc. But here, code and data will be as it is same, but with its own stack and registers. If I'll create another thread like this, i.e., T2, then it'll have its own stack and registers. This is the way how it'll perform here. 

So here I'm explaining the basic difference between them. First, I'm writing in process that system calls involved in the process. Fork is a system call which is used to generate a child process, and that child process is a clone, meaning a complete as-it-is copy. So this was a system call here, meaning we are using kernel. With the help of the operating system, we are generating a new process through a fork system call. But no system call gets used in a thread. A thread is an application program that creates multiple threads. So you can also say we are talking at the user level end. Threads are generally of two types, i.e., user level and kernel level, so we are talking about user level threads. So it's the user's or application's responsibility to create multiple threads. There'll be no role of the kernel or operating system. 

And that's leading to even more differences that I'm telling you here. The operating system treats different processes differently. As I'm saying, when we're creating a child process here, then the operating system will take it as two processes. Both of their process IDs will be different, but their parent ID will be the same. The operating system will consider both of these processes differently. But in the case of threads, the operating system is considering this whole process as one. This is a major point. 

Different processes have different copies of data, files, and code. As I said just now, the more child processes you'll keep creating—and its formula is $2^n - 1$—this many child processes can be created. The more times you'll write fork, the more it'll create. If you'll write fork 3 times, then $2^3 - 1$, so 7 child processes will be created and one parent, so there will be a total of eight processes. So all of them will have different code and different data files. So you can see how many overheads are getting created here. So that's the point that I'm saying here, that they'll have different copies. 

But here, a thread shares the same copy of data and files as well as codes, but their stack and registers will be different for each of them, and they don't create too much overhead because it is very fast. Here they are sharing memory and addresses, but here they are using different memories and addresses. 

Then, context switching is slower. These points are related to each other; you don't have to remember or cram these points. If this concept gets clear to you, then everything is clear. Context switching means what we use is a time-slicing environment. Time-slicing means time quantum, meaning we run both of them and keep switching it after milliseconds. Meaning, when we use this type of environment, let's say I run this process or executed it just a little bit, then I switched to this process. So when I'll switch here, then I have to save old values in the PCB, i.e., Process Control Block, where I have to save a lot of values. And here the process is taking help of the operating system in doing this. So because of this, context switching takes too much time as compared to here. There's no switching here majorly; you can say there's a little bit switching of registers and addresses, which is not very time-consuming and expensive. But this is very time-consuming. Here you have to save whole values first and switch to another level, save it there, and then come back to the first level. This is a very, very important point, and many times questions have arrived on it in exams asking where context switching is fast and where it's slow. 

Then, blocking a process—here I'm star-marking these two points. These are very important points. Blocking a process will not block another process. Why does a process get blocked generally? Because while executing in the CPU, if a process asks to access a hard disk, file, printer, or any other hardware, so when they do the input-output request, then the kernel says to block it for some time, and when it'll get that input-output, then it can come back in the ready state again. So when it'll get blocked here, then it doesn't mean that if the parent got blocked, then the child will also get blocked. The child will keep moving because it's independent; they both are independent. 

But here this point is very important: if I'm blocking this thread—I already told the meaning of blocking a thread. If it's requesting hardware or input-output, the moment it'll request, the request will reach to the kernel, the operating system which governs all hardware. But the kernel doesn't even know that some threading took place here, because threading is the responsibility of the user or application. So the kernel will take it whole as one process, so it'll block the process. So due to it getting blocked, T2 and T3 also get blocked. All the threads in that process will get blocked. So this is a very important point: blocking a process will not block [others], but blocking a thread in a process will block the entire process. This is a major point. So this is independent, and this is interdependent with each other. The reason is because they are sharing memory and addresses. 

So these are the major points between processes and threads. You can bring out some more points from these if you want, but these are major points and will help you a lot in all competitive exams. 

So guys, if you liked the video, then please share it as much as possible, subscribe to my channel, and please like my videos. Thank you!