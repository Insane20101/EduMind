Hello everyone, welcome to Gate Smashers. Today we are going to discuss the topic Semaphore. In GATE and other competitive exams, many times questions are asked on semaphore. What actually is a semaphore? It is a method or a tool which is used to prevent race conditions. When we synchronize processes or run multiple processes at a time, especially cooperative processes in between them something is common. If so many processes are run at a time, then a race condition can occur in them. Due to race conditions, loss of data, deadlock, and many problems occur. Now to remove that race condition or we want to prevent it, one of the methods for that is the semaphore. 

Now, we start with the definition: a semaphore is an integer variable which is used in a mutual exclusive manner by various concurrent cooperative processes in order to achieve synchronization. So what is a semaphore? It is first of all an integer variable. It is an integer variable and to this integer variable, whatever the value in this, that value is used in a mutual exclusive way using many different processes, and ultimately what I want to achieve is synchronization. 

Now, semaphores used are of basically two types: Counting and Binary semaphore. Counting semaphore means what? In which the value of this integer variable can vary from minus infinity to plus infinity, and in Binary there are only two values, like you know, 0 and 1. 

So first we talk about counting semaphores. Before starting counting semaphores, we use many different operations in synchronization. Where do we use operations? Like in our critical section. In the critical section, basically common code—meaning between multiple processes whatever is common—that common piece of code we keep inside the critical section. Now, multiple processes, whatever number of processes, all of those try to use the critical section; meaning they want to enter the critical section. But to enter the critical section, they have to first run entry code, and when they complete their work in the critical section, they have to run the code of the exit section. After executing this thing, their processes can be terminated. 

Now, in the entry code and exit code, there are various operations. What are these operations? These operations we are going to use in semaphore. The operations are P() and V(). There is one more synonym of P which we use, that is Down, and for V we use Up. And one more is Wait, and there is Signal. With Signal we use Post sometimes, Post or Release. 

We use these operations—P(), Down, Wait—in the entry code. In the entry code we use either the P() operation (corresponding to P() we use V()), corresponding to Down we use Up, corresponding to Wait we use Signal or Wait-Post or Wait-Release. 

Now, what is the meaning of P() and V() or Up and Down? I have already written this: what is the meaning of Down and what is the meaning of Up. 

Now let's say that the critical section is initially free, completely free, and multiple processes want to enter into the critical section. To enter the critical section, they have to first execute the entry section code. What is the entry section code here? Down. As I have already told you, Down, P(), or Wait, these three are synonyms. You can use any of these three; meaning if I write P in place of Down, it doesn't matter, it's the same. Or instead of P if I write Wait, instead of Down if I write Wait, that doesn't matter, these are the various operations. When we do coding in C, or we can code this thing, at that time we use any of the operations out of these operations. 

Let's say, first of all, Down. Down means the entry section. Now, in the entry section, what we do in Down: first of all, S, S is what? A semaphore. It is an integer variable, let's say S, A, B, C, whatever variable name you want to give, let's say the S value is the semaphore value. Now this value in the counting semaphore can vary from minus infinity to plus infinity. 

So: `S Value = S Value - 1`

Let's say the semaphore value is initialized with 3. You can initialize with any value; let's say it is initialized with 3. Now when a process comes, let's say P1 comes. P1 wants to enter into the critical section, but for P1 to enter the critical section it has to run the entry section code. What is the entry section code? Down. When it runs this Down code, then what is the first line? `S Value = S Value - 1`. Meaning the value of 3 will be reduced to 2. Successfully the value has been reduced from 3 to 2. Now check if S Value is less than 0? No. So we don't need to use this. If S Value less than 0 was true then we would execute this, but it is false; so this process P1 will easily enter the critical section. 

Let's say one more process, P2. P1 is already there in the critical section; it is already sitting in the critical section. Let's say P2 comes. Now P2 also has to run the entry section code to enter. What is the entry section code? Down. Now to run Down: `S Value = S Value - 1`. What is S Value now? 2. So from 2, the value becomes 1. One value was reduced. Now check if S Value is less than 0? No, S Value is still 1. So this line was not executed, so it will exit, so will return. Return means this P2 also successfully enters the critical section. 

Now let's say a third process, P3, comes. P3 also wants to enter, so it will also run the entry section code: `S Value = S Value - 1`. The value of S will be reduced to 0. Now check if S Value is less than 0 (whether 0 is less than 0)? False. So this line will again not run, therefore we directly exit; meaning P3 is also successfully inserted in the critical section. 

Now you might be thinking that we are talking about mutual exclusion—that multiple processes will use it one by one. Yes, they will use it one by one, but we are just for the sake of example, showing how the Down code is executed, for that I took the value of S as 3. If you take the value of S as 1, then you can simply say that only one process can enter at one time. It is just for explanation of how we execute Down. 

So now let's say we continue this. One more process comes, P4. When P4 wants to enter, it will also run the entry section code: `S Value = S Value - 1`. But now see, what is the S Value? 0. So from 0, what does the value become? -1. One value was reduced. But now we will check if S Value is less than 0 (meaning what is the value? -1). Whether -1 is less than 0? True. True means this line will run: put the process (PCB) in the suspended list, that is called the Sleep operation. Meaning we will not bring this process, this P4 process, into the critical section. Its PCB—PCB is what? Process Control Block, where all the information of this process is stored, its process ID, list of open files, priority, and all—all that information related to this process, all of that we will store where? Directly put it in the suspended list, or we can say we have blocked P4. Meaning there is a block list here, a queue is there, in that we put P4 because we could not successfully execute P4 or put it in the critical section, so we will block it. Or in technical terms we call it Sleep, meaning it went to sleep, went into the blocked state. 

Similarly, if one more process wants to try, let's say P5 also wants to try. If P5 tries, then again: `S Value = S Value - 1`. What is S Value? -1, again it will be reduced to -2. Check: if S value is less than 0, is -2 less than 0? Yes, -2 is again true, that is less than 0. So put the PCB of this process into the suspended list again. Meaning we will block P5 also. So this is how we execute the Down operation. 

So if I take a simple value, let's say that the semaphore value at present is -4. If at present the semaphore value is -4, then what can we assess? The answer is that at this time there are 4 processes which are sitting in the blocked state; meaning there are 4 processes which have been suspended or sitting in the blocked state. 

If I make S Value 0—let's say S Value is 0—what is the meaning of S Value 0? Meaning further, any process, if S Value is 0, then any further process cannot enter the critical section. This is the last value because after this, if we make the value -1, then definitely the -1 value is less than 0, this line will be true, and that process will go into the suspended list. So if S value comes to 0, then this is the last value; after this, no process can enter the critical section, so they will all go into the blocked state. 

So some processes enter the critical section, some went into the blocked state. Now, these blocked processes—how do we unblock them? What we do after this, when P1, P2, P3—let's say that P1, P2, P3 are already sitting in the critical section—when some process comes out of the critical section, then it has to run the exit section code, or what is called executing the exit section code. 

Let's say that if P1 says, "My work is done, now I want to exit the critical section," let it exit, no problem, but it will have to run the exit code. What is the exit code? This Up. At exit, this Up code will be executing. What can we call Up? Up means V() or Signal or Post. You can call it anything—call it V(), call it Signal, call it Post—all these are like synonyms. These are all synonyms. So when a process exits the critical section, then it has to run this code. 

Now what is the meaning of this code? 
`S Value = S Value + 1`

Now what was the current scenario? The current scenario is, we are moving further with the same example; let's complete this example. In the example, we had successfully inserted P1, P2, P3 in the critical section, but when P4 and P5 wanted to come, they both went to the blocked state, and this semaphore value, the current semaphore value going on, is -2. I'll take this example so that you will get clear on the whole example when we do from start to end; after that we will discuss one or two more examples. First we will complete this example so that you will know all the steps of how a particular Down or Up works. 

So at present, what is the value of the semaphore? -2, and there are 3 processes in the critical section (P1, P2, P3) and there are 2 processes in the blocked state (P4 and P5). As I have already told you, if the semaphore value is -2, what is the meaning of this? Two processes are in the suspended list. So obviously, there are two processes, P4 and P5. 

Now we are saying that P1 says, "My work is done, now I want to exit." Let it exit, but it will have to run this Up code. When it runs the Up code, let's say semaphore S, S Value is the same variable: 
`S Value = S Value + 1`
What is S value? -2. From -2 we made one value up, one up, and the value becomes -1. 

Now check: if S value is less than equal to 0? Is the value of S less than equal to 0? Yes, because what is S Value at present? -1. Is -1 smaller than 0? Yes, -1 is smaller than 0. If yes, it is true, then this line will execute. What is this line saying? Select a process from the suspended list and wake up(). Meaning, whatever processes are lying in the blocked list, out of those, pick one process and wake it up. What is the meaning of wake up? They are lying in the blocked state; now from the blocked state they cannot go to the running state directly, so they have to be put in the ready queue first. So what does it mean to put them in the ready queue? We are waking them up, making them active. 

Now you might be thinking that out of P4 and P5, which one will we wake up? You can pick anyone, but FIFO—we generally follow FIFO. So what is First In? P4. So First Out should be what? P4. So we will pick P4 from the suspended list and we will wake it up. So I picked P4 from the blocked list and made it wake up. Wake up doesn't mean that we put it in the critical section; now it can try again. P4 can try again to enter the critical section. 

Now let's say once again we'll run the code. Let's say that P2 also says, "I want to exit the critical section." No problem, P2 can also exit the critical section, but when it comes out, then it has to run the exit code, and what is the exit code? This Up. 
`S Value = S Value + 1`
What is the S value? -1. We made it up, so it becomes 0. What is this value? 0. 

Now see if 0 is less than equal to 0? There is an equal to here—there was no equal to before, but there is an equal to now. 0 equal to 0, true. Meaning: select a process from the suspended list and wake up. Meaning, one more process that is there, pick it up from here and make it also wake up. So what was the other process? P5. So we will bring P5 out from the suspended list, and again, it doesn't mean putting it in the critical section; it can now try—meaning it is now awake, in the ready queue, and now it can try to enter the critical section. This is the meaning of how we actually execute Down and Up. 

Let's say now P1 and P2 both have exited the critical section, so I will erase this from here. But still, what is there in the critical section? P3. Let's say that now P4 says that I want to enter. P4 wants to enter the critical section because now it has come out of the block list, so it can try. But let's say that on the entry section when it runs the code—which code will it run? Down. So when it runs the Down code, let's say: 
`S Value = S Value - 1`
What is S value? -1. Put the process in the suspended list if the value of S is less than 0—yes. So P4 again will be suspended because already a process P3 is in it, P4 now again cannot come, we will have to suspend it again. 

Let's say our value is still 0. Now P3 says that I also want to exit the critical section. No problem, let it exit. It will run the exit code. What is the exit code? This one: 
`S Value = S Value + 1`
Meaning the value of 0 will be updated to 1. If S value—what is S value? 1. 1 less than equal to 0? False. So this line will not run; it will directly execute, meaning it has come out, it has exited, and the value of S is now updated by 1. 

Now, what is the meaning over there? If the value of S is 0, and if we are running the Up operation, the value of S means that... because if the value of S was -2, -3, -4, then they would be suspended. If you are asked a simple question—that the value of S is 0, the value of the semaphore is 0, how many processes are in the suspended list?—because when that value becomes minus, then it gets suspended. But the value of S is now 0, so how many are there in the suspended list? None. So if there is nothing in the suspended list, then obviously this code is not required to run. You can directly make the value of S from 0 to 1 if you are running Up and no process is in the suspended list, so no need to wake them up. So this is how we actually execute Down and Up. 

Let's see one more question. At any point in time, the semaphore value is 10. If I now ask you, how many processes can enter into the critical section successfully? The value of the semaphore is 10; how many processes can successfully enter into the critical section? 10. Because the value from 10 is 9, 9 to 8, 8 to 7, 7 to 6, so it will go up to 0. So all these 10 processes can enter the critical section. Now when the value will go from 0 to minus, then they will automatically be suspended.

If the value of S is 10, then how many processes can come? All 10 of them can come into the critical section. If the value of S is 10, then how many successful operations can be performed? That answer is also 10. Successful means if a process is inserted in the critical section successfully, then it is called a successful operation. But if it is blocked, then we call it an unsuccessful operation. 

For example, if the value of S is 0 and now we perform a Down operation—Down means what?—we will make the value of S as -1. Is -1 less than 0? Yes. Then the process gets what? Suspended. In this case, how many successful operations? This is not a successful operation; this is an unsuccessful operation because successful means that a process is entering the critical section, whereas unsuccessful means it was blocked. 

Now, if I take the value of S as 10 at any point in time, and after that, if I perform 6 P operations and 4 V operations sequentially—the initial value of S is 10, and after that, I am performing 6 P operations then 4 V operations—what will be the final value of the semaphore? This question has already come in GATE, quite some time before, but still, it is important. What to do? Nothing. What is the current value of the semaphore? 10. Then you run 6 P operations. P means Down, meaning how many times you did Down? 6 times. This means 6 processes must have come, so for those 6 processes, Down was run 6 times. Down means what? 6 times they reduced the value. So what is the value? 4. And then 4 V operations were done, meaning 4 times we made the value Up, meaning plus 4, so the final answer will be 8. This is how we have to solve this simple, very simple question. 

Let's say the S value is 17, and then I am performing 5 P operations, 3 V operations, and 1 P operation in sequence. With the S value at 17, if I perform 5P, 3V, and 1P, then what is the final value of the semaphore? Simple. What is the value at present? 17. After that, 5 processes may have come; those 5 processes successfully did their work and entered the critical section. P means what? Minus 5. After that, 3 Up operations were done. Up means what? We have added to the S value. How many times did we add? 3 times. After that, again, 1 P operation was done, meaning the value was decremented. What is the answer? 17 minus 5, that is 12; plus 3 is 15; minus 1 is 14. So the answer will be 14. 

This is how these simple questions have come in GATE. You have to remember only two things in this whole thing: one is what the P and V concept is—with the P value we make it Down, and with V we make the value Up—and this is how we have to solve the question. 

Thank you, guys. If you like this video, then like it and please subscribe to my channel. Thank you.