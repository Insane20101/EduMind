Welcome to our channel! I am Naina and in this video, I am going to explain the types of arrays to you. In this video, I will discuss the types of array. There are two types of array. The first is One-dimensional array, and the other is Multidimensional array. In one dimensional array, we use a single dimension to access the data. In the last video, the example that I mentioned in the introduction part was about one-dimensional array only. When there are multiple dimensions, it is known as multidimensional. Multidimensional means array of arrays. 

First, we will understand how to represent one-dimensional arrays. Once I set a base, then we will move forward to multi-dimensional. First of all, let's define a single-dimensional array. 'a' is the name and its size is 5. To represent it, let's create 5 locations. 1, 2, 3, 4, and 5. We gave an index to it. 0, 1, 2, 3, and 4. We named it 'a'. If I want to access the second element I will access it simply by a[1]. It will access this location. 

Now let's move toward the multi-dimensional. We also did this in the last video. In multi-dimensional, we will increase the dimensions. In the 2d array, there will be a dimension for the rows, and another for the number of columns. You must have seen a simple 2d array. In the excel file, we add the data in the form of rows and columns. Apart from this, the data is also entered in tabular form on the computer screen. We can't see it, but the screen is divided in the form of rows and columns only. Apart from these, you must have seen matrics. Matrics also work as a 2d array. 

Let's define the 2d array. We took the number of rows here. And we took the number of columns here. Here index. To represent a 2d array, we use two indexes. One shows the number of rows, and the other shows the number of columns. We took it as integer type only. How to represent it? Let me take three rows here. 1, 2, and 3. Let me take four columns. 1, 2, 3, and 4. You must be able to see that there will be 3 one-dimensional arrays. See, 1, 2 ,and 3. If we combine them, it will become 2d. So there will be three rows and four columns. 

Now we will define an index value for every row and column. Let's start with 0. 0, 1, 2. This is the number of rows. We will also give an index to the columns by starting from 0. 0, 1, 2, and 3. Its name will be 'a'. How to access an element? If I talk about this, how will we access it? a[0][0]. The name of its location is a[0][0]. Similarly, the second one will be a[0][1]. This will be a[1][0]. If we want to access a[1][3], which element will occur? a[1]. The row is 1. And what's the third... sorry! Since it starts from 0, this will be the fourth column. The element in this box will be accessed. This is a 2d array. 

Now we will also understand the 3d array. How will we define the 3d array? Let's take integer type only. And a, the name of the array. Here, another dimension gets added because it's a 3d array. So I entered a[2][3][4]. This is the number of arrays. The number of arrays. It will be clear to you. And this is inside the array... The number of arrays is 2. The rows in those arrays will be 3. And the columns will be 4. This is the number of rows and this is the number of columns. 

How will we represent it? Look, we have already created an array of 3x4. Remember one thing, the total number of elements will be 3x4. 3 is the number of rows. 4 is the number of columns. 3x4 will be 12. The total number of elements will be 12. Then, there are 2 arrays, one is already drawn. Let's draw the other one. Same! There will be 3 rows and 4 columns. Let's give dimensions to this too. 0, 1, 2. 0, 1, 2, 3. Look here, Its base address will be... look here. This is 0, and this is 1. 0 links the first array and 1 links the second array. Its name is 'a'. The number of arrays is 2. There will be 3 rows and 4 columns in every array. Look here, we have 2 arrays. 

See how we will access any element. If we want to access a[1][2][3], which element be accessed? The first array...sorry! The second array. Which one is the second array? This one. The second row of it... Which one is the second row? This one. And then the third. Here is the third one. Which element will be accessed? This element will be accessed. This way, the elements are accessed. What will be the total number of elements? 2x3x4. The total number of elements is 24. This way we can find the total number of elements. 

This is not the memory representation. This matrics form is not that. All the 2d or 3d arrays, which means all the multi-dimensional arrays are stored as 1d arrays in the memory. We just see it this way because we are representing it in front of you in this way. But when they take the locations inside the memory, they are stored as 1d arrays only. In the next video, we will study how to give them addresses and how to find the address of a particular element. We will see that in the next video. Thank you.