Welcome to Gate Smashers! The choice of the data structure depends upon our problem. There are some strengths and weaknesses of every data structure. We just need to see what our problem and requirements are. For example, we use linked list when the number of insertions and deletions is high and the size of the elements is unpredictable. Then we use linked list. In this video, we are going to discuss the strengths and weaknesses or the difference of array and linked list. 

Before that, subscribe to our channel because according to the analytics of your channel, a lot of students watch the videos without subscribing. So, I request you to subscribe to our channel. Like the video and let us know your suggestions by commenting. Let's start with the difference between linked list and array. 

The first difference is allocation. Memory allocation. Memory allocation is of two types. The first is the static allocation. The second is the dynamic allocation. We have already discussed that the array uses static allocation. That means that we will decide the number of elements required beforehand. And we won't add more elements to it. If we fixed the size of the array as 5, it will take the contiguous memory of 5. If the sixth element comes, it will not insert it and return an error. 

But, if we don't already know the number of elements that will be used, in that case, we use linked list because linked list uses dynamic allocation. That means that if we want one element, we use one node in the linked list. If we want two elements, we will take another node at the runtime. Right? What are linked list and array? I have given the links to the videos. You can watch them. 

The memory efficiency concept works in linked list. Suppose, we unknowingly used the array data structure and defined its size as 100 elements. In this case, we must know our problem. I took the size 100 but used data element 2 at the runtime. The rest of the space will get wasted. In the linked list, I needed only two elements, so I used only 2. I didn't need more than these, so I didn't use them. 

Here comes the second difference, which is memory efficiency. Here, linked list is memory efficient. But, if we consider a single element, array uses less memory. It is using small memory. If we already know that its size is fixed, 5 spaces have been used for 5 elements. 5 boxes have been taken. And a single box is being used. You know that we use two boxes for a node, the first for the data, and the other for the address. The storage of this pointer is being used in the linked list separately. Okay? We should decide the data structure after analyzing these strengths and weaknesses. 

The next difference is the search time. I have already prepared an array. Suppose that I want to search for the fourth element, how the search will happen? It will be direct as it is random access. The search time in array is very less. We can search the element in the constant time only. The reason behind it is that there is random access in it. How does it happen? We know that if we want to search for the fourth element, the base address of this, which is the address of the first element plus, the value of the offset/index of this. You know that the number of bytes of an integer depends upon the machine. Here, I have taken a 4-byte integer. We will add 3x4=12 to it and we can access it directly. There is no need to go in a sequence. 

But in linked list, if we want to search this element, we know that the pointer is given from the starting. The Head pointer points the first element. First, the element that we need to search will be compared to this element. Then it will jump to the next address. Then it will be compared to this on the next node. Then it will be compared to this. In the worst case, it will be compared with n. Its searching time will be order of n. So, there is a difference in the search time too. In array, it will be order of 1. In linked list, it will be order of n. Okay? 

The next difference is insertion and deletion. The insertion and deletion in array are very cost-effective. Insertion and deletion are done in very tough size. Let me explain it to you with an example of array. Suppose that I created an array. The elements inside it are 2, 3, and 4. We entered three elements. If we want to insert an element at this place, suppose that I want to add element 5. We will have to shift the positions of all these elements by 1. 4 will be shifted to the next position. Then 3 is shifted here. Now we will copy element 5 here. Okay? Look, it costs a lot to shift all the elements. We will have to shift all of them again in the next insertion. 

The deletion works the same way. If we want to do deletion in the array, how will we do that? Suppose that I entered 5, 2, 6, 9. If I want to delete 2, we will simply copy 6. And the next element will be copied here. So we will have to make all these changes in deletion too. 

But, in linked list... Suppose that we want to insert in linked list. Insertion can be done in 3 ways. We will discuss insertion and deletion in linked list separately in different videos. But, let me give you a hint. The head points here. All I need to do is... If I want to insert a node even at the beginning, its address will be transferred to the next part of this. And we will point the head point here. We have to make changes in the pointer in linked list whether it's insertion or deletion. This is a major difference between array and linked list. 

We discussed many differences in this video. I have created some points. What are the strengths of array? The search time will be fast as it is random access. Less memory needed per element. Okay? I just told you the strengths of linked list. Fast insertion and deletion time. Dynamic size. Efficient memory allocation/utilization. 

Their weaknesses are also given here. Slow insertion and deletion time. There is a fixed size. Inefficient memory allocation/utilization. There will be a slow search time in linked list. More memory needed per node because pointers are being used. Thank you so much!