Welcome to Gate Smashers! This is Naina, and I am going to explain to you Binary Search. Before that, subscribe to our channel if you haven't done it yet, and press the bell button to get all the latest updates. Let's start Binary Search. 

Till now, we studied Linear search in which we search every element sequentially until we get the desired element. We search for it in every element in the array. But in binary search, there will be two conditions. First, we can only use it in an array. Second, the elements must be sorted. Then only can we apply and implement binary search. Otherwise, we can't do so. 

Its working principle will be to always start searching for the element from the middle. That means that firstly, we will find the middle element of the list and compare it with the desired element. If the mid-element of the array matches it, that's fine. Otherwise, we know that our list is already sorted, what we will get to know from this is... Suppose that the element that we want to search is greater than the mid element, then obviously, we will start searching on the right side. This way, until we get the mid element equivalent to our item, we will keep repeating the same process until we find that element. If we don't find that element even at last while comparing it to the mid, that means that our search is unsuccessful. 

Let's learn its principle and try implementing it. I have created an array that consists of 9 elements. I have given an index to it which starts from 1 and goes on up to 9. To do the binary search, first of all, we need to find the mid. And what will we do to find the mid? Let me take two variables, B for the beginning, and E for the ending. (Beginning + Ending) / 2. What will be found out? The mid will be found out. What's the index here? We have to play all the games with the index number only. 1 is the beginning index and the ending index is 9. (1 + 9) / 2. We can find the mid element with this. We got our first mid as 5. What's its element? It's 78. 

Suppose that I want to search for our key element 87. We will compare it with the value of mid. We use the index of the mid to reach there. But obviously, the comparison will be by the elements of the index. We compared 87 and 78. Do they match? They don't match. Since they don't match, what will be the further process? We have to go to a new list and again find its mid. How will we go to the new list? We will check. The element that we need to search for is greater than the mid element. Since it's greater, in which direction will we move? We will move to the right side of the mid-element. So the first half of the elements will be neglected. 

Now the variable that we took as beginning and ending will get changed. In this case, the beginning variable will get changed while the ending variable will remain the same because we have to move this side. If we had to move the left side, the ending variable must have changed. Now the beginning will be mid index + 1. So the beginning is this. Now we will create a new mid while we already have a new beginning. What new mid will we get? 6 is at the beginning and 9 is at the end. (6 + 9) / 2. It will be 15 / 2. That means that we will take its floor value. The new mid that we will get will be 7. 

Does the value inside the mid match our element? It doesn't. Since it doesn't match, we will make another new list and repeat the same steps. Here is the element. Compare the mid element with our element. It's smaller than the mid element. Since it is smaller, we will go to the preceding list. That means that we will go to mid - 1. The mid was at 7, and the -1 is at 6. Now the ending one will move. Where will it come? Here. And the beginning one was already there. 

Now see, beginning + ending (6 + 6) / 2. We got 12 / 2, which means 6. The new mid is 6. Now we will compare this with our element. Do they match? Yes, they do match. So finally, our search is successful. This is binary search. 

Now we will analyze its algorithm to learn how it works. Look here, A is the array, N stands for the total number of elements inside the array. What does the 'item' variable mean? It is the key element that we need to search for, on which the whole algorithm is based. We have also taken a local variable named Location. If we don't find any element and the search becomes unsuccessful, it will return -1. -1 means that the search is unsuccessful. 

Then see, the beginning variable starts from 1 and the ending one goes up to N. This was just an example. In the algorithm, we will only consider N. Now we ran a loop. We ran a loop because the same process is happening repeatedly. How so? We have to find the mid and then compare the mid with the element to find out whether they match or not. That complete process will happen in a loop. 

So here, we found the mid: (B + E) / 2, and take its floor value into the mid. Then we will check whether the item is equivalent to mid or not. Here we found the mid value and here we have compared it to the element. A[mid] means the element and mid means its index. Don't get confused here. If the item matches with A[mid], the loop will exit and we will find the item that we were searching for. We found it in the array. 

If it doesn't happen, if the item is greater than the mid, what did we do? We moved towards the right and shifted the beginning one by making it mid + 1. Then, if the item is smaller, we will shift the ending by making it mid - 1. 

Now see, the condition that will apply here will be that the beginning must be less than or equal to the ending. So at last, what was the beginning? What was at the last here? 6 and 6 were there. They are equal. Suppose, the key item that we were searching for was 86. What would we do if it was 86? We would make the ending position mid - 1. Look here, we would make the ending position mid - 1 if it was 86. We would have reached here. Our ending would reach here, and the beginning is already here. So when the ending will be 5, and the beginning will be 6, the condition will be False. If the condition is False, it will not enter the loop. That means that our search will become unsuccessful. 

In this case, it will return LOC. -1 was already lying in LOC, which means that our search has been unsuccessful. If it gets successful, it will match the mid item, and it will return this LOC in which we have stored mid's value. The value of the mid will be returned. If it gets successful, it will exit from here. And if it gets unsuccessful, it will not enter the loop. 

This is all about Binary Search. We will discuss its time complexity in the next video. Thank you!