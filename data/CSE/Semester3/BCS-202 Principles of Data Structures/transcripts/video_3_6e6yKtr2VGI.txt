Welcome to Gate Smashers. I am Naina and in this video, I will discuss a very simple and widely used data structure, that is Array. This is used in high-level languages like C, C++, and Java. The array is also used for implementing data structures like stack and queue. Let's start with the definition of Array. 

An array is a data structure which can be defined as a finite ordered set of homogeneous elements. Let's understand it by breaking it out. See here, what does 'finite' mean? An array is a finite data structure. It means that the number of elements that we can store in an array is fixed. That means that we will define the number of elements used in the array beforehand. Suppose that I decided to use 5 elements. I will have to define the number of elements as 5. So here, finite means fixed elements. Fixed elements. 

The next is ordered set. Ordered set means that every element will be in a sequence and every element will be denoted by a number/index. For instance, the first element will be in the first position. Then second, then third, and so on. This is an ordered set, which means that every element will be arranged in a sequence. 

Its next property is the homogeneous element. Homogeneous means that the type of each element will be the same. For instance, if the element is integer, all the elements in the array of which we initially decided the number of elements, all the elements in that array must be integer. Or they must be float of char type. You can't take half elements as integer and half float or mix a rector inside it. It's not like this. The elements must be homogeneous. That means that its type must be the same. 

The next property says that the elements of the array will be stored in the contiguous memory. It means that all the elements will be stored together in the memory. They will not be stored separately in different locations in the memory. The sequence of their memory will also be contiguous. Firstly one element, then the element that will be along with it will be there. Then 3rd, then 4th, and so on. Its elements are stored in contiguous memory. 

The next property is Random Access. What does Random Access mean? If we have 10 elements in an array and we want to access the 4th one, we don't need to cross the 3 elements one-by-one, we can access it directly. That means that we can do random access. 

Let's understand it with an example. Suppose you go to a party, and the host makes you play a game and fixes the number of participants. Suppose they fixed the number of participants as 5 and they also decide that only males or females can play this game. Doesn't it form an array? The number of participants is fixed and the type is either male or female. The 1st participant will get the 1st position and they will be given a number. The 2nd participant gets number 2, the 3rd one gets number 3, the 4th one gets number 4, and the 5th participant gets the number 5. But, if a 6th participant also comes and asks to play, they will not be allowed because the number of participants is fixed. Apart from this, if I say that only females can play and the males will not be allowed, all 5 participants must be females. You can say it's a disadvantage of arrays as the number of elements is fixed. You can comment on what you think is the advantage and the disadvantage of it. 

Let's understand about array in more detail. Let's consider an example. Before that, let's define an array. The array I have taken is an integer type. Its name is 'A' and its length/size is 5. This is the name of the array. 'A' is the name of the array. And this is the total number of elements in the array. I have used the syntax of C and C++. Every language has its own different syntax. So don't get confused. 

I have drawn the 5 locations of the array inside the memory here. We will give it a sequence. For example, I considered the first location as 1, second as 2, third as 3, fourth as 4, and the fifth one as 5. In C and C++, an array is always started with 0. But since I am giving you a general example, I have started it from 1. You will also get it from 0 in many books: 0, 1, 2, 3, 4. But the total number of elements is 5. The number of elements is fixed as 5, the sixth element cannot be added to it. 

If we want to access the first element... As you already know, we have taken the name of all the five elements as 'A'. We will not give different names to different elements. A unique name will be given to every element but its position will be determined by its index number. These are its index numbers. We can access the first element by A[1]. We can access the second element by A[2]. A[3] means the 3rd element, A[4] means the 4th element, and A[5] means the 5th element. 

If we want to add something to it, we will assign the marks of any random student, that is 40. Here, I entered 50, here 55, 35, 42. Suppose I entered the marks of the students. Let's put the value in A[1]: 40, 50 on the second position, 55, 35, 42. You just have to remember that the position of every element is determined by its index number. What's the index number here? The index number of the first element is 1, second 2, third 3... These are not the memory addresses. They will get separate addresses. 

As you know that every element gets some space inside the memory after getting defined. You know that the array is contiguous memory. Suppose that I gave the address 1000 to the first element. Remember one thing, the size of an element depends upon the machine. If it takes 1 byte, the address will be accordingly. And if it takes 2 bytes, let's assume that our computer takes 2 bytes for integers, the first element got this address. Then the second element gets the address only after leaving 2 bytes. Since we know that it is a contiguous memory, we have to give the addresses in a sequence. We will give the address leaving 2 bytes. Then, the next address to the next element. Next... leaving two bytes. Again, give the next address by leaving two bytes. This is the memory of 5 elements which is contiguous. 

This was all about the elements in the memory, how we can access them, there is a defined position for it, and they use contiguous memory. Apart from this, if we want to find the length of the array, first of all, the index at which the array starts will be called the lower bound. And the last element will be called the upper bound. To find the total number of elements or the size of the array, the formula will be UB - LB + 1. 

What's this? This is the size of the array or the length of the array. The upper bound is 5, minus lower bound, which is 1, plus 1. The total number of elements is 5. If we start from 0, the upper bound is 4, minus lower bound is 0, 0 + 1. The total number of elements came out to be 5. So don't get confused about whether to start with 1 or 0. You can take anything here. I used a general array and explained you by starting from 1. 

This was the introduction part of array. We will discuss arrays in more detail in the upcoming videos. Thank you!